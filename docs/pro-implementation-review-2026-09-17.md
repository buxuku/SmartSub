# 专业版路线图实现评审与修复记录

- 评审基线：`6e17039`，分支 `feature/lxd/ui`。
- 范围：正文第 1、2 节及 Phase 1、Phase 2 全部验收项；之后顺序实施正文第 3、4 节及 Phase 3、Phase 4。
- 状态：第一、二部分已提交并推送 `c6f15acade6cc49eb0f90c940df89b07f6b7f620`；2026-09-17 再次读取远程 `feature/lxd/ui` 确认一致。第三、四部分的主要功能与治理已实现，本阶段随本记录提交；P3-03 稳定 60 FPS 与外站/跨平台验收限制见文末“当前交付状态”。路线图中的勾选按该范围解释。
- 方法：源码追踪、确定性失败注入测试、真实 Electron + Playwright 界面测试、产物与磁盘状态断言、截图检查。应用界面自动化不等同于接管整个 macOS 桌面；原生确认在独立测试实例中预设响应，实际应用事件处理器仍执行。

## 评审结论

原始基线 `6e17039` 不能认定为完整实现第一、二部分：存在虚假保存、编辑恢复缺失、导航绕过、云服务状态误报、批量工具不完整等问题，测试不足以支持路线图中的勾选。以下记录 27 项问题及修复证据。修复后已补齐 Phase 1、Phase 2 的工程实现和核心验收路径，最终检查见文末；第 3、4 节尚未完成，不计入本阶段交付。

本文前半部分为需求与缺陷矩阵，后半部分按实施顺序保留历史验证记录；历史失败不代表当前仍失败，也不删除曾经失败的证据。“零数据丢失”“零微秒误差”是目标，不能作为涵盖任意硬件故障和所有媒体格式的无条件保证。

## 发现与修复

| 编号 | 严重度 | 问题与源码证据                                                                                                                  | 当前处理和验证                                                                                                                                                                                                                          |
| ---- | ------ | ------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A01  | P0     | `useStandaloneSubtitles.ts` 未持久化编辑草稿，刷新或崩溃可能丢失工作。                                                          | 加入按字幕路径隔离的内存及 LocalStorage 草稿、恢复/放弃选择、存储失败提示。组件测试与真实刷新恢复通过；系统级断电不在已验证范围。                                                                                                       |
| A02  | P0     | 字幕保存仅检查 `error`；缺失响应或 `success:false` 可被当作成功，保存期间新编辑可能被清空 dirty。                               | 严格 `success === true`、快照比较、共享保存 Promise、常驻错误。组件测试覆盖畸形响应、失败、并发保存和保存期间编辑。                                                                                                                     |
| A03  | P0     | `NavigationGuardContext.tsx` 忽略 query，取消后退会破坏历史，只保存第一个 dirty guard。                                         | query 比较、历史游标恢复、多项保存、放弃回调及主进程未保存确认。新增同步 dirty 二次检查，防止第一份内容在第二份保存时又被修改；稳定注册顺序，连续历史事件按真实索引恢复。组件竞争测试与真实多项保存、连续后退通过。                     |
| A04  | P0     | `workItemStore.ts` 显式保存后延迟 800ms 写盘，IPC 可提前返回成功；删除/重命名失败会先改变内存。                                 | 显式保存等待 electron-store 原子写盘；失败不发布内存状态；高频进度仍合并写入并重试。退出前 flush 失败取消退出。真实目录权限失败与成功响应后 SIGKILL/重启测试通过。                                                                      |
| A05  | P0     | `proofreadData.ts` 两处直接覆写 sidecar JSON，存在截断风险；`proofreadStore.ts` 共享数组引用可在保存失败前污染已保存数据。      | 复用已有原子写入；校对条目深拷贝隔离。确定性存储失败测试覆盖更新、完成、追加、删除；文稿/原子文件回归 74 项通过。                                                                                                                       |
| A06  | P1     | `proofread.tsx` 批次未可靠区分保存失败；`updateProofreadTask` 对不存在任务返回 `success:true,data:null`；空列表删除未自动保存。 | 保存状态机、持久错误、严格 ID 校验、空列表保存。真实批次删除后保存失败、离开阻断与手动重试通过。新建批次保留前端条目 ID。                                                                                                               |
| A07  | P1     | `Layout.tsx` 本地模型状态含云 ASR，初始未知可显示就绪；配置凭据不能证明连通。                                                   | 本地模型独立判断；云服务初始未验证，仅记录主动真实测试结果，5 分钟过期，错误不显示绿色。ASR/翻译/TTS 健康记录；真实 HTTP 状态栏回归通过。                                                                                               |
| A08  | P1     | 首版健康缓存把 `strictStructuredOutput` 测试标志作为配置，翻译测试成功后无法匹配保存的服务商；缓存无容量上限。                  | 忽略此测试控制字段，递归稳定配置序列化、容量限制、过期清理、脱敏副本。缓存单测及真实成功/失败/凭据变化/重试通过。                                                                                                                       |
| A09  | P1     | `TaskWizard.tsx` 自动恢复而不询问，草稿遗漏配音、压制与门禁配置。                                                               | 显式恢复选择、完整 pipeline 草稿、Zod schema、存储配额失败常驻提示及导航阻断/重试、内存输入输出深拷贝。向导配音改为任务内状态，不再写工作台 dubbingConfig；恢复/往返/隔离/丢弃及缺模型安装自动回填均通过真实测试，安装交接细节见 A23。  |
| A10  | P1     | Phase 2 声称全工具批量队列，实际多数面板仍为单文件；取消、失败重试语义不一致。                                                  | 8 工具共用顺序队列；独立状态、进度、去重、失败重试、取消锁、多文件原生拖拽、导航守卫。真实双输入、产物、取消再启动与只重试失败项通过。                                                                                                  |
| A11  | P1     | 工具输出覆盖已有同名文件，无效指定目录可能静默回落；同语言内封字幕输出冲突。                                                    | 独占预留唯一输出路径，无效目录返回错误，失败仅清理本次预留文件。内封字幕按轨捕获失败并保留部分输出，重试跳过仍存在的成功轨；接入任务取消和进度，等待进程 close 再解锁。真实第二轨权限失败/恢复重试通过，FFmpeg 测试覆盖扫描与轨间取消。 |
| A12  | P1     | 工具完成行动条仅带入首个结果。                                                                                                  | 翻译/配音向导携带全部结果；校对通过重复 file 参数导入所有字幕；单视频压制入口提供每份输出的选择菜单。真实多产物向导、校对批次与压制菜单验证通过。                                                                                       |
| A13  | P1     | 帧率虽定义标准比值，最终时间计算仍为浮点乘法。                                                                                  | BigInt 独立换算毫秒时间戳；7207.2 秒、24000/1001 fps 素材经生产压缩/硬压制/软封装通过，三个版本全部 172800 帧逐帧 PTS 精确一致，首中末字幕像素边界与软字幕时间验证通过，见长时间轴记录。                                                |
| A14  | P1     | 四业务场景缺短剧，角色、术语、语言风格与排版承诺未接通。                                                                        | 已补独立短剧及角色/术语/语气词/口语化翻译/双行排版，真实角色模型及生产语言链路通过。最终引擎、适配器、精修、角色和交付测试通过；确定性 HTTP 响应不代表 AI 质量验收。                                                                    |
| A15  | P1     | 向导与普通任务验证分离；普通任务配置写全局或只存文件，切项目可能串表单。                                                        | 三入口共用提交状态机；普通任务完整项目草稿与运行快照分离，串行持久化、错误守卫、进度隔离。真实配置/文件恢复、模式切换、磁盘失败/重试、SIGKILL、全局隔离通过。                                                                           |
| A16  | P1     | `workItemStore.ts` 迁移先写版本再写数据且提前删除旧 tasks；恢复遗漏 refine/manuscript/speaker 阶段。                            | 迁移数据与版本同次提交，旧数据保留；补齐中断阶段。失败重试迁移与中断状态单测通过。                                                                                                                                                      |
| A17  | P1     | 测试未接入脚本、renderer 旧测试依赖缺失且类型过期；仅验证无页面滚动会漏掉内部裁切。                                             | 加入可执行组件/队列/存储/健康及独立 Electron E2E。配置栏按容器分为核心一行和高级操作一行；1024/1440 控件边界和 hit-test 通过。已修复旧测试并纳入全量类型检查，54 项 Jest、类型检查、构建通过；16 组基线回归加入 CI。                    |
| A18  | P1     | 向导和任务行重试直接发送 handleTask，绕过 TaskControls 的云端音频上传确认。                                                     | 三个入口共用 useTaskCloudConsent；取消不派发，单次同意不持久化，记住同意才写配置；确认后再次刷新依赖。真实测试经过生产保存、队列、提取音频及导出字幕，仅向本机 ASR 测试服务发送短片，无外部上传。                                       |
| A19  | P1     | 视频区间验证失败会阻断预览加载，用户无法纠正；双语拆分第二次路径预留失败留下空文件。                                            | 预览与导出区间验证分离，切换素材重置播放位置；双语先序列化再预留且回收自身占位输出，拒绝无字幕的次文件。单测及逐文件区间 E2E 通过。                                                                                                     |

| 编号 | 严重度 | 问题与源码证据                                                                                                                                             | 当前处理和验证                                                                                                                                                                                                                                                                                                                             |
| ---- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| A20  | P0     | 向导 saveTaskProject 仅保存文件，单向 handleTask 后立即清草稿；配置延迟落盘，后台未接受也可能跳转。                                                        | 新增 submitTask 应答协议，文件/配置/请求回执同次原子写盘后同步入队；持久化失败不入队，繁忙文件/配置冲突拒绝。请求历史防重复，草稿保存稳定工程 ID 与请求编号；延迟应答、畸形应答、回复丢失后刷新重试、提交期间新编辑的草稿保留均验证。                                                                                                      |
| A21  | P1     | gateManager 先保存 passed 再异步尝试启动，失败后不能正常再次放行；校对包壳放行失败仍跳下一项。                                                             | passed 与续跑配置一起由提交协议原子保存；失败保留 review 并可重试，校对包壳仅成功后切下一项。真实目录权限故障、字幕/配音两级门禁续跑及全自动真实 TTS/FFmpeg 压制通过。                                                                                                                                                                     |
| A22  | P1     | 任务运行事件仅凭文件 UUID 找工程，复用草稿文件记录时可能更新另一工程；renderer 也未校验工程归属。                                                          | 主进程按 AsyncLocalStorage 工程上下文定位工作项；运行事件携带 taskProjectId，界面过滤并在切换工程时清空旧缓冲。生产队列测试覆盖同 UUID 的两工程独立更新，组件测试覆盖无关事件与旧缓冲隔离。                                                                                                                                                |
| A23  | P1     | `home.tsx` 缺模型时仅保存 files，安装后 session 文件交接跳过完整草稿恢复，丢失配方；再次导入会静默覆盖旧文件；下载弹窗不区分进度来源且有未清理的延迟跳转。 | 导入保存独立 ID、完整配置、目标及 pipeline，按 ID 自动恢复；已有草稿明确追加/替换，追加保留意图并去重。存储失败阻断安装/跳转，内存保留并可重试。下载严格回执及安装复验、请求去重、进度过滤、源选择、按请求与窗口归属取消、失败常驻；状态栏安装完成立即刷新。真实官方 Base 下载、SHA-256、取消/503/重试、完整恢复及内置快捷工程独立性通过。 |
| A24  | P1     | `TaskDraftManager` 初次读取失败或 schema 损坏返回 null，向导随后保存空草稿会删除原数据。                                                                   | 显式区分读取失败和无草稿，读取失败时禁止自动覆盖，包括空列表删除；界面提供重试读取/明确放弃。单测覆盖读取异常、损坏内容、原始字节保留；真实界面验证修复数据后恢复，以及首页明确替换损坏草稿。                                                                                                                                              |

## 需求验收矩阵

| 路线图要求                | 当前证据                                                                                                                   | 结论                                                                                                    |
| ------------------------- | -------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| §1.2 零虚假保存、可信状态 | A02–A08、A20–A27 单测与真实存储/导航/提交/参数测试                                                                         | 已修复已发现缺陷，核心故障及恢复路径通过；不声称任意故障下零风险。                                      |
| §2.1 / P0-01 全域守卫     | 侧栏、Cmd+K、query、前进/后退、连续后退/跨两条历史、刷新/退出/关窗、多 dirty guard                                         | 真实入口通过；保存期间编辑由生产 hook + 多 guard 组件测试验证。任意外部浏览器历史不在当前已验证范围。   |
| §2.1 / P0-02 保存状态机   | strict success、失败常驻、重试、磁盘故障、SIGKILL 恢复                                                                     | 已修复并验证主要路径。                                                                                  |
| §2.1 / P0-07 双层草稿     | 字幕与向导双层草稿、显式恢复、完整 pipeline、schema/配额故障、接受回执/刷新重放/提交期间新编辑、安装自动返回、损坏草稿保护 | 上述路径及最终 Electron 恢复回归通过。                                                                  |
| §2.2 / P0-05 帧率精度     | 分数定义、整数时间缩放、2 小时生产压缩/压制/软封装、全帧 PTS 与字幕像素验证                                                | 校准路径通过，无累积误差；SRT 毫秒、ASS 厘秒量化不能称为无限微秒精度，也不替代所有容器/变帧率素材验收。 |
| §2.3 / P0-03 健康度       | 本地/云/GPU 独立，过期与配置变更失效                                                                                       | 缓存及真实 HTTP 状态栏验证通过，不自动发起计费测试。                                                    |
| §2.3 / P0-04 配方语义     | 两级门禁停靠/写盘失败/重试，四内置卡片原生拖拽，真实 Whisper Base、翻译、配音和 FFmpeg 成片                                | 默认字幕人工把关与显式全自动均通过；本机翻译/TTS 固定响应不代表外部服务质量。                           |
| P0-06 双语拆分拖拽        | 原生文件拖拽与真实拆分、合并                                                                                               | 通过。                                                                                                  |
| P1-01 零折行配置栏        | 中文向导、英文普通任务及超长引擎/模型/提供商，1024/1440 控件边界、hit-test、截图                                           | 通过上述视口；额外修复顶部模式标签裁切，小屏工具按钮独立一行，标签全部可见可点。                        |
| P1-02 统一任务状态机      | 统一提交/复验/应答、配置草稿/快照隔离、失败守卫、项目/模式切换与崩溃恢复                                                   | 单测、真实应用恢复、类型检查和最终构建通过。                                                            |
| P1-03 四套业务预设        | A14，真实角色模型、ASR 适配器、语言策略和交付格式回归                                                                      | 四套配置及处理链已实现；固定模型响应只证明集成，不证明开放式模型语言质量。                              |
| P1-04 输入即草稿/安装返回 | 真实原生拖放、完整配方落盘、官方 Base 下载与哈希、失败/取消/重试、按 ID 自动恢复、旧草稿选择、配额与损坏数据保护           | 这些路径通过，未运行转写的源素材不变。                                                                  |
| P1-05 八工具闭环/批量     | 多输入、真实输出、取消、重试、部分输出恢复、向导/校对/压制菜单承接                                                         | 八工具最终 Electron 回归通过。                                                                          |

## 可复现验证记录

测试依赖开发 renderer（默认 `http://localhost:8888`）及本机 Electron。工具测试从授权目录的 `demo.mp4` 派生短片；产物写入系统临时目录，源素材不覆盖。故障注入只作用于脚本创建的独立配置/素材目录。

| 命令                                                                                       | 最近结果与覆盖                                                                                                                                                                                                                                                                                                                                                             |
| ------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `npm run test:work-item-durability`                                                        | 通过：同步保存、失败不提交内存、条目 ID、延迟写重试、退出 flush、原子迁移、中断恢复。                                                                                                                                                                                                                                                                                      |
| `npm run test:provider-health`                                                             | 通过：嵌套字段顺序、测试控制参数、凭据变更、TTL、时钟回退、TTS、脱敏、缓存容量。                                                                                                                                                                                                                                                                                           |
| `npm run test:proofread-reliability`                                                       | 通过：畸形/失败响应、草稿恢复、并发保存、保存期间编辑。错误日志为主动注入。                                                                                                                                                                                                                                                                                                |
| `npm run test:manuscript`                                                                  | 74 项通过，含原子文件替换失败保留原文件回归。                                                                                                                                                                                                                                                                                                                              |
| `npm run test:toolbox`                                                                     | 10 个脚本通过：转换/双语/同步、队列锁/取消竞争、输出唯一性、无效区间预览、真实内封字幕部分失败/取消/恢复。                                                                                                                                                                                                                                                                 |
| `npm run test:navigation-save` / `npm run test:proofread-reliability`                      | 通过：同步 dirty 优先于旧渲染状态，已保存编辑器再次被修改、新 guard 加入、失败阻断；生产字幕 hook 参与竞争用例。                                                                                                                                                                                                                                                           |
| `npm run test:task-readiness` / `npm run test:task-draft`                                  | 通过：所选引擎、运行时、提供商、配对文件、已产出字幕复用、每次启动刷新依赖、深拷贝/schema/配额故障。                                                                                                                                                                                                                                                                       |
| `npm run test:e2e:storage`                                                                 | 通过：真实权限错误、磁盘/内存保留、导航阻断、恢复权限重试、SIGKILL/重启。证据 `smartsub-storage-e2e-ka7sPN`。                                                                                                                                                                                                                                                              |
| `npm run test:e2e:batch-save`                                                              | 通过：创建、删除后更新失败、常驻错误、保存离开阻断、重试。证据 `smartsub-batch-save-e2e-ap8EAt`。                                                                                                                                                                                                                                                                          |
| `npm run test:e2e:proofread`                                                               | 通过：侧栏、前进/后退、连续后退与跨两条历史取消、query、Cmd+K、原生刷新/退出/关窗取消、恢复、多项保存失败重试。证据 `smartsub-proofread-e2e-4CINeD`。                                                                                                                                                                                                                      |
| `npm run test:e2e:wizard`                                                                  | 通过：独立配置、完整 pipeline、工作台配音偏好隔离、专家入口、往返、配额故障守卫/重试、丢弃、1024/1440 每个配置栏控件可达。最新证据 `smartsub-wizard-e2e-OLirB1`。                                                                                                                                                                                                          |
| `npm run test:e2e:launchpad`                                                               | 通过：真实原生配方拖放、完整草稿预存、跨模型进度隔离、非所属请求取消无效、所属请求取消保留草稿、旧草稿取消/追加去重、HTTP 503 常驻失败/切源重试、官方 Base 下载、安装后自动完整恢复、排除陈旧 session 交接、底栏更新、配额故障恢复、损坏数据保留/读取重试/明确替换、两个内置快捷任务工程不覆盖向导草稿。证据 `smartsub-launchpad-e2e-Ik2f4y`，截图 `restored-recipe.png`。 |
| `npm run test:e2e:readiness`                                                               | 通过：三个入口真实保存/队列/ASR/字幕产物、确认期间删除服务阻断、目录权限失败保留草稿、延迟应答不清草稿、丢失应答后刷新重放不重复执行、提交期间新编辑另存草稿。证据 `smartsub-readiness-e2e-Jh44L2`；仅本机 HTTP 服务，无外部上传。                                                                                                                                         |
| `npm run test:e2e:gates`                                                                   | 通过：字幕人工停靠、写盘失败/重试、配音生成/人工停靠/放行不重复 TTS、全自动配音与压制，产物非空且可解码、输入字幕不变。证据 `smartsub-gates-e2e-RRE5XU`。                                                                                                                                                                                                                  |
| `npm run test:task-submission`                                                             | 后台生产模块和 React hook 测试通过：严格回执、写盘回滚、请求重放/冲突、进程重启后幂等、输入深拷贝、并发锁、卸载取消、同 UUID 跨工程隔离。                                                                                                                                                                                                                                  |
| `npm run test:subtitle-output` / `npm run test:launchpad-import` / `npm run test:pipeline` | 分别 317 项导出检查及三任务类型格式 UI、13 项导入回归、51 项流水线逻辑检查通过。导出故障注入收紧为已存在 JSON 的最终元数据更新，避免前置 sidecar 原子写入消费故障。                                                                                                                                                                                                        |
| `npm run test:e2e:health`                                                                  | 通过：真实本机 HTTP 成功/401、状态栏变化、改凭据失效、重试恢复、无自动网络测试。证据 `smartsub-health-e2e-21Ayol`。                                                                                                                                                                                                                                                        |
| `npm run test:e2e:toolbox`                                                                 | 通过：8 工具双输入、原生拖拽/真实输出、第二字幕轨目录权限失败与保留首轨重试、双语配对、逐文件区间、GIF 取消、校对/压制菜单/向导承接、1024/1440。证据 `smartsub-toolbox-e2e-8vH7nP`。                                                                                                                                                                                       |
| `npm run check:i18n` / `git diff --check`                                                  | 通过，提交前重跑。                                                                                                                                                                                                                                                                                                                                                         |
| `npm run typecheck`                                                                        | 最终 renderer/main 全部通过，包括旧测试；初次审计的 184 条诊断已修复，没有排除测试来掩盖错误。                                                                                                                                                                                                                                                                             |

证据前缀：`/var/folders/7w/s752k0b92b5bntbh5jkxcgyh0000gn/T/`。本机截图不作为二进制提交，脚本可重新生成。

安装 E2E 从 Hugging Face 官方源经生产下载器下载真实 `ggml-base.bin`，大小 `147951465` 字节，SHA-256 `60ed5bc3dd14eea856493d334349b405782ddcaf0028d4b5df4088345fba2efe`。模型位于独立测试 profile-dev 的 whisper-models 目录，未更改用户模型目录；测试不向外部服务上传素材。内置任务独立性断言以离开向导后的稳定草稿为基线，避免把向导加载时正常模型默认值校正误认作任务页污染。

历史补充检查曾暴露代理 Agent 签名、ConfigurationManager/ParameterValidator/MigrationManager 重复实现及旧类型、文稿索引签名和 sourceSrtSaveOption 缺失。最终已修复上述诊断，并使用与实际 webpack 构建一致的 bundler 模块解析进行 main 全量类型检查。

## 场景预设处理链补充验证

本节记录 A14 的第一轮修复，当时尚未完成全部场景；后续角色、语言和交付补齐见下文：

- 引擎档位映射移至无运行时依赖的 `types/subtitleOutcome.ts`，主进程和 renderer 使用同一映射。任务级 VAD 阈值/时长优先于全局和档位默认；非法任务值不覆盖有效设置。
- 访谈预设接入宽度 28、最长 3 秒、停顿 0.25 秒和保留原声停顿。共享词级断句、碎片合并、最短显示时长共同执行这些限制，避免后处理再次填满停顿。段级引擎只能保持原段端点并按文本权重估算内部切点，不能声称恢复了未提供的词级时间或段内静音。
- 内置/云端/faster-whisper 词级路径共用规则；CLI 补上段级后处理，写入成功后才报告转写完成。faster-whisper 混合或缺失词级数据时分别处理有词时间的连续片段和段级估算，避免把整段当作单词。
- AI 断句的物理护栏同样尊重保留停顿；段级 AI 输入在已知停顿处分窗。超时长但不超宽的段级结果也会重拆，合并与显示延长不得突破设置上限。修正降级结果给每条 cue 重复附带整个词窗口的隐患。
- 专家模式从当前档位物化有效参数，支持实际引擎可用的 VAD 滑块、字幕时长/停顿及保留停顿开关；切换预设清除旧专家字段。滑块名称传至可聚焦控件，键盘可调。中英文移除错误的“全局设置”文案。

本轮验证：`test:engines` 851 项及 sherpa worker 通过，`test:scenario-presets` 通过，`test:refine` 66 项和 validation 30 项通过，`test:task-draft` 通过。`test:e2e:presets` 通过真实 Electron 预设选择、转专家、键盘阈值调整、刷新恢复、全局设置不变、1024/1440 视口和真实本机 HTTP ASR 管线，最终 SRT 三条分别保留 0–200、600–800、1200–1500 毫秒时间。素材来自授权目录的短片副本，没有外部媒体上传。最新证据 `smartsub-presets-e2e-Tmonrf`，含 `expert-1024.png`、`expert-1440.png`、`completed.png` 和实际 SRT；截图已人工核对。此 E2E 使用确定性 ASR 响应，不证明本地模型识别准确率。

适配器补充回归 `scripts/test-scenario-adapters.cjs` 已纳入 `test:scenario-presets` 并通过：使用生产 faster-whisper 适配器和确定性运行时响应，检查真实下发 VAD/抗重复参数、词级与段级混合输出不重复/不丢字；CLI 使用真实 Node 子进程生成 SRT，验证段级切分及退出成功但产物缺失时不得发送 done。证据目录 `smartsub-scenario-adapters-pwFTyT`。该测试未运行真实 faster-whisper 模型。

本阶段最初未补齐独立短剧、网课术语/语气词、双行布局及情感翻译，后续实现与证据见下方新增记录。此时存在的 184 条旧测试类型诊断已在最终回归前修复。

### 访谈角色分离链路

- 移除向导、配方和配音/合成任务对角色配置的静默清除；访谈预设开启本地聚类，模型与运行库缺失时明确阻止启动，字幕文件单独输入不运行角色分析。
- 配对媒体提取音频但跳过 ASR；专家入口在不翻译时仍可达，允许关闭/开启角色分离，阶段链同步显示。已完成角色数据的下游续跑可在模型移除后复用；sidecar 丢失或强制弃用内封字幕重新 ASR 时不可豁免依赖。
- 推理失败/无结果等降级以常驻警告记录；重新提取音频或意外阶段异常明确失败，不永久 loading。标签标注使用原子替换，多文件失败或取消回滚已提交文件；保护输入路径及其硬链接/软链接，成功聚类仍写角色 sidecar。
- 新增生产 stage 失败注入 65 项检查，覆盖运行库/模型不可用、同步和异步推理失败、空结果、重复标注、输入链接保护、输出链接一致性、磁盘替换失败和取消回滚。`test:speaker-diarization` 共 10 配置 + 65 stage + 31 对齐检查通过。`test:subtitle-output` 增至 338 项，覆盖配对提取/跳过 ASR、角色落盘、续跑、sidecar 丢失重建及持久告警；`test:task-readiness` 通过。
- `test:e2e:speakers` 使用授权目录两段真实语音，经实际 sherpa 模型聚类为两名说话人，四条 cue 角色进入校对 sidecar 和配音会话；导入 SRT 内容不变，四次本机 HTTP TTS 请求不朗读角色标签。临时移开模型后放行配音门禁成功，未重复推理或 TTS。首次运行经生产下载器下载实际模型；复测复用隔离目录缓存。最新证据 `smartsub-speaker-e2e-qHeCxl`，缺模型/关闭再开启/恢复草稿/配方持久化均通过，两个截图已核对。
- `test:e2e:presets` 再次通过，证据 `smartsub-presets-e2e-DbKwIo`。该暂停断句用例显式关闭角色分析以隔离 ASR 测试；独立角色 E2E 承担实际模型覆盖。本机固定 TTS 响应只验证调用与状态恢复，不证明语音合成质量。

### 四场景语言策略与交付排版

- 已补齐独立短剧预设；网课启用去幻觉档位、AI 校正和空洞犹豫词清理，影视/短剧保留情绪语气词并使用口语化翻译。术语校正仅下发源语言标准写法，避免把译词写回原文；原文回显校验失败时保留原句并显示本地化常驻警告及黄色阶段。未配置 AI 时明确阻止依赖 AI 的操作，普通机器翻译不能假装执行情感风格。
- 影视/短剧启用单语双行、双语每语言一行的交付策略。SRT/VTT/ASS 共用排版，不改 cue 数量或时间；校对保存沿用任务排版，sidecar 与配音输入保留原始正文。行宽是目标而非截断上限：不可分割长词或过长角色名保持完整。ASS 仍遵循格式固有的厘秒量化。
- 修复双语重排丢失显式角色标签、长角色名被切断、混合中英文无空格边界重试变化。分词沿用 ICU，边界仅选择可无损合并的位置；宽度前缀累计避免逐候选扫描全文。导入字幕保持只读，排版只作用于任务生成的交付物。
- `test:scenario-presets` 通过预设映射、实际适配器参数与 57 项语言策略检查；`test:subtitle-output` 最新 368 项及三入口格式 UI 通过，覆盖标签保留、双语导出、校对后重排、格式转换失败重试和纯文本配音来源。
- `test:e2e:scenario-language` 最新证据 `smartsub-language-e2e-vEKe7I`：真实 Electron 选择网课/影视/短剧，生产 ASR/校正/翻译/导出链路，验证模型请求策略、术语、机器翻译阻断及切回常规、两行产物、未改时间和校正拒绝提示。HTTP 返回为本机固定响应，只证明请求和处理链，不证明模型语言质量。
- 实际角色模型复测 `smartsub-speaker-e2e-MjaTHb` 通过，新增双行配置下 source-sidecar 配音路径，四条 TTS 请求正文逐条等于原始 cue；原始配对 SRT 不变。已核对角色警告截图及语言校正警告截图。
- `test:e2e:presets` 最新 `smartsub-presets-e2e-GDFQkm` 通过。A14/P1-03 的四套业务配置及上述处理链已补齐，但全引擎最终回归与真实语言质量验收仍不能由固定响应代替；不据此宣布整个 Phase 2 完成。

### 普通任务配置隔离与可靠保存

- A15 进一步确认：普通任务在导入前写全局默认配置，导入后只保存文件、不保存配置；同一页面切到新项目可能保留前一项目表单。已改为项目内 `taskDraft`，包含配置、文稿池和文件列表，与已接受的 `configSnapshot` 分离。空文件的配置草稿也可落盘，预设不会提前把未运行草稿变成只读。
- 项目加载完成前禁用编辑与启动；读取失败显示错误并可重试，不把读取失败解释为空项目。切到新项目重新读取默认值，返回旧项目恢复草稿；兼容模式切换先确认保存再迁移项目类型。
- 保存队列串行写盘，只确认实际落盘的版本，保存期间新编辑继续排队；失败常驻提示并接入导航/原生关闭守卫，可重试。主进程同时保存草稿与文件但不覆盖执行快照，并保留更晚的后台进度。后台进度不属于用户未保存编辑，不再触发多余离开确认。
- `test:task-project` 覆盖单飞保存、编辑竞争、畸形回执/ENOSPC、重试、改回初始值、跨项目迟到响应及进度隔离；`test:work-item-durability` 新增配置空草稿、真实生产保存失败回滚与保留阶段/快照检查，均通过。
- `test:e2e:task-project` 最新证据 `smartsub-task-project-e2e-5QSUbV`：独立 Electron 中配置即保存、刷新恢复、新旧项目往返、模式切换、授权素材导入保留配置、真实目录只读失败、离开阻断/重试、全局默认不变、1024/1440、SIGKILL 重启恢复。额外检查英语超长提供商/模型标签、每个检查器控件 hit-test，以及顶部模式标签所有裁切祖先边界；修复 1024 英语模式标签被工具栏遮挡，截图已核对。
- 启动与门禁最终复跑通过：`smartsub-readiness-e2e-CSJPmM`、`smartsub-gates-e2e-BPYcwP`。修复过程中两脚本曾因后台进度触发额外 beforeunload 而失败，已按用户输入单独计算 dirty 后通过；该失败未计作通过证据。
- `test:task-submission`、`test:subtitle-output`、i18n 检查通过；此时仍存在的旧测试类型错误后来已修复，最终全量检查通过。

### 两小时分数帧率全链路

`npm run test:e2e:timeline` 通过，证据 `smartsub-timeline-e2e-UHSY8q`。生成 320×180 黑底校准视频，帧率精确 `24000/1001`、172800 帧、时长 7207.2 秒；它是可测量的合成素材，不是实际两小时电影内容。通过真实 Electron IPC 调用生产字幕同步、视频压缩、完整硬字幕压制和软字幕封装，未模拟 FFmpeg 或输出。

- 24 fps 源字幕经 `1001/1000` 精确换算，三个 cue 为 1001–3003、3603600–3605602、7199192–7201194 毫秒；导出与软封字幕提取后完全相同，原文件不变。
- 原片、压缩片、压制片全部解码为 framecrc，三个版本分别验证 172800 个时间戳及帧总数，每一帧 PTS 精确等于 `帧序号 × 1001/24000`，无累积偏移或增减帧。
- 首/中/末 cue 各检查前一帧、内部两帧、后一帧的灰度像素，仅内部帧有字幕。末段截图 `final-cue.png` 已核对，结构化结果在 `report.json`。
- 初版首中末 PTS 采样 `smartsub-timeline-e2e-WfLVBn` 也通过，但最终结论采用上述全帧复测。当前未覆盖音视频同步、可变帧率或所有格式，不能据此声称任何输入均无限精度。

### 参数编辑可靠保存与全量类型检查

- 新增 A25/P0：`useParameterConfig` 原实现按 truthy 响应确认保存，旧保存完成可清掉新编辑的 dirty，失败提示定时消失；关闭自定义参数弹窗不等保存完成，未失焦和新参数行不受守卫保护。现改为串行保存、严格成功回执、快照确认、同步 dirty、常驻错误/重试和离开守卫；弹窗关闭/按 Esc 等待保存，未完成或非法 JSON 留在编辑器，两页签保留未完成行。旧服务商读取不能覆盖新服务商，读失败禁止编辑，不将 undefined 回执解释为空配置。
- 新增 A26/P0：`ConfigurationManager` 先改变内存再直接覆盖文件，读失败当空配置；导入/删除/恢复有相同风险，并打印完整请求头。现复用 `atomicReplaceTextFile`，串行完成磁盘替换后才提交新 Map；读取损坏保留原字节并阻止写入，初始化单飞，返回配置深拷贝，删除不存在配置幂等成功。迁移在副本上完成后持久化，保存/导入/恢复失败不提交内存；删除残留且无类型定义的模板逻辑，既有模板文件不删除。配置日志不再打印参数值，OpenAI 请求诊断不再打印完整服务商、请求头和请求体参数值。
- 新增 A27/P1：`ParameterValidator.validateParameterValue`、`MigrationManager.parseVersion` 各有同名实现覆盖，参数校验与版本兼容性判断不可靠。分离单值/配置校验、统一语义版本结构，复用共享校验响应类型；校验错误按前端 `key/type/message` 结构返回。合法认证模板、Content-Type 和普通文本不再被敏感词启发式误拒绝，头部控制字符、非法类型及非有限数字仍被阻止。
- 测试基础设施：加入 Jest/jsdom/testing-library 和必需 DOM peer dependency，修复旧测试 schema 与不完整 UI mocks。hook 测试直接调用生产实现，不再在测试文件内重写模拟 hook。现 `test:renderer` 为 5 套件、54 用例通过，涵盖保存竞争、关闭 autosave、跨服务商迟到读取、flush 期间新编辑、丢弃/重置、读取失败及缺失回执。主进程旧集成用例纳入执行，独立 verification 脚本失败会抛出而非只打印；5 项 verification 通过。
- `test:parameter-persistence` 通过真实文件与生产管理器的 rename ENOSPC/EACCES 注入，覆盖保存/删除/导入/恢复回滚、重试、并发不同服务商、深拷贝、损坏文件保留、0.9.0→1.2.0 迁移和参数不进日志。最新证据 `smartsub-parameter-unit-ifzLSw`。`test:custom-parameters` 7 项真实 SDK 请求/参数对象回归通过；`test:translation-proxy` 15 项通过。
- `test:e2e:parameters` 最新证据 `smartsub-parameter-e2e-L0AEiy`：真实 Electron、1024×700、参数目录 chmod 只读；自动保存失败常驻、关闭失败保留内容、后端读取与磁盘仍为旧值、恢复权限后重试与重新打开/刷新、未失焦 Esc 保存、不完整新增行阻止关闭、页签切换保留输入、补齐后 Esc 提交、导航“保存并离开”失败阻断/恢复后成功。截图 `save-failed.png` 已核对。测试仅操作独立 profile，不请求外部 AI 服务；后续依赖同步后仍需最终重跑。
- 新增 `main/tsconfig.json` 按实际 webpack ESM 打包方式使用 bundler 模块解析，不排除测试来隐藏错误。修复代理 Agent 签名、Node 可变默认模块导入、文稿参数多余索引签名、任务源字幕命名字段和未翻译检测的空数组推断。`npm run typecheck` 全部通过，替代前文 184 条历史诊断记录；CI 已加入全量类型检查、渲染与参数持久化测试。
- `npm run build` 成功完成 40 个静态页面及主进程 webpack 构建。非致命警告：Jassub worker 分块循环依赖、Browserslist 数据过旧、英文设置/引擎/向导页面数据超过 128 kB，以及静态生成中两条 i18next 初始化警告。这些警告未被隐藏，也不等于构建失败；后续性能/视觉阶段仍需处理。
- 新增测试依赖已同步 `package.json`、`yarn.lock`、`package-lock.json`。npm 锁文件原本停留在 3.3.0 且缺既有运行依赖，同步引起较多历史差异，最终提交前仍需完成锁文件审查和全量回归。尚未提交或推送，正文第 3、4 节仍未完成。

### 依赖同步后的集中回归

`yarn install --frozen-lockfile --ignore-scripts --ignore-platform --ignore-engines` 完成；明确加入 `@testing-library/dom` peer dependency。未运行安装脚本，原生库经 `ensure-native.mjs` 确认已存在。`typecheck`、5 套件 54 项 Jest、参数持久化、代理 15 项、i18n、diff 检查再次通过。

- 单测复跑：任务提交/项目/向导草稿/预检、导航/校对/WorkItem 持久化、健康状态、启动台 13 项、字幕输出 368 项与三入口 UI、预设映射/适配器/语言策略 57 项、引擎 851 项与 worker、精修 66+30 项、角色 10+65+31 项、文稿 74 项、配方、流水线 51 项、工具箱全部通过。
- 修复集中回归发现的旧启动台测试夹具缺 `ready/beforeStart`，以及文稿类型收紧后的文件描述字段缺失，修复后上述测试才记为通过。
- 普通项目新增在途保存后放弃用例，确认旧应答会把已放弃新版本重新排队；现按项目放弃版本忽略迟到成功/失败，不再次保存已放弃编辑，单测通过。已经提交给主进程的旧保存不能取消，不承诺撤销已经落盘的内容。
- 真实 Electron 最新证据：参数保存 `smartsub-parameter-e2e-WDtMCI`；提交预检 `smartsub-readiness-e2e-UGk1P7`；门禁/配音/压制 `smartsub-gates-e2e-97DMpx`；项目配置/崩溃恢复 `smartsub-task-project-e2e-jFETQC`；校对全域导航 `smartsub-proofread-e2e-sOrxFI`；向导恢复 `smartsub-wizard-e2e-ELVKrW`；工具箱 `smartsub-toolbox-e2e-4EtD6d`；批次保存 `smartsub-batch-save-e2e-SyiTHM`；磁盘容灾 `smartsub-storage-e2e-sQzykN`；健康度 `smartsub-health-e2e-nGFKMu`；预设 `smartsub-presets-e2e-RkRs58`；语言策略 `smartsub-language-e2e-LH4iHU`。均已退出测试实例，用户开发实例保留运行。
- `build` 已通过，但上述最后项目放弃竞争修复在构建后加入，提交前仍要再次构建与完整验收。真实角色/两小时校准/官方模型下载的证据仍采用前文记录，不将本轮未重跑的项记作本轮通过。
- 最后修改后补跑参数窗口 `smartsub-parameter-e2e-sn4CqJ` 和普通项目 `smartsub-task-project-e2e-fELVIr` 均通过；输入尚未提交时不再保留上一轮“已保存”状态。开发服务恢复在 `http://localhost:8888`，CDP `5858`。当前没有仍在执行的测试进程，未提交或推送。

## 第一阶段最终验收

- 新增 `test:e2e:recipes` 从四张内置卡片原生拖入真实素材执行。配音成片分别保留默认人工字幕门禁、显式关闭门禁，均经真实 Whisper Base、生产翻译/TTS HTTP 客户端、配音轨生成及 FFmpeg 硬字幕成片；检查门禁前无 TTS、放行后生成可完整解码的音视频、字幕正文及输入不变。其余三卡分别执行原文字幕、双语字幕、已有字幕翻译，不再只断言路由或草稿。模型 SHA-256 校验后复制至隔离 profile，翻译/TTS 使用本机固定响应，无外部素材上传。命令需要 `SMARTSUB_E2E_WHISPER_MODEL=/绝对路径/ggml-base.bin`，可用 `SMARTSUB_E2E_VIDEO` 指定视频。
- Yarn 锁文件语义审计最初发现意外运行依赖升级及跨平台 FFmpeg 条目缺失，已纠正。最终相对基线 **0 个原有选择器删除、0 个原有版本改变、305 个新增测试依赖选择器**；npm 锁文件原本为 3.3.0 且与 Yarn 不一致，按 CI 所用 Yarn 的版本重新生成，全部直接依赖版本一致。Next.js 自动补齐缺失的各平台 SWC 可选项。`npm ci --dry-run --ignore-scripts` 成功，仍有 Jest 的可选 Node 类型 peer 警告；没有通过强制升级整棵依赖树隐藏警告。
- `npm run typecheck`、5 套件 54 项 Jest、参数持久化、`test:pro-baseline` 的 16 组命令全部通过。后者涵盖启动台、校对/导航、存储、健康、草稿/项目/提交/预检、四场景、流水线/配方、八工具、精修、角色与文稿，已加入 CI。
- 另外 20 组最终回归全部通过：引擎、翻译解析、字幕输出、词库、结构化响应、请求参数、翻译取消/代理/未翻译/连通测试/免费翻译/Qwen-MT、配音/MiMo/声音克隆、合成/ASS、下载/路径及 i18n。完整日志目录 `smartsub-final-units-6SPiJH`。
- 最后生产改动及依赖收敛后的 `npm run build` 成功，40 个静态页面和主进程 webpack 均完成。已知非致命构建警告仍为 Jassub worker 分块循环、旧 Browserslist 数据、三页英文翻译数据体积和静态生成 i18next 初始化提示；留给第 4 节性能治理，不将其掩盖为零警告。
- 最终 13 组 Electron E2E 全部通过，总日志 `smartsub-final-e2e-IfYZnC`。参数 `8ITqs9`、项目 `OemDoY`、预检 `BdW3Mn`、门禁 `TN0EwI`、校对 `VybilV`、向导 `pM9lN9`、工具箱 `uf8Wo4`、批次 `NvWCYe`、存储 `P08m1A`、健康 `kIQ3vp`、预设 `Bjt522`、语言 `jcio7c`、内置配方 `KOCNIJ`（分别对应前文命名目录前缀）。参数失败、1024 英语长标签及成片画面截图已核对。
- 真实 sherpa 角色、官方 Base 下载、两小时全帧 PTS 采用前文已验证证据，本次没有重复把未运行项记为新通过。仍未验证系统断电、所有平台原生运行库、任意容器/VFR、所有商业云模型语言质量；段级 ASR 的内部切点只能估算，SRT/ASS 保留格式固有量化。此边界不影响已实现的保存/恢复/状态/批量路径结论，但禁止宣称任意输入“绝对零风险”。

## 后续顺序

1. 第一、二部分修复和核心验收完成，已提交并推送 `c6f15acade6cc49eb0f90c940df89b07f6b7f620`，远程提交号已核对。
2. 按第 3 节顺序实现波形缩放/区间/静音吸附、切分合并、行内 Diff、术语范围/替换、CPS、压制画布/安全遮罩/字体/模式隔离、角色阵容/音色筛选试听/时长对齐、下载流水线/原生字幕。
3. 按第 4 节完成最小窗口、Cmd+B、键盘流、撤销重做、常驻故障和万行性能验收，再次提交推送。
4. 当前未发现创建新 Codex 会话的工具；阶段提交时明确说明限制，不将无法新建会话当作后续功能完成，也不跳过后续实施。

## 第三阶段实施进展

以下为第一阶段提交后的新改动，不属于上述已推送提交。原生 Computer Use 插件尝试连接时报 `Computer Use requires nodeRepl.createElicitation`；本阶段继续采用独立 profile 的真实 Electron + Playwright，未声称已接管整个桌面。当前不能主动创建新的 Codex 会话，按既定顺序在现有会话继续。

### 波形与快捷编辑

- 使用 WaveSurfer 7.12.8 的 Regions/Timeline 插件显示预计算音频包络。FFmpeg 流式解码为 8 kHz 单声道 PCM，每 20 ms 保存一个峰值，不把完整 PCM 装入内存；两份缓存按真实路径、大小、mtime/ctime 失效，任务按窗口归属取消，窗口销毁和退出清理进程。12 小时解码上限防止无界输入。
- 播放器下方显示灰青色波形、字幕区间及播放头；缩放、点击定位、拖拽左右手柄、静音边缘吸附、C 按播放位置切分、X 合并邻句、Cmd+B 收折可用。手柄释放才提交一次历史命令；非法时间/邻句交叠不写入。拆分位置按字素边界计算。
- `test:proofread-waveform` 检查真实 FFmpeg 音调/静音、缓存失效、无音轨/不存在文件/目录/相对路径拒绝、取消归属、跨 PCM chunk 的采样完整性、吸附和拆分数学。最近证据 `smartsub-waveform-unit-m4oJI9`。
- `test:e2e:waveform` 最近证据 `smartsub-waveform-e2e-QhZqcp`：授权目录 demo.mp4 派生副本、非空 canvas 像素、真实指针缩放/拖拽/静音吸附、实际字幕保存、C/X/撤销重做、1024×700 与 1440×900、输入框快捷键隔离、媒体暂时丢失常驻错误/恢复重试，全部通过。1024 截图已查看。
- 边界：静音检测分辨率为 20 ms，不宣称采样级精度；五小时波形和万行滚动性能仍待第 4 节专门验证。

### 行内 AI Diff 与阅读诊断

- 移除原单条大弹窗和 `BatchAiOptimizeDialog`，保留非阻塞服务商/提示词/每批数量配置。单条和全文均支持优化、缩短；结果在字幕行内按词显示红色删除/绿色新增，支持逐条采纳、忽略、Enter/Esc、撤销重做和正常字幕草稿/保存。输入框的 Enter 不会误采纳，IME 和对话框不触发行内采纳。
- 发现旧逻辑按采纳时当前索引写回，编辑、导航或拆分后可能错行。现每次请求绑定文件、语言、行快照和时间结构；采纳前再次校验最新数据。编辑/结构变动会提示建议过期，切换文件和卸载取消请求，迟到响应不重新引入已忽略或已采纳结果。
- 单条/批量后端优先使用当前文件语言，转写模式默认不翻译。取消注册按窗口与请求 ID 隔离，逐批结果带 batchId 并提前通知行内审查；服务失败常驻且可重试。空字符串、null、数字、对象、数组等不合规结果不会覆盖字幕。提示词插值将字幕内容作为字面值，防止 `$&` 或嵌套占位符被意外展开。采纳原文同步 `content`，采纳译文清理旧失败状态。
- 行内实时显示字符数、最长行字符数和 CPS；中文阈值 8、英文及其他非中文默认 20，恰好等于阈值不警告。按字素计数（组合字符不拆开），换行不计字数，空白/标点计入；语言缺失时含汉字采用中文阈值。超速以柔和黄色图标/数值和提示说明展示。
- `test:inline-ai` 执行生产 React hook 与真实后端处理器的离线测试：请求身份、迟到响应、结构变更、切换/卸载、部分结果采纳、取消隔离、错误恢复、语言传递、提示词字面值、异常响应、批量参数与 CPS 边界通过。
- `test:e2e:inline-ai` 最近证据 `smartsub-inline-ai-e2e-sIO5co`：真实 Electron → IPC → 本机 OpenAI 兼容 HTTP 服务 → 行内 Diff，覆盖日语/法语文件语言、采纳/忽略/撤销重做、输入 Enter、在途编辑防覆盖、批量字幕落盘、两个窗口尺寸、失败重试、取消、新请求、逐批完成前审查、部分采纳后取消余下任务、全文缩短和实时 CPS。1024 截图已查看；固定 AI 响应用于协议/交互验证，不代表真实商业模型的语言质量测试。
- 集中回归：`typecheck` 通过；`test:pro-baseline` 现含 18 组命令全部通过；Jest 5 套件 54 项通过；i18n 和 diff 检查通过。`test:e2e:proofread` 最近证据 `smartsub-proofread-e2e-ytgH1V`，侧栏/历史/命令面板/原生关闭守卫、刷新恢复、写盘失败重试全部通过。
- 依赖审计：相对第一阶段提交，Yarn 原有选择器和版本全部保留，仅增加 `diff@8.0.2`、`wavesurfer.js@7.12.8`。npm 锁也仅增加这两个无额外依赖的运行包，未接受安装过程产生的无关版本或元数据漂移。
- 最后补齐采纳后的原文 `content` 与译文失败标记清理、转写批量默认提示词后，重新运行 `typecheck`、`test:inline-ai` 及 `test:e2e:inline-ai` 全部通过；最新完整界面证据为 `smartsub-inline-ai-e2e-M5bJ2U`。开发服务 `http://localhost:8888/zh/proofread/` 返回 200，测试实例均已退出。

### 划词术语与项目范围

- 校对原文/译文输入框选词后显示入库气泡，支持项目或全局范围。未保存校对批次先可靠创建项目；术语与新词库在同一次 electron-store 写入中保存，失败不遗留空集合或发布新值。相同术语重复保存幂等，已有不同译词必须显式确认并进行旧值比较，防止覆盖其他窗口新修改。自动集合名按界面语言生成并保证 80 字符限制内唯一。
- 现有全局词库格式兼容；可选 `projectId` 保存范围，非法范围记录不会降级为全局。项目术语优先于全局，同范围沿用列表优先级；单行/批量 AI、失败重翻及正式翻译链路均携带或继承项目上下文。词库页显示范围并纠正原有“全部全局”的说明。
- 保存后单独确认全文更新，展示匹配数量及前三条预览，可编辑现有译法。双语仅替换译文，纯转写仅替换原文且同步 `content`；替换作为一次撤销操作进入原有保存/草稿链路。字面值匹配保护拉丁词边界，支持中文子串、特殊字符和被拒候选后的重叠有效匹配。确认时再次比对字幕快照，变化后必须重算。
- 修复初稿 StrictMode 挂载状态、未生效的保存并离开、关闭弹窗丢弃输入、滚动后气泡悬空及长名称截断重名问题。异步保存绑定文档版本，建档/IPC 返回后切换文档不污染新界面。失败重翻也补齐文件/语言身份和行快照保护、切换/卸载取消、窗口销毁后中止并清理，避免失败标记尚在时覆盖手改译文。
- `test:glossary` 74 项通过，新增 `test:context-glossary` 覆盖字面替换、作用域隔离、项目上下文、原子回滚、幂等、冲突比较和名称界限。Jest 新增 5 项真实组件测试，覆盖 StrictMode、离开保存、过期替换、建档时切换文件及迟到回包。
- 真实 Electron 最新证据 `smartsub-context-glossary-e2e-kUWIHP`：划词气泡、项目/全局保存、双语列隔离、全文更新及单次撤销重做、真实字幕落盘、冲突确认、目录写保护失败/回滚/重试、关闭确认保留输入、真实 HTTP 请求的跨项目术语隔离、应用重启保留，以及未保存项目自动建档与纯转写替换。1024×700/1440×900 截图及最小窗口布局已核对。测试使用隔离 profile 和自建字幕，不修改原素材；HTTP 固定响应仅验证请求链路，不代表模型语言质量。
- 集中回归：`typecheck`、20 组 `test:pro-baseline`、6 套件 59 项 Jest、i18n 全部通过。开发服务已重启在 `http://localhost:8888`；原生 Computer Use 仍缺 `nodeRepl.createElicitation`，未将 Playwright 验证称作原生桌面接管。

### 合成画布、异常恢复与软字幕容器

- 发现 V4+ ASS 的 `Alignment` 被错误转换为旧 SSA 编码，导致中排/上排位置错乱。生成的 V4+ 样式现保留九宫格原值，旧 `force_style` 接口仍使用对应兼容转换。`test:compose-canvas` 使用真实 FFmpeg/libass 检查九个位置的字形像素，以及 25%/50%/75% 绝对 Y；最新证据 `smartsub-compose-canvas-unit-RwL3HP`。
- 增加字幕 Y 轴拖拽、数字定位/重置、箭头 1 像素与 Shift 10 像素微调、Esc/指针取消。未释放的拖拽只做临时预览，切换模式/字幕/样式时取消。影视 80%/90% 框和短视频右侧/底部遮罩可切换，软字幕/处理中禁用样式操作。拖拽框优先依据真实 JASSUB 透明像素边界，最长边下采样到 512、最多每秒 10 次读回；引擎未准备好时保留估算框。原生 ASS 读取当前 `pos/move` 锚点，避免从默认底部起拖跳动；已有数字位置优先于迟到渲染锚点，避免连续键盘微调丢步。
- 原生 ASS 显式 Y 定位保留事件正文/时间和已有 X，按原 PlayRes 换算 Y，生成 ASS 用于预览及硬烧。已验证单条 `pos`/字号覆盖及竖屏；尚未证明复杂动画、SSA 颜色/alpha、缺失 Format、多层重叠/绘图、重置标签的完整保真，不据此勾选 P2-05。预览和未显式 Y 的旧 ASS 导出仍有不同样式路径，后续需统一或验证。
- 修复选中字幕文件丢失时悄悄显示样例、异步换轨/重绘错误漏报、失败后无法恢复、初始化失败残留 worker、切换文件迟到更新污染新预览及调试句柄残留。按预览会话串行写入 worker，每个异步边界检查归属；错误常驻 Banner、可展开详情和重试，错误状态不显示旧字幕/CSS 样例、不允许拖拽。JASSUB 内部持续播放回调的全部异常路径仍需后续专项覆盖，不能将 hook 覆盖等同于库内所有行为已验证。
- 竖屏真实测试发现当前机器无系统 `ffprobe`，合成页读取失败一直按 16:9 显示，而导出已有 FFmpeg 兜底。现预览元数据复用内置 FFmpeg 探测，并由实际视频尺寸补充；元数据读取绑定请求序号，切换/清空/卸载后不回填旧信息。损坏视频修复后的重试追加媒体 URL 版本，避免 Chromium 缓存失败请求。
- 软封装新增 MKV/MP4 容器选择，默认兼容 MKV，偏好跨重启保存并从运行队列恢复。MKV 使用 SRT，MP4 使用 `mov_text`，保持视频流复制，拒绝其他软字幕容器。双音轨 UI 沿用 MKV，未收窄底层原有 MP4 双音轨能力。输出扩展名与容器联动，硬字幕保存对话框选定的格式不被重写；保留软模式中性文本预览和播放器效果提示。混音/换轨仍可能编码音频，不宣称所有软封装组合都免编码或耗时恒为毫秒。
- 最新 `test:e2e:compose-canvas` 证据 `smartsub-compose-canvas-e2e-Kjz47v`：隔离 Electron profile，真实指针拖拽/取消/键盘微调，字幕文件 ENOENT 常驻错误/恢复，1024×700 与 1440×900 无页面横向溢出，两个安全区，实际硬烧像素、MKV 可提取轨道且视频不烧字、MP4 视频包 SHA-256 与原片完全一致且字幕正文/毫秒时间可独立提取；竖屏 ASS 由 75% 拖至 55%、保持 X、成片像素正确；播放时间实际前进，损坏媒体修复后可重试播放并恢复竖屏比例。素材为可测量的合成黑底视频，不将其等同于所有实际媒体兼容性。此前 `wiPQ80` 的竖屏/错误及最小窗口截图已核对。
- 新增 10 项预览 hook/透明像素边界测试、5 项合成状态测试：StrictMode、初始化/换轨/重绘拒绝、切换文件/视频/卸载、串行样式更新、字体读取失败、元数据迟到、清空时在途请求、容器恢复、硬格式保留及无扩展名路径。最新 Jest 为 **8 套件 74 项通过**；20 组 `test:pro-baseline`、`test:ass-builder`、`test:compose`（新增 MP4 keep/replace/mix 参数与无效容器检查）、`test:compose-canvas`、i18n 和 diff 检查通过。后续完整生产构建及全部 Electron 总回归尚未执行。

### 字体、专业预设与统一 ASS 路径

- 原字体排障中“JASSUB 不支持 TTC”的推断已被真实实验否定。Helvetica、宋体 TTC 直接可用；Hiragino 失败源于系统显示别名 `Hiragino Sans GB` 与字体内部 family `Hiragino Sans GB W3` 不同，改用内部名称后真实 CJK 字形正常。引入 `fontkit@2.0.4` 解析字族/字形覆盖，保留 TTC/CFF 能力，不再按扩展名禁用。字体菜单按真实字面渲染样例，缺失项禁用；缺失字体的替代在画布上常驻提示，单纯别名规范化不误报替代。
- 后备字体按缺失字符显式写入 ASS，英文 Courier New 等宽字形不再因同条中文而整条替换成比例字体。菜单样例也使用相同逐字后备规则；本机字面通过 `FontFace local()` 加载，避免每次打开列表经 IPC 复制几十 MB 的字体集合。预览加载全部所需字体与独立 TTF 的相邻粗斜体字面，并修复从缓存切回字体时 defaultFont 不更新的问题。无法找到所需字形会显示错误，不静默输出缺字方框。当前字体来源仍为常用系统字体映射，尚非任意用户安装字体的完整索引。
- 新增 B 站高对比黑底/白字/黄色词高亮、Netflix 黄白双行/Courier New/细阴影、综艺粗描边/斜体/粉色外发光；高级面板可调高亮词/颜色、次行颜色及发光强度/颜色。样例卡扩高容纳双行。修复新预设插入首位意外改变 TaskWizard 默认样式，以及普通字幕被不必要颜色标签改写的问题。
- 所有硬字幕输入（含原生 ASS/SSA）现在使用与预览相同的 ASS 生成路径，取消原生 ASS 未设置 Y 时单独走 `force_style` 的分支。SSA 样式转换为 V4+，补全缺省 Format；绝对 Y 调整保留 `move` 的 X 起终点与时段，原事件时间、逗号文本、绘图、淡入/动画标签不被拆成普通字幕。高亮跳过绘图指令，发光层保留事件层级顺序。复杂 `t`/`r` 覆盖、跨标签词高亮、内嵌字体/多脚本字形及所有 SSA 变体仍需进一步专项验收，不能据简单夹具宣称任意 ASS 保真。
- 补齐 JASSUB 内部不等待 Promise 的播放/resize 渲染失败观察及 worker error/messageerror，统一常驻错误与重试。失败会移除画布、释放 worker 并保留错误状态；新文件或重试建立新会话，旧 worker 迟到错误不污染当前预览。当前 mock 覆盖这些失败路径；没有声称已在真实 Electron 中复现全部底层 WASM 崩溃。
- 新增 `test:subtitle-appearance`：最长优先字面高亮、缺字报错、字族别名、逐字后备、move X/时段保留、绘图保护、无 Format、SSA 颜色/alpha，以及三套预设 × SRT/ASS 与 SSA/no-Format 共 8 个真实 FFmpeg 像素用例。最新证据 `smartsub-appearance-unit-2U9CHy`；已加入 `test:pro-baseline`，Linux CI 明确安装 DejaVu/Noto CJK 字体前置条件，本机未执行 Linux CI。
- 最新真实字体/预设 E2E `smartsub-compose-fonts-e2e-Cll8DQ`：菜单全部可用字体真实加载、Hiragino CJK 字形、三套预设界面选择与实际视频导出、Netflix 英文等宽保留及 CJK 后备。预览/导出亮像素 IoU 为 0.951/0.906/0.595；综艺合成粗斜体存在约 1–2 像素栅格化差异，使用明确 2 像素邻域检查字形双向覆盖 0.995/0.999，另两套均为 1.000。保留原始 IoU 和 PNG 证据，未声称像素完全相等；所有实际预览无缺字日志。菜单、Netflix、综艺预览及导出帧截图已核对。
- 最新画布 E2E `smartsub-compose-canvas-e2e-1J7RO2` 重跑拖拽/撤销手势/数字位置、两种安全区、1024/1440、字幕丢失恢复、硬烧位置、MKV/MP4 轨道与视频包校验、竖屏原生 ASS X/Y、实际播放和损坏媒体恢复，全部通过。最新 Jest **8 套件 77 项通过**；21 组基线、ASS/compose、typecheck、i18n/diff 检查通过。后续新增严格字体响应检查已补跑预览 hook 13 项；最终生产构建与全套 E2E 仍待执行。
- 安装依赖时纠正 Yarn 自动剔除既有 peer 选择器和 npm 条目排序漂移。相对第一阶段提交，npm/Yarn **0 个原有条目删除、0 个原有版本改变**；新增 fontkit 及其依赖、原定 diff/WaveSurfer，以及补全的跨平台 FFmpeg/esbuild 可选条目。未运行安装脚本。开发服务已恢复，当前服务日志 `/tmp/smartsub-review-dev.log`。
- 最后严格字体响应校验后，`typecheck`、13 项预览 hook、`test:ass-builder` 与字体 E2E 再次通过，字体最新证据为 `smartsub-compose-fonts-e2e-uRL8qd`。像素单测修正测试自身传入的主字体，使原生 ASS/SRT 预设使用各自真实字族，8 个像素用例再次通过，证据 `smartsub-appearance-unit-Cgt8b1`。远程 `feature/lxd/ui` 经只读核对仍为 `c6f15acade6cc49eb0f90c940df89b07f6b7f620`，本阶段没有提前提交。

### 合成工程草稿、历史与作业归属

- 原合成页仅记忆输出偏好，文件、样式、音轨和输出位置切页即丢；初始偏好回包和默认路径生成也可能覆盖用户刚做的修改。现将这些字段收敛为 `ComposeDocument`，按入口视频/字幕路径隔离，完整动作一次进入历史；连续同字段编辑分组，鼠标释放/失焦/保存结束分组。恢复和撤销增加文档代次，过期元数据/默认输出请求不回填；选择输出位置后迟到默认路径不会覆盖。
- 合成草稿采用内存 + LocalStorage，自动草稿与显式保存基线分开。成功落盘才报告已保存；存储失败保留最新内存状态、错误 Banner 和离开守卫。损坏草稿禁止悄悄覆盖，提供读取重试或显式放弃；放弃删除失败仍保留恢复对话框。全局守卫新增消费 `onDiscard` 的失败结果，失败不能跳转。恢复未保存工程后可撤回上次保存基线；已保存工程重新进入直接恢复。
- 工具栏增加保存、撤销/重做、样式面板收折；支持 Cmd/Ctrl+S、Cmd/Ctrl+Z、Cmd/Ctrl+Shift+Z、Cmd/Ctrl+B，输入框保留原生文本撤销，对话框不触发工程快捷键。空格播放/暂停避开输入、选择器、按钮、视频原生控件与对话框。收折只隐藏面板，不卸载高级设置。合成页减少多重边框，保留中性分层与两种目标尺寸；预览故障详情与合成失败详情可展开，不再无界铺满画面。
- 合成提交使用请求 ID 绑定主进程返回的队列作业 ID，进度和取消仅消费该作业；同步阻止重复提交和处理中编辑，严格验证成功结果必须包含输出路径，卸载后不回调旧页面。运行中作业可切页后重连，队列上下文补齐真实样式/画质/编码方式，晚到偏好不覆盖运行中的上下文。文件选择取消不再伪装错误。偏好/元数据/预设/队列等失败由常驻错误暴露，未再静默吞掉。
- 新增 `ComposeDocument.test.tsx` 和扩充合成状态测试，专项 **19 项**覆盖 StrictMode、分组/无操作、完整恢复、读取损坏、写盘/删除失败、失败后撤销到基线仍保持守卫、同步脏状态、迟到偏好与元数据、手选输出保护、重复提交、严格成功响应、定向进度/取消、运行作业上下文和卸载后响应。
- 新增 `test:e2e:compose-document`，最新证据 `smartsub-compose-document-e2e-HdRulX`：隔离真实 Electron，保存/撤销/重做、Cmd+B、空格和输入隔离、侧栏/历史/Cmd+K、原生刷新/退出/关闭取消、真实页面刷新后草稿恢复、注入 LocalStorage quota 故障时保存及放弃均阻断、重试保存并离开、同路由查询变化隔离、1024×700/1440×900，以及真实 FFmpeg 长片运行中离开/重连/定向取消和半成品清理。存储故障为注入，不等同于物理磁盘满或系统断电测试。最小窗口、常驻故障及宽屏截图已查看；原生视频控件截图出现短时 loading 指示，实际空格播放时间推进已单独验证。
- E2E 首轮暴露新工具栏漏 TooltipProvider 和面板收折丢局部展开状态，均已修复；测试驱动沿用现有 Electron beforeunload 处理方式，避免与 Electron 原生确认重复处理。原画布 E2E 最新 `smartsub-compose-canvas-e2e-l4Xxs6` 全部通过；字体/预设 `smartsub-compose-fonts-e2e-dncCDw` 全部通过，三套 IoU 为 0.954/0.881/0.590，2px 字形覆盖保持 1.000/1.000、1.000/1.000、0.994/0.999，不声称严格逐像素相等。全局守卫修改后校对 E2E `smartsub-proofread-e2e-9ICcGq` 再次通过。
- 最新完整 Jest **9 套件 91 项**、`typecheck`、21 组 `test:pro-baseline`、导航保存及 i18n/diff 检查通过；最后内存失败快照修正后又通过 19 项专项测试及完整合成工程 E2E。新代码未执行最终生产构建，未作第二次提交。开发服务 `http://localhost:8888` 正常；本轮重启后的 nextron PID 为 68854，日志仍在 `/tmp/smartsub-review-dev.log`。

### 合成输出保护与已有流封装

- 修复 FFmpeg 直接 `-y` 写最终路径、失败清理可能删除源文件/既有成品的风险。所有统一合成作业先写输出目录中的独占 `.smartsub-compose-*` 子目录，成功后才发布；同名目标保留并自动递增 `_2` 等后缀。提交前检查输入同路径、硬链接和符号链接身份；发布时仍以不覆盖操作应对执行中出现的新文件。队列终态、合成页、流水线及配音视频导出均消费实际发布路径，界面不再停留在预期旧路径。
- 支持硬链接的文件系统通过链接完整临时文件发布；不支持链接时采用 `wx` 独占创建、流式复制和同步。复制失败/取消仅清理本作业创建的 inode，若路径被其他写入者替换则保留。此 fallback 不承诺断电时原子可见，尚未在真实 FAT/exFAT/网络卷上验证；测试注入了不支持链接和 ENOSPC 错误。临时清理限定于独占作业目录；AAC 预编码固定使用该目录内的安全名称，硬件失败后 CPU 重试复用同一音轨，而非重新产生另一时间戳路径。
- 软字幕显式映射视频、全部音轨、新字幕及原字幕；MKV 原字幕/附件流复制，新 SRT 为默认轨，旧默认位撤销但 forced 位保留。MP4 文本轨转为 `mov_text`，排除不兼容附件/数据流，检测到原位图字幕时明确失败并建议 MKV。硬烧 keep 模式也改为显式映射视频/全部原音轨，修复自动选流只保留部分音轨或额外带入旧软字幕导致双重显示的问题。混音/换轨仍可能编码音频；无原音轨时的 mix 行为和用户可见流丢弃提示仍需收尾。
- `test:compose-output` 包含输出助手与真实 FFmpeg runner。最新集中证据 `smartsub-compose-output-wcLELI` / `smartsub-compose-runner-MXGHjt`：源/硬链接/符号链接、执行中碰撞、并发发布、取消、fallback、复制失败与并发替换归属、空输出、无效字幕、硬件失败后 CPU+addTrack 重试、长视频中途取消，以及原文件哈希和临时目录清理通过。
- 新增真实 Electron `test:e2e:compose-output`，最新证据 `smartsub-compose-output-e2e-zF8FCv`：保留既有成品字节和源文件哈希、界面显示冲突后实际路径；新默认 SRT+原 ASS/forced/附件保留；MP4 两条文本轨独立提取、时间精度和视频包 SHA-256 校验；硬烧保留两条原音轨且不附带软字幕；真实 FFmpeg 失败后保留旧成品并清理私有目录。测试使用独立 profile 和合成素材，成功截图已查看。
- 修改输出链路后，22 组 `test:pro-baseline`、9 套件 92 项 Jest、`typecheck`、`test:compose`、五组合真实 `smoke:compose`、i18n/diff 检查均通过。合成草稿 E2E 最新 `smartsub-compose-document-e2e-sBCe8G`、画布 `smartsub-compose-canvas-e2e-omsqBM`、字体 `smartsub-compose-fonts-e2e-XuLRK3` 全部通过。字体 IoU 0.954/0.881/0.590，2px 双向字形覆盖 1/1、1/1、0.994/0.999，未宣称严格逐像素一致。

### ASS 嵌套标签回归

- 复核发现原逐正则替换可能将 `t(...)` 内的字体/绘图/重置标签误当作立即生效的顶层状态，并破坏闭合括号。新增共享的无损顶层标签扫描：保留注释、未知指令和嵌套内容原文，只对顶层定位、字体和绘图状态做现有变换；预览拖拽锚点也不再错误读取动画内部位置。发光层在命名重置后恢复发光标签时不再插入空或破损花括号。
- 扩充 `test:subtitle-appearance`：嵌套标签原文与状态隔离、命名重置、发光重置、动画内无效字体名不能改变后续字体、绘图模式不能泄漏，以及同一 FFmpeg/libass 三个时点处理前后像素完全一致。参考动画自身必须变化，避免静态夹具产生假阳性。最新证据 `smartsub-appearance-unit-3rO5JB`，原有 8 个预设/SSA/no-Format 像素用例同时通过。此扫描不是新的完整 ASS 渲染引擎；渲染仍由已有 libass/JASSUB 完成。
- 新增真实 Electron `test:e2e:compose-ass`，证据 `smartsub-compose-ass-e2e-XdhnYy`：带实际运动与缩放的文本、嵌套裁剪、绘图、多层、逗号正文、命名样式重置、行内字体和中文后备；0.24/1.2/2.4 秒预览与真实硬烧视频的 2px 双向亮字形覆盖均为 1.000，导出帧哈希互不相同。尚不覆盖任意 ASS 变体或内嵌字体；不能据该夹具宣布完整复杂字幕兼容性完成。
- 最后复跑结果：22 组基线、92 项 Jest、类型检查、i18n/diff、九宫格像素测试均通过；复杂 ASS E2E 最新 `smartsub-compose-ass-e2e-sfzZ9j`，字体 E2E `smartsub-compose-fonts-e2e-I93MLh`，画布 E2E `smartsub-compose-canvas-e2e-uhLcay`，全部通过。最新单测动画证据 `smartsub-appearance-unit-d9OLgM`，输出保护/runner 证据 `smartsub-compose-output-3QKbFJ` / `smartsub-compose-runner-xF4BSt`。复杂 ASS 截图已查看。远程分支只读核对仍为 `c6f15acade6cc49eb0f90c940df89b07f6b7f620`；未进行第二次提交。

### 导出样式校验、无原音轨混音与颜色保真

- 硬字幕渲染/导出新增共享语义校验：完整颜色、有限数值、合法位置/透明度和不破坏 ASS 字段的字体名。草稿仍允许保存未输入完整的颜色等中间状态，不借校验丢弃输入；硬字幕按钮显示常驻字段错误并禁止提交，主进程独立拒绝无效配置。软字幕不应用这些样式校验。预览因无效样式失败后，修正样式即建立新会话恢复，不要求额外手动重试。
- 原视频没有音轨时，mix 改为使用所选配音轨；原片探测结果同时传给初次编码与硬件失败后的 CPU 重试。界面显示无原音轨提示，以及 MKV/MP4 各自保留/转换/排除流的实际规则。MKV 仍保留原字幕与附件，MP4 文本轨转 mov_text、附件/数据不写入；位图字幕需选择 MKV，不将软封装描述为所有输入均零编码。
- 视频探测支持取消、15 秒超时终止、stderr 上限 1 MiB，并等子进程 close 后完成。新增工具箱专项 `test-media-probe.cjs`，证据 `smartsub-media-probe-kkdG0U`：真实有声/无声/损坏/空/缺失媒体及预取消；注入启动失败、超时 kill、close 顺序、取消、分块元数据、过量 stderr 和定时器释放。原工具箱测试仅验证裁剪参数，现将此专项加入其总入口。
- 复核发现校验接受的带空格 rgb/rgba 与 ASS 转换不一致，可能变成白色；RGBA alpha 原先完全忽略，CSS 背景也只解析 hex。现共享完整颜色解析，CSS 与 ASS 均将颜色透明度和独立背景透明度相乘，非法颜色不再静默回退白色。ASS 行内颜色采用独立 BGR/alpha 标签，发光副本保持填充透明，避免新增 alpha 标签造成重复填充。原生颜色选择器从 RGB/RGBA 正确显示 hex 色样，文字输入保留原值。
- ASS 高亮按可见文字匹配后映射回原文本片段，`Smart{\\b1}Sub` 可以完整高亮；原覆盖标签顺序、硬空格与绘图数据保留，不跨显式换行或绘图片段匹配。测试覆盖命名重置、最长词匹配、绘图状态、嵌套动画、透明度恢复；并对跨标签高亮、RGB 空格、半透明发光三组样本做真实 libass 像素完全一致检查。透明/半透明/不透明白字在黑底的最大亮度实测为 0/125/253。
- 完整 `test:pro-baseline` 22 组通过，最新样式像素证据 `smartsub-appearance-unit-nZDtqe`（8 个原有预设/SSA 用例、3 个动画时点、3 组新增颜色像素对照）；输出/runner 证据 `smartsub-compose-output-oqKNK8` / `smartsub-compose-runner-61LtMa`，包含 hard/soft/none 无原声混音和 CPU fallback。9 套件 94 项 Jest、`test:ass-builder`、`typecheck`、i18n/diff 检查通过。后加的探测专项单独通过，尚未将它记为前述基线运行的一部分。
- 真实 Electron 输出 E2E 最新 `smartsub-compose-output-e2e-7AQK1u`：不完整颜色常驻错误/禁用硬导出、IPC 拒绝与修改后预览自动恢复、输出冲突和源文件保护、MKV/MP4 流与视频包校验，以及 hard/soft 无原声混音均通过。此前 `OMkdsW` 的 1024×700/1440×900 截图已检查，动作可见且无页面横向溢出；左侧标题在紧凑宽度下截断，仍留待第 4 节统一治理。
- 真实 Electron 复杂 ASS E2E 最新 `smartsub-compose-ass-e2e-JrLayZ`，通过界面设置跨标签高亮与带空格 RGBA，验证色样、实际成片及 0.24/1.2/2.4 秒动画。2px 双向可见字形覆盖约 0.9988/0.9978、0.9988/0.9979、0.9989/0.9980，并分别检测黄色高亮与半透明字形。测试掩码由亮白改为任一通道亮度 >95，覆盖有色与半透明文本；仍保持 98% 邻域覆盖门槛，未声称逐像素等同。失败夹具首帧与最终 2.4 秒预览/导出及完整界面截图均已查看。
- 字体/三套预设 E2E `smartsub-compose-fonts-e2e-1d6z0d` 通过：IoU 0.954/0.881/0.590，2px 字形覆盖 1/1、1/1、0.994/0.999；字体别名/CJK 后备及发光预设没有因颜色修改退化。远程只读核对仍为第一阶段 `c6f15acade6cc49eb0f90c940df89b07f6b7f620`，第二阶段未提交。
- 最终补跑 22 组基线（已含新增探测专项）、94 项 Jest 全通过。最新证据：样式 `smartsub-appearance-unit-54In2C`、输出 `smartsub-compose-output-isVuxg`、runner `smartsub-compose-runner-1AsCUF`、探测 `smartsub-media-probe-wuvKLe`。画布 E2E `smartsub-compose-canvas-e2e-Tqyk9t` 通过拖拽/取消/数字位置、两种安全区、1024/1440、文件丢失恢复、硬烧像素、MKV/MP4 流/包哈希、竖屏原生位置、实际播放和损坏媒体修复重试；本轮所有测试进程已结束。

### 合成错误重试与预设写入可靠性

- 通用重试原先只重新读取配置/元数据，无法重新执行失败的文件选择、打开文件夹、取消与预设写入；预设读取成功还可能清除独立的保存失败。现按操作作用域保存重试动作，错误保留至对应操作真正成功。文件/输出对话框与取消同步防重复；迟到选择受文件路径、文档代次及输出修订限制；打开文件夹与取消分别绑定原输出、已确认的作业 ID。
- 预设保存使用稳定客户端 UUID 和不可变样式快照，主进程支持同 ID 幂等 upsert，删除不存在 ID 也返回成功。写盘后响应丢失可重放，不会新增重复预设。预设命名弹窗保留失败名称与错误，保存期间阻止重复 Enter/关闭，兼容输入法 Enter。损坏的非数组预设存储不再当作空列表覆盖。
- 偏好设置写入串行化，只有最新版本响应可改变错误状态，重试保存完整当前设置。初始预设列表与写入并发时，合并读取开始后确认的保存/删除，既保留其他已有预设，也不复活已删除项。读取类重试现在等待实际 IPC 完成并保持按钮禁用，不再触发全局无关重读。
- 后端专项 `test:compose-presets` 覆盖稳定 ID 重放、更新、失败写盘、畸形样式/ID、删除重放、旧调用方式与损坏列表保护。合成状态测试新增延迟读取、防重复与晚到响应、并发列表合并/删除保护；预设弹窗新增 UI 回归。存储单测含注入故障，Electron 使用真实生产持久化。
- `test:e2e:compose-retry` 最新证据 `smartsub-compose-retry-e2e-vEQtXY`：真实预设保存/删除后注入响应丢失、同 ID 重试、输出选择失败重试后真实 FFmpeg 导出、偏好写入重试、文件选择重试、失败取消重试精确停止原长视频作业并清理半成品，以及读取延迟期间按钮禁用与错误常驻。打开文件夹核对的是生产处理器传给 shell 的精确路径，shell 被替换以避免打开 Finder，不声称验证了 Finder 窗口。此前 `3pogqJ` 的最小窗口与常驻错误截图已查看；1024/1440 仍无页面横向溢出，但紧凑标题截断留待第 4 节。
- 同期合成文档 E2E `smartsub-compose-document-e2e-sX5EmN` 通过保存、历史、快捷键、导航守卫、刷新恢复、存储失败和运行中重连/取消。单一路径重连通过不等同于多个同路径作业的精确选择，该项继续待办。

### ASS 多层布局平移与定位历史

- 原 `moveVertically` 将所有事件及 `\\move` 的两个端点改成相同 Y，导致多层字幕、绘图与纵向动画坍缩。改为按统一偏移量平移原坐标；首次画布拖拽记录原生参考锚点，随后拖拽、数字修改、保存恢复沿用该参考。重置定位同时清除参考值，撤销/重做完整恢复二者。
- 保留各事件原有 X、对齐覆盖、移动时段与两端差值，平移旋转中心、矩形/矢量裁剪及嵌套动画裁剪。绘图本身的相对路径不改写。没有显式坐标的上下对齐事件在目标边距有效时通过事件 MarginV 移动，保留 libass 自动避碰；隐式锚点按实际 PlayResY、事件边距、现代/旧式对齐标签计算，不按 288 默认分辨率跳位。
- `test:compose-canvas` 真实 libass 测试新增三层绘图/移动/裁剪样本，在 0.12/0.52/0.92 秒逐像素证明所有层精确下移 20 像素，另验证同时出现的普通字幕仍自动堆叠而不重叠。九宫格与单字幕绝对位置回归保留；该专项已加入集中基线。Jest 验证参考锚点只捕获一次、连续微调、软字幕禁止操控、磁盘 schema 保留参考值及隐式锚点。
- 真实 Electron `test:e2e:compose-ass` 最新证据 `smartsub-compose-ass-e2e-qpTASs`：真实指针下移 5%（脚本坐标实测 14.401），确认三个层的间距、原生对齐、绘图及移动差值保留；撤销/重做/清除定位/撤销清除恢复精确事件文本。实际 FFmpeg 导出 0.24/1.2/2.4 秒分别与 JASSUB 预览比较，2px 双向覆盖为 0.99959/0.99783、0.99961/0.99792、0.99962/0.99799，动画帧哈希各异，跨标签黄色高亮和半透明文字仍通过。等价前次 `s4L9rV` 的完整界面及 2.4 秒预览/成片截图已查看。
- 画布 E2E `smartsub-compose-canvas-e2e-40mI7p` 通过已有拖拽/取消/键盘微调、安全区、1024/1440、文件错误恢复、硬烧像素、软封装轨道/视频包哈希、竖屏原生位置与播放测试。仍未把任意复杂 ASS 全部宣布支持：居中自动碰撞布局、移出边界导致无效边距、滚动 Effect 和自定义/内嵌字体需后续专项审计。
- 本轮最终回归：**24 组集中基线、11 套件 110 项 Jest、typecheck、i18n、diff 检查全部通过**。最新单元证据：画布 `smartsub-compose-canvas-unit-wrAKxb`、样式 `smartsub-appearance-unit-eKL3gc`、输出 `smartsub-compose-output-jBWmfD`、runner `smartsub-compose-runner-q7ApGj`、媒体探测 `smartsub-media-probe-0TPakK`。最终隐式锚点修改后画布 E2E `smartsub-compose-canvas-e2e-hmIYhE`、文档 E2E `smartsub-compose-document-e2e-sxhGnV` 重跑通过；复杂 ASS 最新 `qpTASs` 的 2.4 秒预览以及重试 `vEQtXY` 最小窗口截图已查看。所有测试进程已结束，开发服务 `http://localhost:8888/zh/home/` 返回 200；本轮未生产构建、未新增提交。

### 内嵌字体与已安装字体索引

- 新增 ASS/SSA `[Fonts]` 附件解码，遵循偏移 33 的六位编码（不是 base64），覆盖不完整尾组、非法数据、数量与容量限制。单字体限制 32 MiB、单文档 64 MiB、最多 64 项；附件名不作为磁盘路径。字体经 fontkit 检查，使用文档局部上下文覆盖同名字体，不污染全局名称映射。SHA-256 标识实际字体字节，TTC 多字面在 IPC 中按哈希合并传输。预览直接加载附件字节，硬烧仍保留相同附件与字体解析规则。
- 系统字体发现扩展到各平台的系统/用户标准目录，识别 TTF/OTF/TTC/OTC、字族/完整名/PostScript 名及真实字形覆盖；同字族相邻变体不再仅靠文件名前缀查找。扫描深度上限 8、文件数上限 4096、刷新间隔 30 秒，字体文件缓存限制 32 项/64 MiB。删除文件后不会继续标为可用。仍未完成超大字体库主进程同步扫描的性能验收，也不声明支持所有非标准目录、符号链接目录或任意非 Regular 字面名称语义。
- 字体菜单接收当前字幕路径，内嵌字体可选择并显示样例；系统字体优先使用真实 PostScript/完整名本地加载。JASSUB 重建身份包含字体名称和附件哈希，同名但不同字节或移除附件不能沿用旧 worker 字体缓存。真实 E2E 曾发现附件字体缺空格时预览与成片字宽不同，现将缺失空格也纳入显式字形后备；ASS `\\h` 按不换行空格覆盖检查并保留原转义。
- 新增 `test:embedded-fonts`（二进制解码、文档隔离、菜单/加载、字形和空格后备、原生 libass 像素等价、附件移除）以及隔离用户字体目录的真实 TTF 索引/损坏邻居/卸载失效测试。未向用户真实字体目录安装测试字体。该入口加入集中基线，当前 **25 组基线通过**。
- 真实 Electron `test:e2e:embedded-fonts` 最新 `smartsub-compose-embedded-e2e-Vs0q8v`：使用系统未安装的 codicon 私有字形，实际菜单选择、默认/行内字体、FFmpeg 硬烧与 JASSUB 2px 双向覆盖 **1/1**，损坏附件和删除附件均显示错误而非静默替代，修复后恢复。菜单截图原透底为淡入动画中间帧，已等待 opacity=1、断言背景为不透明 `rgb(37, 42, 49)` 并滚动到目标字体；没有为测试截图改主题。
- 系统字体/预设 E2E 最新 `smartsub-compose-fonts-e2e-YJRE9a`：Menlo 在旧映射之外可发现、样例可见并真实导出；三套预设与 Menlo 的 IoU 分别为 0.954/0.881/0.590/0.909，2px 覆盖 1/1、1/1、0.994/0.999、1/1。复杂 ASS 回归最新 `smartsub-compose-ass-e2e-DB8QAV` 通过原位移/动画/裁剪/分层/撤销及三个时点像素检查。以上不证明所有 ASS 扩展节兼容：未知节紧跟字体附件时仍可能报错；前端 FontFace 样例缓存生命周期及大型字体库性能仍须后续治理。

### 合成多窗口草稿保护

- 发现同一工程两个窗口各自持有旧内存快照，自动草稿、显式保存和放弃都可能互相覆盖。现用 Chromium Web Locks 按草稿键独占编辑，持锁窗口离页、关闭或 renderer 崩溃后自动释放；等待窗口不读取旧快照、不编辑、不保存、不放弃，交接后读取最新磁盘版本。锁服务不可用时禁止写入并显示重试，不以无保护模式降级。
- 成功落盘数据不再永久占用内存优先缓存，仅失败自动写入保留内存草稿，并关联其磁盘基线；后续读取不会用旧成功快照覆盖另一窗口的写入/删除。属性和文件/输出控件在等待时禁用，输出路径缺失提示不再误导用户先选路径。偏好初始化在取得锁后重新读取，避免异步取得锁跳过默认偏好。
- 新增 4 项锁生命周期/StrictMode/等待取消/失败恢复测试，以及外部草稿写入、删除、损坏不被旧缓存遮蔽测试；完整 Jest **12 套件 116 项通过**。同一 profile 的真实 Electron 双窗口 `test:e2e:compose-multiwindow` 最新 `smartsub-compose-multiwindow-e2e-Nv5JKq`：第二窗口禁止保存及输出选择，快捷键不能写草稿；第一窗口离开后恢复最新已保存 Y=55；第二窗口改至 Y=65 后通过 `forcefullyCrashRenderer()` 实际崩溃，第一窗口自动取得锁并恢复草稿，撤销回到 Y=55。不同草稿并行和未取得锁即卸载由单测覆盖；此轮未执行生产 `app://` 包装后的验证。
- 合成工程 E2E 最新 `smartsub-compose-document-e2e-0y4QZ8` 完整通过保存/撤销、Cmd+B/空格隔离、导航和原生刷新/退出取消、真实刷新草稿恢复、注入存储失败的保存/放弃阻断、1024×700/1440×900，以及真实 FFmpeg 运行中重连/定向取消/半成品清理。数轮测试曾在后置 `app.evaluate` 注入文件选择器时报调试 Promise 回收错误；把固定测试选择器在启动阶段设置后全套通过，未删除功能断言。
- 本轮 25 组基线、116 项 Jest、typecheck、i18n 与 diff 检查通过；内嵌字体及双窗口最终回归也通过。远程只读核对仍为第一阶段 `c6f15acade6cc49eb0f90c940df89b07f6b7f620`，未生产构建、未作第二次提交。多窗口保护解决的是同一草稿并发编辑，不等同于已经解决多个同路径合成作业的身份重连。

### 同路径作业身份与多窗口队列事件

- 原重连仅按视频/字幕/输出三个路径取队列第一项；同路径多次导出时可接管、取消错误作业，完成期间实际输出自动改名后也无法恢复。现在每次启动先持久化请求 ID，再发 IPC；队列和确认事件同时携带该 ID，草稿运行元数据保存请求/作业 ID，不参与样式历史，也不把运行状态当成用户编辑。身份落盘失败不提交。作业确认前离页时可凭请求 ID 重连，已完成作业按实际发布路径恢复成功状态。
- 有身份时只自动连接该身份，不回退到另一个同路径作业；旧草稿存在多个候选时显示独立选择区（输出文件、运行/排队状态、创建时间、唯一 ID），选择后保存确切身份。失效身份仍可显式选取当前候选，但不自动猜测；新的用户编辑/撤销会解除旧作业关联。队列查询与事件用版本防止迟到读取覆盖新状态。启动回执失败时先核对同请求队列，确认仍在运行则保持编辑锁、取消入口和进度，不开放重复启动。
- 主进程按来源+请求 ID 去重，重复相同配置返回同一作业/完成 Promise，不同配置复用 ID 明确拒绝；空或超长 ID 拒绝。队列事件从只发送主窗口改为发送同 session、同协议/host 的应用窗口；单窗口关闭不阻断其他接收者，主窗口关闭后仍可向存活同源窗口发送。测试覆盖外部地址/其他 profile 不接收以及窗口销毁错误隔离。
- 真实双窗口测试暴露合成页直接加载时 `router.isReady` 首帧与服务器不一致导致 hydration 错误，现等待客户端挂载并且路由就绪后渲染。共享 `Progress` 原仅改变 Indicator、未向 Radix Root 传 `value`，导致可访问性进度一直不确定；已补传并增加数值/完成态/不确定态单测。等待草稿锁时也移除画布编辑回调，防止只改变局部拖拽预览而无法保存。
- 新增 `test:compose-queue`：同路径独立身份、排队定向取消、同请求复用、不同参数拒绝、终态实际路径和来源隔离；已纳入 **26 组集中基线，全部通过**。渲染层新增身份预写、无确认回执重连、输出改名终态恢复、旧查询隔离、失效身份不自动接管、回执丢失后仍保持运行、身份写失败不提交等回归。完整 Jest **13 套件 123 项通过**，typecheck、i18n、diff 检查通过。
- 新增真实 Electron `test:e2e:compose-reconnect` 最新 `smartsub-compose-reconnect-e2e-tD9reW`：两个相同路径的真实 FFmpeg 作业，第二窗口明确选择第二项、离页/重进仍恢复第二项，取消排队项后第一项仍运行；仅保留请求 ID 可恢复第一项，第二窗口进度数值实际增长，定向取消及私有目录清理通过；离页期间真实完成且同名成品已存在，恢复后显示 `shared_2.mp4`，原成品字节保留。1024×700 候选区无横向页面溢出，选择/运行/成功截图已查看；renderer 无 hydration/pageerror。
- 同期完整合成工程 E2E `smartsub-compose-document-e2e-fLjbft`、操作重试 E2E `smartsub-compose-retry-e2e-Gbr0lt`、多窗口崩溃恢复 `smartsub-compose-multiwindow-e2e-aeuSg7` 均通过。回执丢失的运行状态恢复由单测模拟，真实 E2E 的请求 ID 恢复通过修改独立 profile 的草稿模拟丢失确认，未声称实际网络或 IPC 故障注入。队列仍在主进程内存，最多保留 20 个终态；上述重连/去重不承诺跨应用重启继续编码、或已清退终态的永久去重。未生产构建或作第二次提交。

### 字体样例资源释放

- 移除前端永久 FontFace Promise 缓存，改为按字体实际来源（字族、完整名、PostScript 名、字幕路径和附件哈希）引用计数。仅可见菜单样例申请字体，离开可视区域或关闭菜单即释放；异步结果晚于释放时不再安装字体，旧失败不删除同名新请求，重复释放幂等。系统字体先尝试本地实际字面，再取 IPC 字节；内嵌字体仍保持文档隔离。
- 新增 4 项测试覆盖可见性、共享引用、卸载、晚到加载、切换附件文档、本地失败后回退和失败后重试。真实 Electron 全字体滚动 E2E `smartsub-compose-fonts-e2e-cij9r9` 断言已安装样例 FontFace 始终少于 40 个，关闭菜单后为 0；三套预设和 Menlo 导出回归通过，IoU 为 0.954/0.881/0.590/0.909，2px 双向覆盖 1/1、1/1、0.994/0.999、1/1。
- 内嵌字体 E2E `smartsub-compose-embedded-e2e-oX3mzY` 同样核对关闭菜单释放，私有字形预览/导出覆盖 1/1，附件损坏、移除及修复仍通过。新增测试一度因直接遍历 Map 不兼容 TS 编译目标失败，已改用 forEach，类型检查重跑通过。这解决字体样例生命周期，不代表已完成大字体库扫描/菜单性能验收。

### ASS 居中避碰、零边距与滚动字幕

- 真实 libass 像素回归证实两类布局损坏：居中隐式事件加上 `\\pos` 后失去自动避碰；上下对齐事件移至零边距时会回退样式边距，移至负边距时 libass 在避碰前裁掉事件。零边距现在将样式边距设为 0 并逐事件写入实际边距，不再回退；零偏移保留原隐式布局。涉及居中或越界事件时保留完整原布局，经 libass 渲染后平移整个透明字幕层，避免自行估算字高或改写碰撞顺序。
- 预览与导出共享 `buildStyledAssDocument` 的 ASS 内容和归一化偏移。JASSUB 显示层、实际 alpha 包围盒和拖拽锚点一起移动，撤销/重做和清除定位移除旧 transform；导出通过透明 RGBA、预乘 alpha 和整像素 overlay 合成。原显式坐标/动画仍沿用坐标平移，Scroll up/down 的裁剪区上下界随偏移变化，速度/延时及其余 Effect 参数不改写。
- `test:compose-canvas` 新增居中两行上下各 40 像素、顶部/底部零边距与越界三行堆叠、Scroll up/down 在 0.12/0.52/0.92 秒的真实 libass 像素回归；统一 RGB 路径下逐像素相等。四种音轨 keep/replace/mix/addTrack 与透明层、nv12 转换组合均通过真实 FFmpeg 导出，最新证据 `smartsub-compose-canvas-unit-ftuBD2`。JASSUB 新增偏移/复位和零边距锚点单测。
- 新增真实 Electron `test:e2e:compose-collisions`，最新 `smartsub-compose-collisions-e2e-XnSStZ`：实际指针下移字幕组 36 像素，居中两行保持堆叠，独立绘图同时平移，撤销/重做正确；1024×700/1440×900 无页面横向溢出。实际成片与预览 2px 字形双向覆盖 **1/1**，黄色、绿色及半透明文字都可见；最小窗口和导出帧已查看。半透明夹具明确关闭该行描边/阴影，防止把叠在黑描边上的字误当成直接与视频底色混合。
- 原复杂 ASS E2E `smartsub-compose-ass-e2e-vEhJtV` 重跑通过多层动画、裁剪、原生坐标、绘图、命名 reset、跨标签高亮、CJK 后备和三个时点成片比较。当前 **26 组基线、14 套件 129 项 Jest、typecheck、diff 检查通过**；最后新增音轨组合专项在基线之后单独通过。当前整层路径保留 libass 原画面内布局，尚未证明能恢复原渲染区域外已裁掉的字形，亦未做 HDR/10-bit 或该路径的 4K 性能验收，不泛称任意 ASS 均逐像素等价。
- 最后补齐首次数字定位的原生隐式参考锚点，识别行内居中对齐和事件边距，避免按面板默认底边距计算。专项最新 `smartsub-compose-canvas-unit-2BCpgQ`、居中组 Electron 最新 `smartsub-compose-collisions-e2e-8D5SdY` 通过；通用画布 Electron `smartsub-compose-canvas-e2e-CFh8Ae` 覆盖原拖拽/取消/微调、硬烧、MKV/MP4 软封、原视频包哈希、竖屏及错误恢复，同样通过。最终 typecheck、`check:i18n`、diff 检查通过，所有测试进程已结束；开发服务返回 200，远程分支仍为第一阶段 `c6f15acade6cc49eb0f90c940df89b07f6b7f620`。未生产构建或作第二次提交。

### 配音角色阵容、语速/音高与持久草稿

- 角色阵容从左侧配置移到字幕列表顶部常驻横向面板；单角色也显示。每个角色集中绑定音色、语速 0.5–2 和音高 -12–12 半音，角色试听传递相同设置。角色参数作用于所有以其为主说话人的行，包括有独立音色覆盖的句子；更改角色音色仍尊重单句覆盖。只有相关产物标记过期，角色重生成不重跑其他角色。
- 角色设置纳入音频输入身份；默认 1x/0 半音保留旧缓存键，非默认设置使无新身份的旧音频失效。FFmpeg 使用 asetrate/aresample 调整音高并以 atempo 补偿时长，保留采样率；只调音高不会按音高比例缩短音频。此为信号处理，不宣称感知无损或共振峰保持，真人语音主观质量仍需后续验证。
- 音色与语速/音高的显式修改统一等待原子 session.json 写入；失败回滚角色映射、合并冲突、语音过期标记、语言与配置快照。严格写失败不删除之前排队的快照。保存期间禁用角色/文件/引擎配置和开始/导出；迟到角色响应不污染新会话。
- 发现未合成工程离页会删除配置，且没有最近任务入口。现在导入即可靠创建会话和待处理任务，未合成的角色配置可从最近任务回开，按会话找已有任务防止重复。新字幕使用独立任务，不覆盖原任务；重建先落盘新会话和任务引用，再删除用户确认重建的旧会话。角色改名保留设置、单来源角色迁移自动继承；多角色合并且设置不同（包括默认 1x/0）时列出候选并要求确认，冲突跨重开保留，后端阻断批量、单行合成及导出，写盘失败不解除冲突。
- 会话加载按代次校验，切换字幕或卸载后迟到结果不覆盖当前状态；StrictMode 的同一在途恢复复用 IPC，避免双建任务；切页清理播放对象。每次合成用独立路径，成功、失败和取消后清理本次原始/角色/变速中间 WAV，保留仍有效的旧产物，不扫描删除其他会话文件。
- `test:dubbing-speakers` 覆盖真实 440 Hz WAV 的语速/音高/时长/24 kHz 采样率、默认缓存兼容、独立句音色、角色过期与局部重生成、未合成恢复、原子失败回滚/排队快照、重命名/迁移/合并确认、失败/取消中间文件清理和过期导出阻断。最近完整基线中的证据为 `smartsub-speaker-settings-ozAXzl`。合成服务边界使用确定性夹具，DSP 是实际 FFmpeg。
- 真实 Electron `test:e2e:dubbing-cast` 最新 `smartsub-dubbing-cast-e2e-h8rn6u`：UI 绑定两角色、语速 1.5/音高 +12、只重跑两句主讲人（总 5 次 HTTP 请求）、嘉宾产物不变；实际输出约 2 秒、基频 **879.9686 Hz**。首次合成前从最近任务恢复，SIGKILL 后重启配置和任务仍在；会话目录实际 chmod 写保护后显示常驻错误并回滚，恢复权限可重试。校对 sidecar 合并角色后真实点击候选确认；1024×700 和 1440×900 无页面横向溢出，1024 截图已查看，renderer 无 pageerror。全部使用隔离 profile/自建字幕和本机 OpenAI 兼容 TTS 夹具，不代表商业 TTS 音质验证。
- 当前 **27 组集中基线、16 套件 137 项 Jest、typecheck、i18n、diff 检查通过**；配音原有 217 项及语言全链路测试通过。随后补充导入后清除字幕的闭包回归，最终结果见本节后续记录。未生产构建或作第二次提交。
- 随后修复补选/清除媒体会重建配音会话的问题：独立 IPC 在同一会话更新媒体与时长，严格写盘失败回滚，探测期间锁定合成；恢复以 session 元数据为准，不被旧任务链接中的媒体路径覆盖。角色参数、句子和 WAV 不变；同时拖入新字幕/媒体仍按新项目一起加载。专项最新 `smartsub-speaker-settings-iy1d8y` 覆盖真实 WAV 时长、非法媒体拒绝及写失败回滚；Electron 最新 `smartsub-dubbing-cast-e2e-9pyo3p` 已完整通过，并新增 UI 选媒体、最近任务回开、清除媒体后核对相同音频路径。原生文件选择器在测试中固定返回夹具路径，不声称验证了 macOS 文件对话框。
- 最后回归为 **16 套件 140 项 Jest、typecheck、配音 217 项及语言全链路、i18n、diff 全部通过**。27 组集中基线在媒体绑定补丁前通过，补丁后的专项和 Electron 重跑通过，不把前次全量基线冒称为最终补丁全量重跑。所有测试进程已结束；开发服务 `http://localhost:8888/zh/dubbing/` 返回 200；远程只读核对仍为第一阶段 `c6f15acade6cc49eb0f90c940df89b07f6b7f620`。本轮未生产构建，未新增提交。

### 音色库抽屉、流式试听与单句保存

- 全局、角色、逐句音色入口统一使用抽屉，提供名称/ID 搜索、男/女/童声、语言与风格筛选以及未标注选项。只使用声明的元数据，不按名称猜测；移除 AIShell3 全部标为女性的错误值，保留 Azure/ElevenLabs 返回的语言、性别和风格。长列表使用固定行高虚拟化。真实 Electron 曾发现 portal 挂载时滚动容器为空导致“有数量无列表”，已改为容器挂载驱动更新，未用模拟虚拟列表单测代替真实验证。
- 悬浮 180ms 后发起代表句试听，离开/筛选/切引擎/离页取消；播放和缓存音频均最多 3 秒。独立 Audio/Web Audio 播放器不与逐句/全篇播放混用，提供停止、加载和常驻失败状态。角色阵容支持依次试听和中途停止；角色设置传入试听，与生成路径一致。样本按窗口隔离，最多 24 项、10 分钟有效期，LRU 淘汰/窗口销毁清文件；请求 ID 隔离取消，迟到结果不能播放或重新入库。缓存身份包含 provider 配置、语言、文本、角色参数及本地克隆参考文件的 size/mtime/ctime 和元数据；这不是逐次计算完整文件哈希，应用崩溃后的临时文件回收仍须审计。
- OpenAI 兼容与 Azure 的分块响应、ElevenLabs `/stream` PCM 和 Edge SDK MP3 流经实际 FFmpeg 增量解码为 24kHz mono PCM，经当前窗口专属 IPC 交给 Web Audio 调度。解码和播放均限制 3 秒，并对接取消、超时、输入背压和子进程退出；音高/语速在流式解码阶段应用一次，避免重复 DSP。缓存命中直接播放已保存 WAV。首包延迟取决于服务商，不能保证任意端点立即返回；本地 worker、豆包 JSON 流及 MiMo 当前仍走文件完成后试听，不把这些路径宣称为流式。
- 单句音色保存改为严格原子落盘后确认，失败不关闭抽屉、不乐观修改当前值；串行化逐句/角色修改并隔离跨会话迟到响应。已生成句子的修改先可靠保存输入，生成失败保留可重试输入与旧 WAV，并在抽屉显示错误。新 WAV 先严格写入元数据再删除旧产物；发布失败恢复旧路径并清理本次新文件。文本“保存并重新生成”失败时保留编辑区，不再无条件关闭。
- 新增 `test:voice-preview`：LRU/TTL/丢失文件、窗口与请求隔离、取消后迟到结果清理、provider/角色/克隆身份变化；真实慢速 HTTP WAV、MP3、裸 PCM 验证响应未结束即产出音频、3 秒限制、+12 半音约 880 Hz、提前关闭上游和取消后无输出。Edge 使用 SDK 传输夹具与真实 MP3/FFmpeg，覆盖解码取消、元数据等待期间取消后不能迟到打开流；未使用付费 API，也未据此宣称商业端点可用性或真人音质已验收。
- 真实 Electron `test:e2e:voice-library` 最新证据 `smartsub-voice-library-e2e-Cux9QO`：1,200 音色虚拟列表滚至末项，多维/未知值筛选及搜索；HTTP 仍打开时 Web Audio 已开始调度，实际 analyser 检测非零 PCM，累计音频 3 秒；再次悬浮使用 WAV 缓存且 HTTP 请求不增加；慢响应取消不播放、503 常驻错误后重试成功。真实 chmod 拒写时逐句覆盖不落盘，恢复权限后确认成功；已生成句子换音色遇 503 时旧 WAV 仍在，重试成功替换。1024×700/1440×900 截图无页面横向溢出；最新音色库及角色阵容的 1024 截图均已查看。
- 角色阵容 Electron 最新 `smartsub-dubbing-cast-e2e-KIFWt7` 完整通过原有角色绑定、SIGKILL/最近任务恢复、权限失败重试、真实 879.9686 Hz DSP、局部重生成、媒体绑定与合并确认，并新增两角色顺序试听和停止；HTTP 总请求 7 次（生成 5 次、试听 2 次）。原生系统文件选择器仍由夹具返回，不声称测试了 macOS 文件选择 UI。
- 本轮 **28 组集中基线、19 套件 152 项 Jest、typecheck、i18n 和 diff 检查通过**。基线中的角色专项证据 `smartsub-speaker-settings-ggGLMk` 新增逐句拒写回滚、新 WAV 元数据发布失败保留旧文件、预听裁剪和取消清理。新增 Edge 专项在全量基线之后单独通过；配音原 217 项及语言流程先通过，随后新增两项元数据断言因旧助手按 JSON 字段顺序比较失败，修正后最终重跑结果见后续记录。未生产构建、未新增提交，远程只读核对仍为第一阶段 `c6f15acade6cc49eb0f90c940df89b07f6b7f620`。
- 最终补跑：配音 **219 项及语言全链路通过**；增加流式播放到限而服务端未返回、切换音色后的旧分片/迟到文件响应隔离两项回归，完整 Jest **19 套件 154 项通过**，typecheck/diff 通过。所有本轮测试进程均已结束；开发页 `http://localhost:8888/zh/dubbing/` 返回 200。总目标仍进行中，第二阶段未作提交推送。

### 配音仍待完成的明确差距

角色矩阵、音色抽屉、元数据筛选、3 秒样例缓存、悬浮取消、阵容连续试听及四类云端流式路径已有实现和上述验证，豆包 JSON 分片预听已在后续专节补齐。仍需核实本地引擎/MiMo 的实际流式能力与合理回退、补充真实人声样例和主观质量验收。对齐的 15% 阈值、红条、AI 缩写及显式借空白实现与验证见下节；纯音频安全发布和失败任务恢复见后续专节。另需处理一般配置可靠保存（当前 localStorage 写入异常仍未纳入可重试流程）、其余在途操作生命周期和多窗口同配音工程写保护。P2-06/P3-01 继续未勾选。

### 实测时长对齐、显式借空白与 AI 缩写

- 原实现以“本句起点到下句起点”计算窗口，默认增加未知尾部 1 秒，估算驱动原生提速并允许 1.5 倍自动处理，与 3.4 的原字幕区间 15% 规则冲突。现在先按用户整体语速和角色设置合成，读取真实 WAV 时长；不再估算提速或自动再调用付费 TTS。恰好 15% 归入自动压缩，超过边界 1ms 即保留原 WAV 并进入人工处理。自动处理使用实际 FFmpeg atempo，冲刷分析尾窗后限定目标时长，保持采样率与音高；不宣称感知无损、共振峰保持或所有人声均无伪影。
- 原时间窗不隐式借空白。显式借用用排序和前缀最大终点检查真实后方可用区间，覆盖乱序、嵌套重叠、相邻字幕、零长区间和媒体结尾；未知媒体尾部不虚构空白。借用保留完整 WAV、只扩展当前句，导出使用混合轨锚定所有原起点，不推迟后句。借用量严格落盘后确认，失败回滚；重新生成会撤销旧借用，更换为较短媒体会重新验证并恢复超限状态。
- 行内红条展示原窗/生成时长和“超限 +0.80s”，提供 AI 服务选择、“AI 缩写并重生成”和“借用后续空白”。缩写沿用现有 AI 调用，系统和用户提示均要求保留事实、名称、语义和语言并包含目标秒数；拒绝空结果和不更短结果，保留 Speaker 标签。请求有独立取消 ID、60 秒期限、会话/配置变更取消及迟到结果隔离；生成前校验原句/音色/WAV 身份，避免覆盖其他窗口的新输入。缩写先可靠保存文本再重合成；失败保留旧 WAV 与可重试输入。模型语义忠实度仍需人审，不把确定性夹具当作真实 AI 质量验收。
- 新规则与原区间纳入合成身份。旧 WAV 保留播放，但必须重新生成后才能导出，避免把旧隐藏提速当作新规则结果。E2E 发现缩写后语言检测置信度下降会使其他三句错误过期；现在不确定检测保留已有语言，并把该判断持久化，明确的新语言仍能触发更新。批量与单句均先严格发布新 session 元数据，再移除旧 WAV；拒写时保留磁盘引用并清理本次产物。
- 前后端阻止未合成、失败、过期或超限配音静默截断导出，工作台移除旧“超长截断/整体顺延”选择。流水线同样停止而不是输出损坏音轨，失败任务行提供“检查配音”入口。仍需对真实完整任务执行“超限停下→工作台修复→重试成片”的专门验收，尤其是跨窗口设置与任务配置合并；不因基础语言流水线测试通过就称此场景完成。
- 新 `test:dubbing-auto-fit` 使用真实 24kHz WAV 与 FFmpeg，验证 1900/2000/2001/2299/2300/2301/2800ms 对 2000ms 原窗、虚假服务端时长不影响判定、440Hz 基频、880Hz 句尾标记、保存失败/批量发布失败清理、恢复、媒体变短、借用后真实 PCM 空白与后句起点。Jest 增加借用失败重试和并发互斥、取消/切会话/改配置迟到 AI、不更短文本、Speaker 标签保留。原对齐测试已更新，最终 **229 项及语言全链路通过**。
- 真实 Electron `test:e2e:dubbing-auto-fit` 最新 **`smartsub-auto-fit-e2e-9uQ8wC`**：实际 HTTP TTS/AI 夹具共 5 次生成、3 次 AI；2.2s 自动压到 2.0s，2.8s 红条显示 +0.80s；真实 chmod 拒写借用失败后重试成功，无后方空白的句子拒绝借用；AI 400 错误、在途取消及重试均验证。最后从界面点击导出，读取导出 PCM 验证 6.6–6.75s 句尾存在、7–7.95s 静音、8.05s 后句仍按原时间开始。刷新后借用和缩写仍在；1024×700/1440×900 无页面横向溢出，超限与完成截图已查看。未使用商业端点，也未覆盖原生 macOS 文件选择器。
- 最终 **29 组集中基线、19 套件 159 项 Jest、typecheck、i18n、diff 检查通过**。第二阶段仍未生产构建或提交；只读核对远程仍为 `c6f15acade6cc49eb0f90c940df89b07f6b7f620`。P3-01 暂不勾选，保留真人语音、多引擎、流水线恢复、音频安全发布和整体生命周期验收。

### 配音安全发布与失败任务恢复

- 工作台 WAV/MP3 原先直接复制或编码到最终路径，流水线重试还会覆写上次交付物。现在复用合成的私有暂存与排他发布；音频/视频与可选字幕使用同一重名后缀，字幕碰撞会撤回本次主文件并共同换名。取消、字幕发布失败、无硬链接回退写失败时只移除本次拥有的 inode，不删并发替换的文件。单文件硬链接发布原子，但多文件并非跨文件系统事务；进程被强杀在两次发布之间、真实 exFAT/网络卷仍需专项验证。
- 配音轨在会话内独立目录生成，构建期间锁定会话并接受当前操作取消，拒绝重叠构建/重合成。已成功交给流水线的旧轨保留，后续重建不会改写排队成片的输入；临时分轨和失败构建目录清理，工作台导出后清理本次组装轨。保留轨的长期回收与崩溃残留治理尚未完成，不声称磁盘占用已最优化。
- 任务配音源改为按内容 hash 的不可变 SRT 快照（其他格式先经实际解析器转换），上游字幕改变时连同内存会话一起验证；新建失败不先删除旧会话和 WAV。重试按最后任务配置与当前任务字段差异合并工作台配置：未变字段保留人工修正，显式改变的任务字段生效；该任务基线随会话保存。完成检查点重建也重新核对上游文本。任务取消只取消它自己的批次/组装操作，不误取消后续工作台操作。
- AI/人工改过的配音文本进入最终字幕；双语仅替换唯一完整对应的译文行块，保留源文，错位/歧义明确阻断。保持原展示窗口，借空白才按实际延长；完全未改文本和时间轴时沿用原字幕文件，不无条件转 SRT 丢失样式。复杂富文本改写、重新折行造成非唯一对应仍有边界，未宣称任意 ASS/双语布局无损编辑。
- 失败任务进入工作台显示“任务配音修正”，没有审核检查点时禁止放行。整体语速、输出形态、音频格式、对齐字幕开关补可访问名称。截图复核发现成功成片后仍显示上轮配音错误：原因是任务事件合并不会清除已删除键，现用显式空错误覆盖，并在后端和真实 UI 同时断言。
- 后端补测真实 WAV/MP3、字幕单独重名、成组发布失败回滚、并发替换保护、取消、重复导出、构建互斥和轨道不可变；语言流程覆盖工作台配置重试、显式任务改动、双语替换/歧义/时间轴拒绝、重启配置和上游改动保留旧会话。`test:e2e:dubbing-auto-fit` 最新 **`smartsub-auto-fit-e2e-yEDCN7`** 完整通过原对齐/AI/借空白场景，并通过界面重复导出 WAV/MP3、字幕碰撞保留、实际 chmod 拒写/重试和暂存清理，仍仅 5 次 TTS/3 次 AI。
- 新 `test:e2e:dubbing-recovery` 最新 **`smartsub-dub-recovery-e2e-rVoEEt`**：用户素材 `demo.mp4` 截取 7 秒，真实向导提交配对字幕→超限报错→点击检查配音→键盘调整体语速 1.05→AI 缩写→重生成过期行→返回任务→重试，最终生成 MKV。首次 2 次加修正 2 次 TTS，重试无额外调用；核对 WAV 路径复用、源字幕字节不变、成片字幕 `Short.` 与 4–6 秒原窗、实际 PCM 非零/空白/后句起点。另从普通工作台真实导出 MP4 与同名字幕，已有字幕保留并共同改名。1024/1440 无横向页面溢出，完成态截图已查看，旧错误消失，renderer 无 pageerror。服务端为本机确定性 HTTP 夹具，不代表商业 TTS/AI 质量；原生系统文件选择器未验证。
- 成片检查使用 `-copyts` 提取字幕：不加该参数时 FFmpeg 会因 AAC 负起点把字幕统一平移约 43ms，保留容器时间戳后原窗准确，不为通过测试改写编码时间轴。原人工闸门真实 E2E **`smartsub-gates-e2e-ogEPAt`** 通过字幕/配音两检查点、失败恢复及硬烧成片。29 组基线、19 套件 159 项 Jest、229 项配音及扩展语言流程通过；旧错误清理补丁后专项和恢复 E2E 再次通过。第二阶段未生产构建、未提交，远程仍为第一阶段 `c6f15acade6cc49eb0f90c940df89b07f6b7f620`。

- 本轮最终 typecheck、i18n、diff 检查通过，所有测试进程已结束，开发服务 `http://localhost:8888/zh/home/` 返回 200。新增字段类型补齐后 typecheck 再次通过；没有把前次全量基线冒称为最后两行错误清理补丁后的全量重跑。

### 大字体库与 ASS 扩展节收尾

- 系统字体发现改为合并并发请求的异步目录/文件读取，逐文件让出事件循环，30 秒缓存；菜单仅保留字体名称和样例字形覆盖元数据，实际字体解析缓存限制为 32 文件/64 MiB。预览、字体字节 IPC 和压制入口先等待发现完成，同步查字形不再触发全库扫描。刷新清除失效别名，测试覆盖原地损坏、修复、卸载和重命名。
- 字体菜单改为搜索和固定 64px 虚拟行，支持上下/Home/End/Enter、跳过不可用项和 IME 确认隔离。打开才读取列表，读取期间禁止选择，错误常驻并可重试；切换字幕/禁用后关闭并忽略迟到响应。新增 4 项选择器测试，真实 Radix 布局和焦点交给 Electron 验证；JSDOM 中真实弹层布局每次等待约 8 秒，单元测试隔离布局后 8 项选择器/字体样例测试通过，没有靠放宽超时完成。
- ASS 字体解析识别 Aegisub Extradata 和可区分的自定义扩展节，保留可形似 `[AB]` 的合法编码行。仅修解析器仍不够：真实 libass 会把 Fonts 后的未知扩展元数据读成字体内容，造成预览正常而烧录缺字。现在生成的 ASS 把所有字体附件合并到文件尾，其他节保留；幂等和实际像素等价均验证。全大写、仅含六位编码合法字符且后续内容同样无法区分的扩展仍有格式歧义，不承诺任意非标准扩展都可判别。
- 后端 `smartsub-installed-fonts-UxbREz` 在隔离字体目录读取 1,200 个真实 TTF 硬链接副本，合并并发扫描约 275ms，5ms 心跳 30 次、最大间隔 6.1ms；这是同一真实字体的文件数量压力测试，不是 1,200 个不同复杂字族。单文件仍由主线程解析，巨大复杂字体与整层 4K 性能尚未验收。
- 新真实 Electron `test:e2e:compose-font-menu`，证据 `smartsub-font-menu-e2e-w0mj7r`：1,200 个不同选项共享真实系统字体样例，1024×700/1440×900 打开约 111/52ms，无观察到的 50ms 以上长任务；实际滚轮、Home/End 焦点、搜索/空结果、错误/延迟期间禁止选择、重试后 Enter 选择均通过。DOM 选项不超过 12，字体资源少于 40，关闭全部释放；无页面横向溢出，最小窗口截图已查看。这验证 UI 数量性能，不冒称 1,200 字族渲染压力测试。
- 已安装字体真实 E2E `smartsub-compose-fonts-e2e-Du2CIv` 经搜索逐个加载样例，三预设与 Menlo 预览/压制 IoU 0.954/0.881/0.590/0.909，2px 双向覆盖均不低于 0.994。内嵌字体 E2E `smartsub-compose-embedded-e2e-tTtR4K` 带 Aegisub Extradata，实际私有字形预览/成片覆盖 1/1，附件损坏/移除/修复通过，截图已查看。原生像素单测证据 `smartsub-embedded-fonts-unit-hNoHIY`。
- 本轮 29 组集中基线、20 套件 163 项 Jest 和 typecheck 全部通过。第二阶段未生产构建或提交；下一步继续豆包流式试听、配音可靠保存/生命周期、下载承接与全局人机工学，未将总目标标记完成。

### 豆包 JSON 流式试听与残缺响应防护

- 豆包 V3 HTTP 试听从等待全文 JSON 改为增量解析分片 PCM，沿用现有三秒 FFmpeg/Web Audio 管线和角色语速/音高设置；读流按消费进度推进，达到时长或取消时关闭上游 HTTP。沿用项目原有 JSON 分帧逻辑并扩展跨块状态，数据结构仍由 JSON.parse 解析；UTF-8 跨字节、字符串花括号/转义、空行及直拼均覆盖，每帧限制 2 MiB，严格核对 Base64、业务码、结束标记和完整 PCM 样本。
- 审计同时发现正式合成原先容忍残缺 JSON、未见结束码仍会发布半段 WAV。正式合成现复用严格流解析，只在完整成功后写音频；业务错误、协议截断和取消不能作为成功产物。历史宽容全文解析仅用于 HTTP 错误详情提取，原纯函数兼容测试仍通过。试听达到三秒会主动断开，因此不承诺验证三秒之后的服务端内容。
- 新 `scripts/dubbing/test-volc-preview.ts` 纳入 `test:voice-preview` 和集中基线：逐字节拆分、1 MiB PCM 帧、非法编码/超限/缺结束/半样本/业务错误、真实慢 HTTP 在 EOF 前收到 PCM、三秒截断、+12 半音约 880Hz、上游关闭、取消不缓存、短句与正式合成完整性。证据 `smartsub-volc-preview-q3srw6`；服务地址仅在测试中重定向至本机，不修改正式端点、不调用商业账号。
- 真实 Electron `test:e2e:volc-voice-library` 最新 `smartsub-volc-library-e2e-ztmPXs`：1,200 音色筛选/虚拟列表、非零 Web Audio 在 HTTP 未结束前播放、三秒停止和 WAV 缓存重播、悬浮取消、503 常驻错误与重试、真实 chmod 拒写、逐句更换音色失败保留旧 WAV、重试成功，1024×700/1440×900 无横向溢出，最小窗口截图已查看。共 8 次 HTTP、3 次播放。第一次测试因夹具漏填必需 resourceId 被就绪校验拦截，补齐夹具后通过，没有放宽产品校验。
- 原 WAV 协议音色库回归 `smartsub-voice-library-e2e-da9SZL` 通过相同场景。此轮最终 29 组集中基线、20 套件 163 项 Jest、229 项配音及语言流程、typecheck 通过；新增取消等待/超时断言另作最后专项验证。以上为确定性协议与实际播放验证，不能代替商业豆包可用性或真人听感验收。本地/MiMo、可靠配置保存、其他生命周期和后续模块仍待完成，未作第二次提交推送。
- 最后专项 `smartsub-volc-preview-ziROSj` 已通过等待响应时取消、请求超时、正式合成截断/业务错误无产物，最终 typecheck、i18n、diff 检查再次通过。全部测试进程结束，开发页 HTTP 200；远程只读核对仍为第一阶段 `c6f15acade6cc49eb0f90c940df89b07f6b7f620`。

### 配音配置可靠保存、显式恢复与操作异常

- 一般配音配置原先直接使用通用 localStorage hook，读取/写入异常未进入恢复流程；`dubbing:syncVoiceState` 还会吞掉磁盘写入失败。现改为保留输入、常驻错误及折叠详情、重试/还原，接入同事件离开守卫；保存串行化并继续提交等待期间的新输入。会话配置与音频过期标记严格原子落盘后确认，失败回滚内存；读取失败或损坏值保留原始字节，由用户明确重试或重置。
- 保存确认丢失时不能假定后端没写入：还原会重新提交最后确认值，在确认之前阻止离开。项目恢复优先使用会话快照，不让其他项目的全局默认覆盖。默认引擎/音色一旦确定即记录身份；原引擎被移除、模型未就绪或原音色消失时保留选择、显示不可用提示，禁止开始，不自动换引擎或换声。新项目未完成选择时仍可更换素材；实际编辑了不完整配置则不能假报保存。前后端共用配置字段校验，覆盖可选格式、并行数、语言类型与布尔值等。
- 开始/导出/取消补 IPC 拒绝捕获、严格成功结果判断、同事件重复请求互斥和跨会话迟到结果隔离；全量重跑不再在确认前清空原行状态，失败保留现有音频视图。组件卸载的会话清理拒绝不再成为未处理异常；加载项目期间禁改全局配置，避免恢复覆盖新输入。尚未宣称完整在途恢复：执行已发生但响应丢失的幂等重连、多窗口会话写所有权仍待处理。
- 后端专项 `smartsub-speaker-settings-3Na1pJ` 验证非法配置不改磁盘、实际重命名失败回滚配置/过期状态、成功保存恢复；新增真实 Electron `test:e2e:dubbing-config`，证据 `smartsub-dubbing-config-e2e-Gsy0Lb`：localStorage 拒写、真实 chmod 会话拒写、保存并离开失败保持当前页、重试、写入后确认丢失再还原、延迟 IPC 期间连续键盘修改、SIGKILL 已确认配置恢复、项目快照优先、原引擎/音色缺失及用户明确替换。1024×700/1440×900 无页面横向溢出，截图已查看；截图等待离开弹窗完全移除，避免截到退场动画。
- 扩展 `test:e2e:dubbing-auto-fit`，证据 `smartsub-auto-fit-e2e-faPycI`：真实窗口注入开始/取消/导出 IPC 抛错，常驻错误、按钮恢复、重试成功且无 pageerror；原 15% 对齐、AI 失败/取消/重试、借空白、实际 PCM 起点/尾音、WAV/MP3/字幕重名保护、导出拒写回归全部通过（5 次 TTS、3 次 AI）。流水线修复后重试成片回归 `smartsub-dub-recovery-e2e-mVBZHP` 通过（4 次 TTS、1 次 AI），不额外合成已修复行。
- 本轮集中基线 29 组、Jest 20 套件 185 项、配音 229 项及语言全链路、typecheck/i18n/diff 通过；后续新增未配置项目可更换素材的回归以末尾最终结果为准。初次 E2E 因浮点断言 `1.1 + 0.05` 与 `1.15` 严格比较失败，已按 UI 步长规范化期望；一次开发服务重启缺 PATH 导致两次加载超时，修复启动环境并确认 HTTP 200 后重跑，不将这些失败解释为产品已通过。
- 容灾边界：全局默认与项目文件并非跨存储事务；localStorage 写成功而项目保存失败期间强杀，项目仍恢复旧快照，默认可能保留尝试值。尚无未确认配置的持久化事务日志，失败输入当前靠内存和离开守卫保留，不声称强杀时零丢失。同一配音项目多窗口写保护、未提交逐句文本草稿、真人多引擎听感和本地/MiMo 能力仍待完成。第二阶段未生产构建、未提交推送。
- 最终补跑：Jest **20 套件 186 项**与 typecheck 通过；最新配置 Electron 证据 **`smartsub-dubbing-config-e2e-9lwiIF`**、任务修复成片 **`smartsub-dub-recovery-e2e-aCOWHq`** 均通过。开发服务因加载新增文案重启，当前仍在 `http://localhost:8888/zh/home/`，没有把真实 Electron UI 自动化误称为原生 Computer Use 插件控制。

### 配音多窗口写保护与后台任务交接

- 原先两个窗口恢复同一 session 会共享可写内存，任何窗口退出都可释放或取消另一个窗口的会话。现在主进程按 sessionId/webContents 分配单窗口所有权，在恢复和重建确认之前原子申请；10 类写入/取消 IPC 强制校验持有者，非持有者不能保存、改媒体、改角色、改逐句音色、重生成、借空白、导出或取消，释放请求也不能释放别人的项目。正常导航、窗口关闭和真实渲染进程崩溃会释放；整页导航代次隔离迟到加载，互相冲突的恢复/重建 ID 直接拒绝。
- 等待窗口显示常驻占用横幅，不载入可编辑旧快照、不自动写默认配置、不显示误导性的空态导入入口。每秒只重试一次恢复，没有强行抢锁或超时解锁；持有者离开后从当前会话/磁盘重新加载。源字幕变化的确认态也占有项目，取消重建或卸载会释放且保留旧音频。未读取到项目不注册伪脏状态，不拦等待窗口正常离开。
- 进度由原先固定主窗口改为投递当前持有窗口。配音工作台离开不会隐式取消主进程中的批量、单句或导出；后端操作完成时推送终态会话快照、错误和导出路径。接手窗口能够退出忙碌状态并看到结果，原请求仍活跃的编辑器忽略重复快照。真正取消仍走显式取消操作。这里处理的是应用主进程继续存活的交接，并非整个主进程被强杀后的在途执行恢复。
- 新 `test:dubbing-ownership` 纳入基线，覆盖重复申请、竞争、独立会话、旧持有者释放、按窗口清理；Hook 补等待禁写、退出等待不释放别人、拒绝重建释放及后台完成事件的会话隔离。最新 Jest **20 套件 190 项**、集中基线 **30 组**、配音 **229 项及语言流程**、typecheck/i18n/diff 通过。
- 真实 Electron `test:e2e:dubbing-multiwindow` 证据 **`smartsub-dubbing-multiwindow-e2e-7vhCt1`**：第二窗口 10 类原始 IPC 写入均被拒绝，磁盘字节不变，退出等待不影响原窗口；修改 1.05/1.10 语速后切换持有者恢复最新配置。持有窗口在真实 HTTP TTS 尚未返回时 `forcefullyCrashRenderer()`，等待窗口接手、收到完成并可导出，未重复请求；单句重生成离开重进也仅一次调用。真实 WAV 导出在 copy 阶段挂起后离开重进，放行后收到输出路径，FFmpeg 解码成功。源字幕变化时两个窗口争用确认，取消后交接且旧 WAV 仍存在。1024×700/1440×900 无页面横向溢出，等待/恢复截图已查看，renderer 无 pageerror。夹具最初误用 evaluate 内动态 import 导致测试未进入导出，改为仓库现有 `process.getBuiltinModule` 后完整重跑成功；不是产品导出失败。
- 配置恢复回归 **`smartsub-dubbing-config-e2e-bbYhEb`**、失败任务成片 **`smartsub-dub-recovery-e2e-FpCsgF`**、对齐与传输异常 **`smartsub-auto-fit-e2e-N2q2ID`**、角色阵容 **`smartsub-dubbing-cast-e2e-x6VbuF`**、音色库 **`smartsub-voice-library-e2e-9n9MkS`** 均通过；仍使用隔离配置和本机确定性服务，不调用商业 TTS、不代表真人音质验收。
- 边界仍明确：这层锁覆盖一个 Electron 主进程的多个窗口，不是两个独立应用进程共享用户目录的跨进程文件锁；后台流水线与工作台编辑尚未采用同一事务所有权，其他窗口删除项目的全链路一致性也未纳入本轮。单窗口按 sessionId 校验尚未给每次页面挂载分配租约令牌，同一 webContents 的异常延迟旧请求仍需专门防护。未确认配置/逐句编辑的崩溃草稿、执行已发生但确认丢失时的幂等重连、主进程崩溃产物回收仍待继续。第二阶段未生产构建、未作第二次提交推送。
- 最后补入恢复/重建 ID 冲突拒绝和旧整页导航加载清理隔离后，完整多窗口专项再次通过：**`smartsub-dubbing-multiwindow-e2e-0oROHH`**；typecheck/diff 通过，全部本轮测试进程已结束，开发页 HTTP 200。

### 配音页面租约隔离与重建身份修复

- 窗口编号不足以区分同一窗口先后打开的配音页面。现为每次加载分配独立 `leaseId`，所有 10 类写入/取消 IPC、释放、进度及后台完成快照均绑定租约；旧页面迟到的请求和事件不能影响继任页面。释放的令牌在窗口存活期内记为失效，不能在新页面退出后重新取得编辑权；窗口销毁清理令牌，导航/渲染崩溃也使在途加载失效。锁仍限定同一主进程，不是跨应用进程的文件锁。
- 新增只读 `dubbing:getSession`，只生成当前/磁盘会话视图，不申请租约、不修改最近任务，也不将磁盘快照注入可编辑内存缓存。迁移角色阵容、自动对齐、音色库、失败任务修复和说话人流水线的状态查询；测试创建会话后显式释放夹具租约，再由实际页面重新加载。
- 加载尚未完成时离开页面可按租约取消，媒体探测完成后清理新建但已放弃的会话，不遗留任务。StrictMode 的同一在途加载仍复用；确认重建期间保留旧会话预留锁，避免路径回填重复触发 effect 时提前释放。真实端到端测试额外发现仅凭 sessionId 打开的过期源字幕重建会产生新任务，已修复为后端查回并沿用原 workItem；只有新会话及任务关联写入成功才清理旧会话。
- 新增 Hook 回归覆盖旧租约进度/完成事件、同项目重新进入、完成前取消加载及异步重建预留。最新 **20 套件 193 项 Jest**、**30 组集中基线**、**229 项配音单测及语言流程**、前后端 typecheck、i18n 通过。租约单测覆盖同窗竞争、过期释放、令牌退休及窗口清理。
- 完整真实 Electron 专项 **`smartsub-dubbing-multiwindow-e2e-abKA51`** 通过：同窗口重开后 10 类旧租约写入和释放均被拒绝，快照不含租约且磁盘字节不变；实际媒体探测中取消后无残留会话，旧令牌不能重载；双窗交接、真实 renderer crash、单句后台重生成、真实 WAV 导出及 FFmpeg 解码、源字幕变化时取消/确认重建、任务身份保留均通过。1024×700/1440×900 无横向溢出，等待及恢复截图已查看；无 pageerror。首次扩展测试的 IPC 包装早于注册，加入明确就绪等待后重跑；重建身份断言曾检出上述产品缺陷，修复后才计为通过。
- 回归证据：配置容灾 **`smartsub-dubbing-config-e2e-dubIkr`**，失败任务修复成片 **`smartsub-dub-recovery-e2e-fwOoKr`**，对齐及 IPC 异常 **`smartsub-auto-fit-e2e-ZdqjPk`**，角色阵容 **`smartsub-dubbing-cast-e2e-T0TEzK`**，音色库 **`smartsub-voice-library-e2e-XmW8yh`**。这些使用隔离配置和本机确定性 TTS/AI 服务，不代表商业引擎或真人听感验收。
- 仍待：未确认配置的崩溃日志、未提交逐句文本草稿、流水线与工作台共享所有权/删除一致性、执行成功但响应丢失的幂等重连、主进程崩溃清理，以及下节列出的 3.5/4 验收。当前已获准启动的后台操作继续运行是设计行为，租约拒绝的是已经失效页面的新写入，并不撤销已开始任务。第二阶段尚未生产构建或提交推送；远程仍为第一阶段 `c6f15acade6cc49eb0f90c940df89b07f6b7f620`。

### 配音未确认配置的持久化恢复日志

- 原配置失败输入仅保留在 Hook 内存中；已确认项目快照会在重开时覆盖尝试值。现每次配置修改同步写入按 sessionId 隔离的 localStorage 草稿，并串行写入主进程恢复日志；项目配置保存必须等待日志确认。恢复日志在 `dubbing-sessions/.config-drafts/` 下按会话标识的 SHA-256 命名，不使用未经处理的路径片段。临时文件以 `wx`/0600 创建、写入并 `fsync` 后原子重命名；失败保留旧记录，清理本次临时文件。
- 首版仅写 localStorage，真实整应用 `SIGKILL` 当场检出：同步 API 返回并不代表 Chromium 已将数据持久化，失败修改仍可能消失。因此没有加测试等待来规避，而是补上述主进程日志；最新专项在项目保存失败后立即强杀、以及实际保存完成但确认丢失后立即强杀，均可恢复。启动读取两层记录，使用修改版本选择较新输入；等待用户明确恢复/放弃前，不自动保存、不放行生成/导出或替换素材。损坏 JSON、错项目标识、读取异常保留原字节，重读或明确放弃才能继续。
- 配置保存确认后才清日志；版本校验防止“清理期间继续修改”被旧保存误清。日志写入/清理使用前值比较及同内容重放，迟到回调不能清除外部替换记录；写入结果丢失时读回核对，读回也失败则保留未知写入状态，重试前重新确认。两个日志 IPC 也校验页面租约，旧页面与其他窗口不能读取或清除持有者日志。
- 放弃配置现在等待日志清理；全局离开守卫支持异步 `onDiscard`，失败/拒绝留在当前页，执行中禁重复按钮和关闭弹窗。真实“放弃并离开”覆盖日志删除被拒、延迟删除、重试后才离开，且不再把已落盘但丢失确认的项目配置恢复成旧值。现有同步放弃调用方不需变更。
- 新 `test:dubbing-config-draft` 纳入集中基线，实际文件验证原子替换、`fsync`/rename 失败保留旧日志、临时文件清理、版本冲突、重复写入/删除、损坏字节和路径隔离；证据 **`smartsub-dubbing-journal-66xqct`**。Hook 回归覆盖同步草稿、连续修改、恢复选择、只读失败、写入/删除失败、迟到确认冲突、更新版本选择、清理竞态、确认/读回均丢失后的重试。最新 **20 套件 205 项 Jest**、**31 组集中基线**、前后端 typecheck、i18n/diff 通过。
- 真实 Electron 配置专项最新 **`smartsub-dubbing-config-e2e-AqUFbV`**，含立即整应用 SIGKILL 恢复、真实 chmod 项目拒写、localStorage 拒写、确认丢失、损坏恢复记录、异步放弃/重试以及缺失引擎/音色。1024×700/1440×900 恢复横幅无溢出，截图已查看（相同行为前一轮 **`CPpXXh`**）。扩展异步放弃测试最初错误复用已被强杀窗口的定位器，修正后完整重跑，不作为产品失败。
- 共享守卫回归：校对 **`smartsub-proofread-e2e-T7hd2a`**、合成 **`smartsub-compose-document-e2e-x393KX`**、向导 **`smartsub-wizard-e2e-c0MgBp`** 均通过；配音多窗及两个新增日志入口 **`smartsub-dubbing-multiwindow-e2e-w4n2AZ`**，失败任务修复成片 **`smartsub-dub-recovery-e2e-8tRsDr`**，自动对齐/IPC 异常 **`smartsub-auto-fit-e2e-rEDKv3`** 通过。应用主进程仍运行的后台终态在读取草稿期间到达，不再被旧加载快照重新置为运行中。
- 边界：已验证进程强杀，不宣称整机断电的跨文件事务保证；按键事件尚未送达主进程且 localStorage 尚未落盘的极短窗口仍不能承诺零丢失。配置两层存储均不可写时保留内存并阻止离开，不伪称持久化成功。没有导入会话时的全局默认仍走原 localStorage；未提交逐句文案目前仍只有组件内存，切换展开行/离开存在丢失风险，需继续实现逐句草稿及守卫。删除/重建项目与旧日志回收、流水线共同所有权也尚未作最终验收。第二阶段仍未生产构建、未提交推送。

### 配音逐句文本草稿与独立保存

- 代码审查确认逐句编辑原先共用一个组件内 `draftText`，切换展开行就覆盖输入，离开也不注册脏状态；唯一保存入口同时触发 TTS。现由独立 `useDubbingCueDrafts` 按行持有文本、原始文本及起止时间，切换/折叠/筛选/虚拟列表卸载不丢输入；支持单句或全部保存、明确放弃，保存文本不需要引擎配置、不发语音请求、不覆盖导入的原字幕文件。空字符串也作为明确的文本编辑保存。
- 新 `dubbing:saveCueTexts` 按整批校验重复行、类型、时间和比较原文，任一冲突均拒绝整批；同文本重放可幂等重试。沿用严格会话落盘及异常内存回滚，成功后重算文本语言和所有音频输入身份，将不匹配的旧音频标记需更新。最后补测发现仅有文本、未配置引擎且未关联工作项的会话在释放时会被当作空项目删除；新增持久化文本编辑标记并纳入保留条件，已保存配置也纳入保留。没有配置的项目也可独立保存并恢复文本。恢复冲突显示当前已保存文本，只有用户明确确认保留自己的文本后才更新比较基线；普通继续输入不会静默解除冲突。
- 文本草稿复用原子日志存储底层，独立放在 `.cue-drafts/`，按会话哈希命名，并配合同步 localStorage 记录、串行 CAS、写入结果未知时读回核对。读取损坏或失败不清除原字节；重新进入需明确恢复或放弃，未决草稿禁止生成、导出和替换素材。新保存入口和两个文本日志入口均受页面租约保护；多窗口专项现在覆盖 11 个业务写入/取消入口与 4 个日志入口。
- 保存与放弃均接入异步离开守卫，失败留在页面并保留可重试草稿。项目保存成功但确认丢失时，重新读取已确认项目视图但不抹草稿；放弃回到当前项目内容而非过期原文。丢失确认后即使输入改回最初文本，也不会误判为无修改。保存期间禁止继续编辑，避免旧清理覆盖新输入；配置草稿与文本草稿使用独立守卫。“保存并重合成”先完成可靠文本保存，再进入原合成管线，生成失败不丢已保存文案。
- 新增 Hook 专项 10 项覆盖独立行/空文本、同步日志、部分保存、零 TTS、丢失确认、主日志单独恢复、损坏数据、清理失败、快速连续输入、读回重试、旧租约、localStorage 配额、回退原文和显式冲突确认；后端专项扩展批次冲突不改内存、rename 拒写回滚、重复提交、原字幕不变、无引擎配置保存恢复。最终 Jest **21 套件 215 项**、集中基线 **31 组**、配音 **229 项及语言流程**、typecheck 通过。
- 真实 Electron 新 `test:e2e:dubbing-cue-drafts` 最终证据 **`smartsub-cue-drafts-e2e-WXWCeZ`**：100 行切换/折叠/虚拟卸载/筛选保留输入，实际 chmod 拒写及保存并离开失败，重试整批保存；失败后立即整应用 SIGKILL、成功但确认丢失后立即 SIGKILL，两者重开均有明确恢复选择。人工变更磁盘项目后恢复旧草稿被冲突拦截，明确确认后保存成功。文本单独保存无 TTS，保存并重合成仅一次本机 HTTP WAV 请求，随后改文案只标旧音频过期。1024×700/1440×900 无横向溢出，截图已检查；首次 E2E 因开发服务翻译缓存未更新显示键名而失败，重启后完整重跑，不作通过证据。最终后端专项 **`smartsub-speaker-settings-niWOwo`** 验证无引擎配置文本会话保留修复，typecheck/i18n/diff 通过。
- 回归：配置恢复 **`smartsub-dubbing-config-e2e-xvqpUw`**，多窗口 **`smartsub-dubbing-multiwindow-e2e-UvXDVD`**，自动对齐 **`smartsub-auto-fit-e2e-O73BVO`**，任务失败后修复成片 **`smartsub-dub-recovery-e2e-DfIHSG`**。使用隔离用户目录和确定性本机服务，不代表商业 TTS 或真人听感验收。进程崩溃恢复仍不等于整机断电事务保证；输入未送达主进程且浏览器未刷盘的极短窗口、两个存储同时失败仍有既述限制。项目删除/重建日志回收、流水线共同所有权、在途合成幂等重连仍待完成。第二阶段没有提前提交推送。

### 配音流水线共享所有权与事务删除

- 工作台页面租约与流水线现共用 `dubbingSessionOwnership`；流水线在恢复、重建、合成、音轨及导出期间持有独占预约，未确认的配置/逐句日志（含损坏记录）会阻止启动。正在流水线中运行的会话不能被编辑器抢占或删除，等待编辑器在释放后自动载入最新结果。工作台放行配音门禁时先验证所有目标会话及草稿，再交还自己的租约；派发失败恢复租约，成功退役旧租约。此处尚未声称覆盖门禁响应丢失后的全部交互。
- `workItemStore` 的删除及清空入口统一经过事务钩子，覆盖普通工作项和旧 `deleteTaskProject` 调用路径。仍有其他工作项引用的会话保留；实际队列/执行中的任务或被编辑器/流水线占用的会话拒绝删除，不以陈旧的 running 文案判断。会话目录和两种恢复日志先同盘移动至 `.deleted/<uuid>/`，清单 fsync 后才移动数据，任务列表持久化失败则逐项原位回滚；成功才清缓存及暂存。重建也在新任务关联持久化后才提交旧会话删除。
- 启动按持久化工作项引用恢复事务：仍被引用的会话还原，已删除的任务仅完成清理。回滚/恢复冲突保留事务并阻止会话读写；恢复根目录拒读或清单损坏无法确定受影响会话时阻止配音会话访问，但不终止应用初始化。存在性探测仅将 ENOENT 当作缺失，权限错误不等于数据已删除；未完成清单写入且尚未移动数据时清理本次空事务。会话标识拒绝目录跳转路径。
- 删除提交后广播清理对应 localStorage 配置/文本草稿；启动再次核对实际会话缺失状态，解决进程在任务提交后崩溃、尚未广播的残留。权限失败及恢复未完成不会清浏览器副本。界面异步删除失败保留确认弹窗、常驻错误及可折叠详情，重试不需要重新选任务；说明文案明确内部音频/编辑/草稿会删除，原素材和已导出文件保留。
- 真实 Electron 删除专项 **`smartsub-dubbing-deletion-e2e-sLMw0y`**：实际 chmod 使任务存储及中途日志移动失败，检查原音频、任务和两种日志逐字节还原；共享引用、旧删除入口、清空全部的整批回滚；真实 SIGKILL 分别发生在任务提交前、提交后清理前，重启不会丢恢复数据或复活已删除任务。正常删除检查两层草稿清理；强杀前刚写入的 localStorage 不保证已刷盘，崩溃保护以主进程恢复日志为准。1024×700/1440×900 删除错误弹窗无溢出，截图已查看。测试只操作隔离测试目录。
- 流水线专项 **`smartsub-gates-e2e-rygRCt`** 实际从工作台放行门禁并生成可解码成片；持有真实 HTTP 请求期间验证编辑器等待、删除被拒，完成后自动载入。多窗口专项 **`smartsub-dubbing-multiwindow-e2e-2ZbYN2`** 验证旧租约拒写、双窗口/渲染进程崩溃、后台批量/单句/导出重连和重建任务身份。配置恢复回归 **`smartsub-dubbing-config-e2e-GW6Op5`**；两项新增浏览器清理测试通过。

### 配音耗时操作的丢响应重连

- 代码复查发现：合成/导出已经成功但 IPC 确认丢失时，界面可错误显示失败并允许再次执行。批量合成、单句重合成、导出现携唯一请求标识；主进程以会话、请求标识和参数摘要去重，重复在途请求共享结果，参数冲突拒绝，已完成返回独立快照。每会话保留近期 32 份结果，淘汰后保留标识防止旧请求重新执行。
- 渲染层定期查询原请求；确认拒绝或永久不返回时不会重新派发 TTS/导出。状态读取失败保留当前操作的忙状态和错误，状态查询永久不返回时只有一个未决查询，并继续接收原操作结果；页面离开后停止更新旧页面。完成事件不再提前解除仍待确认的当前操作，避免返回前的重复操作窗口。完成快照通知失败也不把已提交成功结果改成业务失败。
- 新增 6 项专项覆盖在途重复/参数冲突、不可变重放、结果淘汰、跨会话隔离、确认拒绝/悬挂、查询拒绝/悬挂及页面离开。最新 Jest **23 套件 223 项**，前后端 typecheck、**31 组集中基线**、配音 **229 项及语言流程**通过。
- 实际 Electron 多窗口专项最终 **`smartsub-dubbing-multiwindow-e2e-2ZbYN2`**：单句 HTTP 尚未返回时故意拒绝 IPC 确认，界面保持禁用、重复请求合并为一次 TTS、冲突被拒；批量确认永久悬挂，通过查询得到最终行状态；真实 WAV 发布后丢确认，查询返回原路径，重复请求不产生第三份文件，输出可被 FFmpeg 解码。第一次扩展脚本因变量重名未启动即退出，修正测试后完整通过，不算产品失败。自动对齐回归 **`smartsub-auto-fit-e2e-77Ct7V`**，逐句草稿 **`smartsub-cue-drafts-e2e-RmBk3w`**通过。
- 最终恢复路径改用仅 ENOENT 表示缺失的 `lstat` 探测，补测暂存项拒读时保留事务、不误清数据；所有权/删除单元专项 **`smartsub-session-delete-BgOals`**，删除端到端重跑 **`smartsub-dubbing-deletion-e2e-GB6KY4`**通过。失败任务工作台修复成片回归 **`smartsub-dub-recovery-e2e-8ABsbA`**通过，最终 typecheck/i18n/diff 检查通过。丢响应恢复后的实际导出结果截图已查看。
- 边界：该请求结果登记目前在主进程内存，不保证主进程 SIGKILL 后恢复原请求标识或导出确认；主进程崩溃时在途合成/导出临时产物清理、新建重建会话在关联提交前崩溃的孤立目录仍待处理。删除恢复已验证进程强杀，不等于整机断电的跨文件事务保证；目录符号链接等整体路径策略及登记结果的长期内存占用仍需最终审查。第二阶段尚未执行生产构建或第二次提交推送。

### 配音请求收据持久化与主进程中断恢复

- 上节内存去重的跨进程缺口已有增量修复：`DubbingOperationStore` 在会话内 `.operations/` 保存按请求标识 SHA-256 命名的收据，执行前先以 wx/0600 临时文件写入、fsync、原子 rename 提交 pending，提交失败不调用 TTS/导出。完成结果独立快照后再提交 complete；会话事务删除/重建自动包含收据，不另留外部孤立记录。每会话近期 32 份结果及淘汰标记延续原去重语义。
- 重启后的 pending 明确为 interrupted，不自动重发或猜测成功，旧请求标识重放被拒；用户检查保存结果后可明确发起新请求。complete 可恢复原导出路径，重放不再合成或导出；回开独立配音任务时，以存在的已确认导出恢复任务完成状态和产物列表。损坏/错身份/不合法结果收据保留原字节并拒绝载入，不能当作从未执行。完成收据拒写不把真实操作改成失败，结果仍在内存可查询，但附持久化警告；恢复权限后查询/回开会重试落盘。中断警告和恢复导出横幅已有 Hook 测试。
- 启动阶段增加内部音频回收，在删除事务恢复后、任何新合成前执行。只读取合法且可读会话元数据，保留其引用的 WAV、工作项产物及流水线音轨/顺延字幕；仅删除已知 `cue-<index>-<timestamp>-<id>` 尝试音频和无人引用的 `dub-track-<random>` 目录。未知文件、符号链接、损坏元数据会话不清理。真实 SIGKILL 发生在新 WAV 已生成而 session.json 尚未 rename 时，重启保留前次已提交音频，并清掉这次实际生成但未提交的 WAV；不只依赖人工种子文件验证。
- 新 `test:dubbing-operations` 纳入集中基线，最新 **`smartsub-operation-store-rC2nAO`** 覆盖接受/完成收据、跨实例恢复、禁止重放、初始/完成拒写、权限恢复补写、损坏字节、事务删除回滚及引用保留。前端最新 **23 套件 227 项**、集中基线 **32 组**、配音 **229 项及语言流程**通过。
- 新真实 Electron 专项 `test:e2e:dubbing-operations` 最终 **`smartsub-operation-recovery-e2e-o0WCyo`**：实际 chmod 初始收据拒写时零 HTTP 请求；真实请求中整应用 SIGKILL，重启旧音频逐字节相同且旧标识不重发；成功单句的最终收据 chmod 拒写，界面仍显示完成音频与常驻警告，修复后补写；真实新音频提交前 SIGKILL 清理；导出 complete 收据落盘后、IPC 返回前 SIGKILL，重启恢复同一 WAV/字幕路径、任务状态及产物，重放没有额外文件。实际输出 FFmpeg 解码通过，源字幕不变；损坏收据阻止载入，修复原字节后可恢复。1024×700/1440×900 中断横幅无页面溢出，截图已查看。首轮测试缺必填背景音模式被正常校验拒绝，修正测试配置后才计为通过。
- 回归证据：多窗口/进程内丢确认 **`smartsub-dubbing-multiwindow-e2e-wGsC1M`**；事务删除 **`smartsub-dubbing-deletion-e2e-80cpJh`**；失败任务修复成片 **`smartsub-dub-recovery-e2e-BztDL4`**。测试只操作隔离配置及合成素材，已完成的测试进程全部退出；开发服务重启以载入新增中英文文案。
- 仍有边界：最终输出已发布但完成收据尚未提交的极短窗口，目前按 interrupted 处理，不自动重复导出，也未声称精确找回该窗口的输出身份；输出目录下 `.smartsub-compose-*`、无任务关联的新建/重建孤立会话的强杀清理仍待实现。未读/坏收据目前需修复权限或原文件，尚无应用内收据修复面板；大量长期请求的标记及历史结果内存/磁盘上限仍需最终压力验收。已验证进程强杀，不宣称整机断电跨文件事务。本轮未执行最终生产构建或第二次提交推送。

### 配音导出发布过程的中断恢复

- 上节“文件已发布、完成收据未提交”的窗口已有增量修复。工作台导出将唯一私有目录、待发布音频/字幕组和文件身份写入原请求收据；硬链接发布前记录源 inode，复制回退在独占创建及复制完成时分别记录身份。重启只检查收据明确登记的路径，不扫描任意输出目录。完整文件组先补写成功收据，再清暂存；不完整组只回滚仍属该次发布的文件。终态成功清理不回滚用户后来修改的导出，目录身份变化或不可读时保留证据。
- 已恢复的成功收据保留原输出路径，同请求重放不再导出；暂存清理完成后移除发布日志，后续打开不依赖旧输出磁盘持续挂载。复制中断允许清理 inode 相同但长度仍增长的未完成文件，完整文件和硬链接则同时核对长度、修改时间，避免删除已替换/编辑的输出。发布日志写入失败回滚当前文件组，不返回虚假成功。
- 真实 Electron 最新证据 **`smartsub-operation-recovery-e2e-BWkI8f`**：实际私有渲染、首个硬链接、最终硬链接、写入 512 字节的独占复制、完整复制登记后分别 SIGKILL；前两类未完成发布及部分复制回滚，最终链接和完整复制找回成对输出。原请求重放零重复文件、零额外 TTS，输出 FFmpeg 解码通过。保留此前收据拒写/损坏/确认丢失验证，总 TTS 请求仍为 5 次。1024×700 中断横幅截图本轮已查看，无页面溢出。
- 回归通过：**32 组集中基线**（发布专项 `smartsub-publication-recovery-kZmYNR`）、**23 套件 227 项 Jest**、**229 项配音及语言流程**、typecheck；流水线门禁 **`smartsub-gates-e2e-t0cTgH`**、工作台修复成片 **`smartsub-dub-recovery-e2e-6NwR7Q`**、普通合成 **`smartsub-compose-output-e2e-zZVDJ3`**、多窗口 **`smartsub-dubbing-multiwindow-e2e-B3svwD`** 均通过。
- 边界：这里的持久化发布日志仅接入有请求收据的配音工作台导出，普通合成和流水线并未自动获得同等崩溃恢复。独占创建复制目标到记录 inode 之间、私有目录分配到首次登记之间仍有极短窗口，无法确定归属时保留空文件/目录，不猜测删除。尚未使用真实不支持硬链接的磁盘测试；不宣称整机断电事务保证。第二阶段仍未最终生产构建或提交推送。

### 配音新建会话关联与门禁确认恢复

- 新建会话先持久化 `pendingTaskLink` 标记；启动在恢复删除事务后，根据持久化工作项引用确认已关联会话。只有明确标记、没有配置/文本编辑/角色设置/两种草稿/音频/未知文件的未关联创建，才进入已有事务删除。旧版本无标记会话、损坏或不可读数据、符号链接及未知文件均保留；不根据“没有任务”就推断可以删除。创建目录后首次元数据提交前的空目录仍无法确认归属，不作广泛目录清理。
- 代码审查发现流水线会话引用此前使用 800ms 延迟写入，合成请求可能先于关联落盘。`taskFileChange` 改为仅在 `dubbingSessionId` 变化时立即持久化，普通进度仍批量写入；失败回到旧引用，删除本次尚未使用的新会话并阻止合成。单元验证真实存储失败回滚、已关联标记确认、未关联空会话回收、旧数据/草稿/未知文件/符号链接保留及权限恢复后重试；最新会话存储证据 **`smartsub-operation-store-bfyMgm`**。
- 扩展真实 Electron 删除专项 **`smartsub-dubbing-deletion-e2e-9cIBm2`**：普通导入和重建各在新元数据落盘后、任务关联提交后而 IPC 确认前 SIGKILL。关联前新会话回收、旧项目和音频/日志保留；关联后新会话保留、旧会话删除事务按已提交引用恢复。原有 chmod 拒写、共享引用、整批回滚及删除提交前后强杀场景均通过。首版测试尝试替换 fs.renameSync 拦截任务存储，但第三方库已缓存函数，未触发强杀；停止该测试进程后改在真实处理函数完成、确认返回前注入，重新完整运行，不将等待计为通过。
- 任务页和配音检查员放行现使用同一确认恢复逻辑：提交前固定本次目标文件集合，正常响应仍按真实成功/失败处理；确认拒绝或一直悬挂时查询这些文件是否已 `passed`，不重复提交，也不放行随后到闸的其他文件。一次只保留一个查询，原响应仍可完成操作，切换项目/会话或卸载后停止旧回调。任务页明确重试沿用原目标集合。错误常驻横幅带可折叠详情及重试按钮，未确认时拒绝重复提交；状态读取失败不推断已放行。
- 新 **7 项**门禁确认测试覆盖拒绝/悬挂、原集合隔离、业务失败、查询拒绝/悬挂、原确认迟到、页面失效、已放行重入及重试不扩大目标。最新 **24 套件 234 项 Jest**、**32 组集中基线**、**229 项配音及语言流程**、前后端 typecheck、i18n 和 diff 检查通过。真实门禁专项 **`smartsub-gates-e2e-nhwcXR`** 注入任务页确认拒绝、配音页确认永久悬挂，均继续生成可解码成片，没有额外 TTS；同时在实际 HTTP 合成请求尚未返回时直接读取磁盘，证明新会话关联已经持久化。1024×700/1440×900 错误横幅、可折叠详情和重试按钮均可见、无页面横向溢出，最终 1024 截图已查看。失败任务修复成片回归 **`smartsub-dub-recovery-e2e-zOAIIs`** 通过。
- 边界：门禁使用持久化 passed 状态核对，不表示进程崩溃后自动重启整个流水线；重启的中断任务仍需明确重试。已验证进程强杀而非整机断电。未关联但包含工作数据的历史会话保留，不实现自动重挂到任务。第二阶段仍未最终生产构建或提交推送。

### 下载完成后的自动流水线与官方字幕承接

- 第 3.5 节新增下载面板自动流水线开关和已保存媒体配方选择。创建批次时由主进程合并完整配置并固定快照，包含门禁、翻译、配音、合成样式及画质，不在下载完成时读取后来编辑的配方。主进程先返回配置摘要供云端授权，实际启动重新核对摘要，配置变更拒绝旧确认；本地任务不再误记为已同意云上传。下载链接、开关和配方选择可在离开配置页面后恢复；这层 localStorage 草稿尚不等于配音工作台的主进程崩溃日志。
- yt-dlp 使用 `--write-subs --sub-langs all,-live_chat --convert-subs srt` 批量获取人工字幕，不启用自动字幕；`--ignore-config` 防止外部 yt-dlp 配置改变本次语义。产物从 `after_move` 的 `requested_subtitles` 结构化 JSON 认领，只接受本次报告、与视频同目录同主干的字幕，不扫描并认领旧同名字幕。按配方源语言或已知媒体语言选择精确/唯一基础语言；多语言歧义不随意选一个。有效官方字幕作为 `providedSubtitlePath` 进入原流水线，跳过 ASR 音频抽取及模型预热；缺少匹配字幕时仍需实际可用的 ASR 与相应云端授权。
- 下载完成条目与视频/字幕产物统一持久化后，逐条用稳定项目 ID、请求 ID 和原始输入摘要交接。先保存 pending 原请求，再通过已有可靠任务提交入口接受；关联失败单独显示流水线错误，不把成功下载改成失败或自动换引擎重复下载。重试固定原配方/文件列表；子任务已接受但父任务确认未保存时核对持久化收据，不再入队。已提交子任务后来被删除不自动复活；显式依赖失败不在重启时盲目重试。
- 每次交接/重试从实际存储重新核对选中的 ASR 模型和运行库、云服务及模型、翻译/精修服务、角色分离、TTS 模型/音色/语言/克隆参考音频和合成参数，并重新解析官方字幕。界面区分下载成功与交接失败，提供常驻错误、修复建议、可折叠详情、独立重试和打开关联任务。补齐下载页 `home` 翻译命名空间、修复新开关的嵌套 label。终态迟到取消不再把完成条目改回失败；删除任务后仍可停止其在途下载，晚到完成不重建已删除任务，结束后释放运行时及节流记录。
- 新 `test:download-pipeline` 纳入集中基线：**18 个交接场景**覆盖语言消歧、快照、所有本地 ASR 家族/云模型、翻译/精修、配音依赖、磁盘拒写、接受后丢确认、启动恢复、冲突与删除不重放；另有适配器/调度测试覆盖分块 stdout、旧字幕排除、关闭字幕、完成与产物原子保存、磁盘失败不回退、实际网络失败回退、取消及删除竞态。原下载解析 **126 项**通过；任务提交专项验证有官方字幕不预热 ASR，无字幕仍预热。集中基线 **34 组**、Jest **24 套件 234 项**、配音 **229 项及语言流程**通过；最后取消路径补丁后重新通过下载专项、前后端 typecheck、i18n/diff。
- 新真实 Electron 专项 `test:e2e:download-pipeline` 最终证据 **`smartsub-download-pipeline-e2e-Rs9FU0`**：真实 yt-dlp **2026.08.19** 通过本机 HTTP 下载两段用户素材及英/中文 VTT，实际 FFmpeg 转为 SRT；即使所选 ASR 模型不存在，英文官方字幕仍完成翻译、配音、硬字幕视频导出，成片通过 FFmpeg 视频/音轨解码且没有 ASR 请求。真实下载尚未结束时移除 TTS 服务并修改配方，完成后出现独立错误；恢复服务、点击重试后沿用原目标语言，媒体 hash 与 HTTP 下载次数不变，重复交接只有一份收据。无官方字幕任务通过真实授权弹窗后才请求本机 ASR 端点；下载链接离页恢复、1024×700/1440×900 无横向溢出均通过，完成及错误截图已查看。实际应用关闭后对已接受子任务的父确认状态注入 pending，再重启，恢复关联而不重复下载/合成。该重启场景是明确的磁盘状态夹具，不冒称下载边界的真实 SIGKILL 注入。
- 限制：真实网络下载器与 FFmpeg 已验证，但媒体/翻译/ASR/TTS 服务端是确定性本机夹具；本轮没有验证 YouTube/B站当前在线鉴权、限流或真实商业语音质量。尚无专门内置下载配方，使用向导保存的媒体配方；不支持在既有批次中改配方后重新解释原请求。下载器已完成但最终状态持久化失败时保留文件并显示错误，不声称此窗口重启后绝不会再次调用下载器。官方字幕下载失败时 yt-dlp 的整条失败策略、超大播放列表、断电与所有丢响应路径仍需整体审查。第二阶段未进行最终生产构建或第二次提交推送。

### 第 4 节键盘流与五小时万行工程首轮验收

- 字幕行增加 Cmd/Ctrl+Enter：结束当前输入的撤销分组，前往下一条可见字幕并保留原文/译文焦点；遵守失败过滤，修复后被临时保留的当前行离开后再移除；末行保持焦点，不隐式把整个文件标为完成。Tab/Shift+Tab 保留行内双语切换。输入法组合、额外修饰键与按键重复不触发行跳转。字幕文本框增加明确可访问名称；播放进度不再在编辑期间切换当前行、卸载输入框，行导航同步更新播放时间。
- 全局 `useHotkeys` 默认隔离打开的 dialog/alertdialog/menu/listbox，尊重已消费事件、输入法及原生控件的空格/方向键行为；修饰键命令仍可在按钮聚焦时使用。默认忽略重复按键，上下条导航显式允许；精确区分 Mac Cmd 与其他平台 Ctrl。字幕撤销仅接管字幕文本输入框，搜索、时间及角色输入保持本地文本撤销。校对及合成 Cmd+B 在输入框中可收折面板。播放器原生播放/暂停/结束同步 React 状态，修复先点击原生播放后第一次按空格不能暂停的问题。
- 移除字幕命令历史静默截断 200 条的限制，保留文档打开期间全部命令；追加命令仅裁掉 redo 分支，不再复制整个历史栈。测试先复现 150000 行批量撤销的 `Maximum call stack size exceeded`，再将变参数 splice 改为数组区间 concat；验证 1000 次完整撤销/重做、分支、重置、角色名册与字幕结构原子回放。这里不表示撤销历史已经跨进程持久化，也未验证无限历史的长期内存增长。
- 去除滚动/播放刷新时重复统计字幕及失败索引、没有 AI 建议时的全表结构序列化、没有漏识别警告时的全表关联；失败+角色过滤改为 Set 查找，避免双层 includes。草稿采用每 128 行缓存序列化片段，仅重新序列化变化片段，但仍同步、单键原子写入完整旧格式 JSON，不引入保存延迟。期间试验过多键不可变块+索引，虽然写入更快，但跨窗口回收需要额外所有权机制；该实验已从产品代码撤回，23KB/0.2ms 的中间测量不作为最终实现性能证据。当前每次仍写约 3.93MB，是剩余优化点。
- 新 `test:e2e:proofread-keyboard` 使用 FFmpeg 实际生成 5 小时 8kHz FLAC 与 10000 行双语 SRT，在隔离 Electron 配置运行。最终独立复测 **`smartsub-keyboard-perf-e2e-cEngdJ`**：载入 **3067.9ms**，搜索并定位第 10000 行 **162.6ms**；32 次输入到下一 RAF 的中位数 **10.8ms**、P95 **14ms**、最大 **17.3ms**。1024×700 / 1440×900 分别最多挂载 **33 / 41 行**；180 帧每帧滚动 600px 的帧间隔中位数 **15.3 / 16.6ms**、P95 **21.9 / 18.5ms**、最大 **40.9 / 52.8ms**，超过 25ms 分别 **2 / 1 帧**。运行末 JS heap used 约 **146MB**（非强制 GC 后保留量）。这是开发模式、合成连续音频和拉丁文本；不是生产模式复杂影视素材或稳定 60 FPS 的完成证明。脚本验证功能与 DOM 上界，报告性能原值，不把宽松阈值当成 60 FPS 验收。
- 该专项还通过真实键盘播放/输入隔离、源/译文连续编辑、分组撤销重做、失败过滤跳转、搜索原生撤销、弹窗阻止后台保存、末行处理及 Cmd+B 重挂载；整应用实际 **SIGKILL 后重启**，用户选择恢复草稿，最后一行最新输入仍在，并可真实保存到 SRT。最初两次测试因选择器匹配波形与播放器两个 audio、误把搜索按钮当作跳转按钮中止，修正测试后完整重跑。renderer-only 强杀重载因 Playwright page 仍为 crashed 未通过，最终使用整应用强杀，不冒称该路径通过。1024/1440 截图已查看且无页面横向溢出；未执行 Windows/Linux 真机键盘测试。
- 最新回归：**29 套件 261 项 Jest**、**34 组集中基线**通过；最终草稿调整后再次通过 Jest、校对可靠保存专项及前后端 typecheck；i18n/diff 检查通过。真实波形 **`smartsub-waveform-e2e-IJV6fp`**、行内 AI **`smartsub-inline-ai-e2e-cViHYF`**、项目词库 **`smartsub-context-glossary-e2e-2BP1Ap`**、合成工程 **`smartsub-compose-document-e2e-6FXj7y`** 均通过全局快捷键回归。总目标未完成：持久错误/载入错误全覆盖、全页视觉层级、长工程生产模式与复杂文本/角色/AI 下性能，以及下节其他验收继续待办。尚未第二次提交推送。

### 校对加载防丢失、预览恢复与最小窗口导航

- 代码审查发现 `readSubtitleFile` / `readProofreadDataFile` 的读取失败此前返回空数组，前端再吞一次错误；配置了校对中间态却读取失败时还会退回源 SRT，丢掉译文/角色语义。编辑器现启用严格读取，完整取得必需数据后才发布可编辑状态；中间态始终权威，包括合法空条目，不回退到交付字幕。载入失败常驻横幅显示修复建议、可折叠详情和重新加载；编辑、保存、完成入口不可用，键盘命令也不能绕过。加载中保留返回入口和可访问状态名，空文件不残留上一文档行。
- 严格模式先在原始数据上校验，再做既有规范化：SRT/VTT/ASS/LRC 的部分损坏块、非法时间戳/时段、缺失分隔符以及 sidecar 中非数值时间、非文本译文、坏角色结构均拒绝，不因“尚有一条有效字幕”而载入后覆盖。合法空文件、WebVTT 的 NOTE/STYLE/REGION、空文本定时条目、LRC 元数据仍接受；既有非严格调用保持宽容接口。新增 `test:proofread-load` 有 **60 项**检查并纳入集中基线。范围是所支持格式的可识别语法和核心 sidecar 字段，不宣称保留 ASS 全部样式/注释或任意损坏字节，也未扩大为通用无损字幕修复器。
- 双语载入现在先保留精确时间匹配，再进行不重复使用条目的位置配对；同时间戳多行不再被 Map 的最后一条译文覆盖。译文条目多于原文时阻止打开，提示选择匹配文件，不能保存后悄悄丢掉额外译文。仍保留历史的非精确时间位置回退，不宣称异步字幕语义对齐。真实重叠字幕还暴露播放进度反抢选中行，修为选中行仍覆盖播放时间时保持该行；不足 10ms 的字幕 seek 偏移限制在其时段内。
- Hook 按配置值稳定标识文档，修复新配置对象造成的重复载入；读取、保存与字幕预览分别核对当前文档/请求代次。旧文档迟到读写不能标记新文档已保存、删除其草稿或继续写旧译文输出；脏文档拒绝重新载入。单测覆盖 StrictMode、空文档/缺配置、畸形 IPC、文件切换、卸载、迟到拒绝、保存期间新保存去重及预览响应乱序。草稿 layout effect 先核对文档身份，避免文件切换瞬间把旧内容写到新草稿键。
- 播放器预览改为独立异步载入，失败不阻止字幕编辑，常驻详情和独立重试不替换脏文本；替换、卸载和迟到结果均释放 Blob URL。真实测试发现 ReactPlayer v2 仅按媒体 URL 缓存 file config，简单移除 key 重挂载会使迟到轨道根本不显示。现通过原生 media 元素管理 track 生命周期，保留播放器实例和播放头，加载后校正默认轨道，避免浏览器自动选择造成原文/译文同时显示。保存失败横幅补独立“重试保存”按钮。轨道来自当前磁盘交付字幕，本次没有实现每次逐字编辑后即时重建预览。
- 小屏截图发现侧栏设置按钮曾被底栏遮住，`Layout` 在窗口高度不超过 760px 时把导航项收紧至 40px，明确禁止挤压，并为更矮窗口提供侧栏纵向滚动回退。新真实界面断言在 **1024×700 / 1440×900** 逐一核对所有导航链接边界及中心点命中，确保设置等入口不被遮挡；载入错误长路径可换行，无页面级横向溢出。
- 最终真实 Electron 专项 **`smartsub-proofread-load-e2e-IpYVgP`**：四种格式的实际 IPC、目标文件 rename、源文件 chmod000、非字幕与部分损坏内容、坏 JSON/合法 JSON 中坏字段、额外译文及重复时间戳真实保存均通过；注入可选 VTT 失败与保存失败后点击独立重试，字幕实际写入源文件及 sidecar。实际 FFmpeg 生成有音轨 MP4；重试后原生默认 track `readyState=2`、cue 文本正确、只有译文 showing，播放器对象和 1 秒播放头保持不变。加载错误、预览失败/恢复、保存失败截图已查看。初轮脚本经历折叠行精确文本选择器、批次未保存守卫和只读 preload IPC 无效注入问题，修正后完整重跑；重叠行抢选和 ReactPlayer 缓存问题则是真实产品缺陷，已修复后再验收。
- 回归通过：**35 组集中基线**、最终 **31 套件 286 项 Jest**、字幕输出 **368 项及三类任务格式 UI**、前后端 typecheck、i18n/diff。真实校对守卫/保存 **`smartsub-proofread-e2e-1DYjtm`**、波形 **`smartsub-waveform-e2e-nBqeGf`**、行内 AI **`smartsub-inline-ai-e2e-iIZOig`**、项目词库 **`smartsub-context-glossary-e2e-vZuOlo`** 通过。集中基线运行早于最后的小范围重叠行/轨道修改，之后重新通过完整 Jest、类型检查、可靠保存单测及最终加载专项，不将早先结果冒称最终全量复测。
- 五小时万行复测 **`smartsub-keyboard-perf-e2e-z9ezGp`**：载入 **3056.5ms**、搜索末行 **164.8ms**，输入中位 **10.6ms / P95 15.3ms / 最大 15.4ms**，草稿写入最大 **5.7ms / 3,925,028 bytes**；1024/1440 最多挂载 **33/41 行**，滚动中位 **16.0/16.7ms**、P95 **22.2/18.2ms**、最大 **32.5/41.4ms**。运行末 JS heap used **94,421,212 bytes**，非强制 GC 后保留量。真实整应用 SIGKILL 恢复最新末行输入并保存通过。仍是开发模式合成音频/拉丁文本，P3-03 不勾选；该性能复测后只调整了轨道加载完成时默认显示状态，未据此宣称性能新提升。
- 下一轮需补校对批次深链接读取失败的常驻错误、取消/切换项目后批次子读取的身份校验；全页错误与视觉层级、复杂工程生产性能、在线站点和真人配音质量及下述验收仍未完成。开发服务重启用于加载文案，保持 8888；本轮未生产构建或第二次提交推送。

### 校对批次加载、导入配对与首屏初始化

- 批次页现在按路由目标建立独立工作区，等待 router 就绪并校验任务响应、项目身份、条目结构和重复 ID；深链接中的文件去重，`workItem` 与 `file` 冲突明确阻断。所有子读取完成后才发布文件列表，不在载入失败时留下可保存的半批次。加载与失败均保留返回入口，常驻错误包含详情和重试。主进程目录检测/扫描增加可选严格模式，真实 EACCES 向上传递，不吞成空字幕集合；保存的文件语向优先于后来修改的全局配置，`in_progress` 不再降为待校对。
- 路由切换、重置与新导入会使旧加载、保存确认和撤销回调失效。旧项目已接受的磁盘保存不撤销，但迟到响应不能把新项目标为已保存，旧 Promise 的 finally 不能解锁新保存；项目词库的自动建项也核对同一工作区代次。新建项目的输入仍由现有离开守卫保护，此项不等于新增批次级跨进程持久草稿。
- 视频/字幕/文件夹导入、追加和手动选择使用单次在途动作，读取失败不部分提交，保留原选择供显式重试，不重新打开文件选择器；取消选择安静结束，卸载后的结果不能提交。手动选择按文件 ID 定位而非旧索引，删除前行不把结果写到错误条目，已删除目标不复活。错误改为常驻横幅、修复建议、可折叠详情及重试。
- 真实测试发现字幕导入把同目录无关的 30% 候选自动选为译文，代码审查还发现多文件追加只保留一对字幕、文件夹导入漏掉单个非英语字幕和同组第三种语言。字幕导入改用既有后端文件名分组，并保持用户明确选择的原文，不依靠英语启发式反转用户选择；无关文件仍可手选但不自动绑定。视频不自动选择仅同目录的 30% 候选。追加与首次导入一致，为每个唯一选择建立条目；文件夹保留未被配对消耗的字幕为独立条目。批次名不再第二次去扩展名而截掉 `import.fr` 中的语言后缀。
- 原文与译文下拉候选相互排除，手动上传同路径明确报错；编辑器载入时再次拒绝源路径与译文/最终译文路径相同，阻止旧项目绕过 UI 后保存覆盖。已有校对中间态的条目更换字幕前需要确认，取消保留原记录，确认才解除 sidecar、旧成片字幕路径及旧内容格式的关联，并回到待校对。原 sidecar 和字幕文件不删除，替换后真实加载新选择。该保护按配置路径比较，不宣称覆盖符号链接、硬链接或大小写别名；跨项目共享源文件并发编辑仍需整体审查。
- 冷启动测试暴露初始化时序问题：主进程原先等待 `loadURL` 完成才注册大部分 IPC，首屏可能先请求不存在的接口；本轮脚本过早替换 handler 也会与后续注册冲突。现先注册主窗口全部服务与工作项恢复，再载入页面；合成事件广播显式使用预定 renderer URL 保持原有同源/同 session 边界。首屏增加接口注册断言，健康状态、合成多窗口写保护及同路径作业重连均通过真实回归。并非通过跳过首次引导或刷新页面掩盖失败。
- 新批次页、导入动作和配对测试分别覆盖异常 IPC、StrictMode、稳定路由、迟到子读取/保存、重置撤销、选择重试、取消/卸载、文件 ID 隔离、替换确认与所有输入保留。最新完整 **34 套件 321 项 Jest**、**35 组集中基线**、前后端 typecheck、i18n 和 diff 检查通过。集中基线已在本轮最后产品修改后重新执行，包含真实 FFmpeg 输出与恢复专项。
- 最终真实 Electron `test:e2e:proofread-batch-load` 证据 **`smartsub-batch-load-e2e-cAZAgm`**：项目缺失/失败/错身份响应、三种扫描实际 chmod000 拒读及恢复、挂起 A 子读取后进入 B、已接受 B 保存的确认迟到 A、缺文件链接恢复、冲突参数、初次导入/追加/手动选择均通过。重试保持选择器调用次数不变；多选追加保留两个不同文件，文件夹保留单个法语及第三种语言；替换取消仍读旧 sidecar，确认后源/译文文本来自新文件，旧记录字节不变。1024×700 / 1440×900 错误页无页面溢出，缺项目、替换确认和完成编辑截图已查看。使用隔离配置和合成字幕，原生文件选择器由固定路径替身提供，实际文件检查、扫描、配对、任务存储和编辑器均走真实 IPC。初轮文件名截断导致断言失败、另一次首屏注册竞态均修复后完整重跑，不计为通过。
- 真实回归通过：批次保存 **`smartsub-batch-save-e2e-6ScAwy`**、严格加载 **`smartsub-proofread-load-e2e-EWUiMb`**、离开守卫 **`smartsub-proofread-e2e-2HXNw1`**、项目词库 **`smartsub-context-glossary-e2e-Mn7y2n`**、波形 **`smartsub-waveform-e2e-MrKnOK`**、行内 AI **`smartsub-inline-ai-e2e-ffVnJx`**、健康状态 **`smartsub-health-e2e-zgdU14`**、合成多窗口 **`smartsub-compose-multiwindow-e2e-GUh4bI`**、作业重连 **`smartsub-compose-reconnect-e2e-8qaOIe`**。其中保存/加载/词库回归早于最后仅涉及追加/文件夹的修补，最终新增批次专项与全量 Jest 在该修补后执行。
- 万行功能复测 **`smartsub-keyboard-perf-e2e-C7YA6U`**：真实五小时 FLAC/10000 行，加载 **4086.7ms**、搜索末行 **159.7ms**、32 次输入中位 **10.7ms / P95 16.8ms / 最大 30.7ms**，完整草稿最大写入 **6.2ms / 3,925,028 bytes**。1024/1440 最多 **33/41 行**，滚动中位 **18.1/16.5ms**、P95 **23.5/19.6ms**、最大 **43.3/33.3ms**；非强制 GC 后 JS heap used **124,880,668 bytes**。整应用 SIGKILL 恢复最后输入并真实保存通过。本次与集中基线并行，数据保留作受负载影响的开发模式观测，不作为独立性能对比或稳定 60 FPS 证明。P3-03 保持未勾选。
- 第二阶段仍未最终生产构建、提交或推送。下一步继续全页常驻错误/视觉层级与生产模式复杂工程验收，并完成其余模块的明确待办，不能以本轮校对专项通过替代第 3、4 节整体完成。

### 全页首屏巡检与设置保存可靠性

- 新增 `test:e2e:ergonomics`，以独立 Electron 配置遍历 23 个路由/工具视图，在 1024×700、1440×900 各硬导航一次并留截图。首轮发现校对和配音深链接在 SSR/客户端首次渲染时发生 24 条 hydration 错误，现等待组件挂载与路由就绪再创建工作区。修复后最终首屏证据 **`smartsub-ergonomics-e2e-uwPcfJ`**：46 个视图、零 pageerror、零检测到的页面横向溢出或无可滚动祖先的控件裁切。已查看引擎、配音和设置的代表截图；此脚本仅验证初始状态与几何启发式，不代表展开面板、全部弹窗、已加载项目或视觉层级规范整体完成。
- 设置页原先读取异常后仍可显示可编辑默认值；VAD 防抖在真实写入前清空待保存记录，代理/下载端点仅失焦保存且无离页保护，多数写入失败只给瞬时 Toast。新增 `useSettingsPersistence`：先严格读取，初次读取与导入后重新读取失败均阻止编辑；字段版本化、单次在途、后续修订串行发送，严格消费现有 `{ rejectedKeys }` 契约而非假设 `success:true`。只确认确实被接受的字段，失败/畸形确认仍保留输入供重试；后端部分拒绝时只保留未接受字段。脏状态或保存期间禁止重新载入，迟到/卸载结果不发布状态或继续后续写入。
- 开关、VAD、代理、下载端点、语言和目录选择共用保存状态及全局离开守卫；文字在尚未失焦时已登记未保存。放弃取消延时并恢复已确认快照，已在途成功写入不假装回滚，未发送的新修订不会随后偷偷保存。语言切换、端点缓存更新和存储新旧目录提示统一在保存确认后执行，通用重试同样生效。`CustomLanguageManager` 改为使用父页已加载列表，避免独立读取失败被当空列表后覆盖；失败新增仍保留在待保存列表，输入框清空，重试不触发重复语言校验。
- VAD 新增前后端共用有限数值/范围校验：阈值和重叠比率限定 0–1，其余时长与填充不得为负。清空数字框不再自动保存成 0，无效输入留在界面并标记 `aria-invalid`，不会写入磁盘或通过“保存并离开”；直接 IPC 绕过界面也返回拒绝字段。读取/保存/路径、目录打开、代理测试、配置导入导出错误均有常驻详情；弹窗内的错误留在对应弹窗中，打开新导入/导出弹窗清除旧操作错误。目录打开明确校验 `{ success:false }`，不只捕获异常。
- 真实端到端暴露全局守卫的另一处交互问题：代理框打开离页弹窗时失焦并自动保存，保存成功会令活动 dirty guard 消失，导致“保存并离开”按钮从仍打开的弹窗突然消失。现固定弹窗打开时是否提供保存命令；按下时重新读取当前 dirty 状态，若已保存则直接离开。校对现有前进后退、命令面板、窗口关闭/刷新、多个 dirty guard 和真实磁盘保存回归 **`smartsub-proofread-e2e-dvtSEf`** 全部通过。
- 新设置专项 **35 项 Jest** 覆盖畸形/失败加载与重试、StrictMode/迟到/卸载、失败及部分拒绝保存、精确字段修订、单次在途、防抖合并和即时离页保存、放弃期间在途成功/失败、回调隔离及 VAD 边界。最终完整 **35 套件 356 项 Jest**、**35 组集中基线**、前后端 typecheck、i18n 与 diff 检查通过。最后仅把目录打开错误移入存储弹窗后，重新通过设置端到端、类型/i18n/diff；不把稍早的集中基线和全量 Jest 称为此末次 UI 小改后的复测。
- 最终 `test:e2e:settings` 证据 **`smartsub-settings-e2e-V1dwpO`**：真实配置目录 chmod 拒写后开关、VAD、自定义语言、存储目录及语言变更均不显示虚假成功；修复权限后重试并核对实际配置字节，重载恢复。输入聚焦未失焦即导航、VAD 防抖前导航、失败保存留页及明确放弃通过。空/负数/超范围 VAD 不写盘，真实 IPC 拒绝旁路非法参数；语言失败保留中文页，重试后才到英文页；目录重试仍显示新旧位置。读取、代理、导出、打开目录与导入确认后的重新读取失败使用主进程故障替身，导入成功是模拟确认，不宣称完成实际加密文件导入验收；原生目录选择器亦为固定测试路径替身。独立配置均位于临时证据目录，测试权限已恢复，未修改用户日常配置。最终零 pageerror，1024/1440 保存失败及1024 导出/目录弹窗截图已查看，长错误文本可换行且操作可达。
- 范围限制：设置待保存队列是内存状态与离页保护，本轮不声称新增了配置崩溃恢复草稿或多窗口配置冲突解决。服务商配置审查已确认 `ProvidersTab`、`useAsrProviders`、`useTtsProviders` 仍有 fire-and-forget `send`/卸载 flush、翻译面板定时显示已保存及读取失败处理缺口，下一步必须修复并进行同等真实磁盘失败验收。现有多层卡片样式仍未完成第 4 节表面层级治理，生产模式复杂/万行性能及其余模块待办保持未完成。
- 开发服务重启后仍运行于 **http://localhost:8888**；最终只读核对远端 `feature/lxd/ui` 仍为 **`c6f15acade6cc49eb0f90c940df89b07f6b7f620`**。本轮未做生产构建或第二次提交推送，总目标保持进行中。

### 服务商配置可靠保存、并发保护与输入草稿

- 翻译、云 ASR、云 TTS 编辑器统一使用 `useProviderPersistence`，替换仅发送消息、卸载 flush 和定时显示已保存的路径。读取成功前不可编辑；数据结构异常、读取/初始化/迁移写入失败向上传递，不伪装为空列表。仅缺省 `undefined` 视为未配置，磁盘 `null`、坏条目和重复 ID 拒绝加载，不覆盖原记录。保存按修订串行发送，只有真实 `{ success:true }` 才确认；失败保留输入、常驻详情与重试，并接入全局离开守卫。
- 主进程新增确认式服务商写入契约 `{ providers, expectedProviders }`，同步比较并提交，拒绝另一窗口已修改的旧快照。磁盘已等于提交目标时允许幂等确认。前端保留未收到确认的原请求，重试先重放该请求，再发送后续输入，覆盖“写入成功但 IPC 确认丢失”的真实场景。冲突由用户明确确认放弃并重新读取，不自动合并凭据。保留旧 `send` 通道供现有集成兼容，旧通道没有确认或比较交换保证，不宣称所有外部写入者均受保护。
- 翻译默认服务商改为主进程仅更新 `userConfig.translateProvider` 并确认，不再先读后发送整个旧配置。列表首次保存失败后，默认选择保留为独立待保存操作，常驻错误可重试，离页时也会重试。删除成功后才显示撤销入口，撤销把删除项和回退引用合并到当前列表，不用删除前全量快照覆盖后来输入。重新载入时选中项已不存在则回到总览。
- 模型/音色标签尚未回车或失焦的文字上提为页面级内存草稿；切换子面板后仍保留，明确保存会以最新实例合并，自动保存其他字段不会提前提交正在搜索的标签。翻译重命名同样受保护，非法空名称阻止保存；删除/清除配置会明确清掉对应未提交输入。新增服务商弹窗关闭后保留名称和地址，离页必须留页或明确放弃，不在未提交弹窗上自动创建实例。初次创建不成功不会清空表单。以上不是进程崩溃后的配置草稿恢复。
- 翻译测试/结构化模式自动探测、ASR/TTS 连接测试及在线音色读取核对实例身份与请求代次；换实例、改配置和卸载后的迟到响应不会修改其他实例或复活删除项。测试失败与音色获取失败提供常驻详情及重试。云服务页头和状态点从仅凭字段完整就显示“可用”改为“已配置”，实际连通性仍由独立测试和健康状态显示。
- 新增存储校验、共享保存队列、未提交输入和面板异步生命周期测试。本轮最终 **38 套件 406 项 Jest**、**35 组集中基线**、前后端 typecheck、i18n 和 diff 检查均通过；集中基线在末次产品修改后重新执行。参数持久化回归 **`smartsub-parameter-e2e-uetIql`**、健康状态回归 **`smartsub-health-e2e-ZMYbh2`** 通过，后二者早于最后仅涉及空重命名/无效选中项的小改，未冒称该小改后的再次复测。
- 最终真实 Electron `test:e2e:provider-persistence` 证据 **`smartsub-provider-save-e2e-3HkZNC`**：三类服务商均覆盖读取失败恢复、实际配置目录 chmod500 拒写、失败保存阻止离页、修复后重试和重载、真实写入后注入丢失确认并继续编辑、第二个真实 BrowserWindow 写入产生冲突、明确放弃重载、防抖前即时离页、实际新增/删除拒写重试及磁盘 `null` 保留/修复。另验证未失焦标签、翻译默认选择/重命名和关闭新增弹窗后的草稿守卫。所有断言核对隔离配置实际字节，零 pageerror；未调用商业模型，未修改日常用户配置，测试目录权限已恢复。1024×700 冲突确认最终截图已查看，控件可达；1440×900 ASR 错误页在同代码前一轮 **`smartsub-provider-save-e2e-FWu0i6`** 已查看，长路径可换行，先前 1024 TTS 错误页亦已查看。曾因第二窗口使截图缩放选错目标、开发服务缓存旧语言资源而失败，分别固定窗口 ID 和重启服务后完整重跑；另禁用截图动画，避免把淡入中途透明帧当作最终界面。不计失败轮为通过。
- 最终首屏几何回归 **`smartsub-ergonomics-e2e-pNLnpK`**：23 路由 × 2 尺寸共 46 视图，零 pageerror、零检测到的页面横向溢出/无可滚动祖先控件裁切。仍是首屏巡检，不等于所有展开状态或视觉层级验收。服务商读取失败目前会阻止对应资源页主体（包括本地引擎/模型区域），错误隔离范围还可收窄。保存队列不提供崩溃草稿，明确放弃不撤销已经写入但确认丢失的磁盘操作；多窗口保护为列表级冲突拒绝而非自动合并。
- 下一步已确认的缺口：`useUnifiedTaskConfig` 仍有全量 `send('setUserConfig')` 及读取失败后标记已加载；本地引擎设置仍有仅 `await setSettings` 不核对拒绝字段、错误只 Toast、未提交命令缺守卫等路径。全局视觉层级、生产模式复杂工程性能及其余模块验收仍未完成。本轮没有最终生产构建或第二次提交推送；只读核对远端仍为 **`c6f15acade6cc49eb0f90c940df89b07f6b7f620`**。开发服务重启后继续运行于 **http://localhost:8888**。

### 仍待完成

第 3.3 节内嵌字体、标准目录已安装字体、同草稿多窗口写保护、同路径作业精确重连、字体样例释放、居中/零边距/越界避碰和滚动 Effect 已有实现及专项验证；未知附件扩展节和大字体库扫描/菜单已补充上述修复与验收，仍保留整层路径边界与复杂单字体/4K 性能限制。统一合成输出不再直接覆盖最终路径，但不支持硬链接的磁盘仍缺少真实设备验证。第 3.4 节角色阵容与语速/音高已有实现，其余差距见相应节；第 3.5 节下载流水线和原生字幕承接已具备上述实现及本机真实下载器验收，在线站点与恢复边界仍待补验。第 4 节全局人机工学与五小时/万行性能验收仍待完成。新增实现尚未执行最终生产构建，也未作第二次提交推送。总目标继续保持进行中。

### 配置读取与任务配置隔离修复（本轮续做，2026-09-21）

- 复核发现上一轮新增的全局任务配置确认写入没有实际业务调用方，却会让任务表单在读取失败、项目恢复和多个窗口编辑时存在覆盖全局偏好的风险。任务配置现统一视为任务/工程草稿：向导继续把配置写入草稿，任务页使用工程 `configSnapshot`；只有新建任务在没有快照时读取 `getUserConfig` 作为只读默认值。移除了无消费者的 `useFormConfig`、`useLocalFormConfig` 和全量 `setUserConfigAck` 路径，也移除了 provider 面板中无消费者的整份配置同步函数，保留旧 IPC 通道供既有集成。
- `useUnifiedTaskConfig` 现在区分 `autoLoad` 与项目快照加载，固定初始配置引用，所有表单快照深拷贝；默认配置读取严格校验对象形状。读取失败时保持 `loaded=false`，不发布空的可编辑默认值；错误详情常驻且可展开，修复建议和重试入口明确。项目快照 hydration 会使旧的默认读取响应失效，清除旧错误并阻止迟到失败覆盖已恢复工程；卸载后响应不再发布状态。向导在配置未加载成功前不显示可编辑表单或允许开始，任务页在工程/默认配置未完成前保持阻塞。
- 新增 `useUnifiedTaskConfig.test.tsx` **12 项**：畸形/失败读取、显式重试、快照优先级、迟到成功/失败、错误清理、项目模式不读取全局默认、内联初始参数不重载、嵌套快照不可变、StrictMode 首次请求、卸载响应隔离。真实 Electron `test:e2e:wizard` **`smartsub-wizard-e2e-Xdx7VQ`** 通过显式草稿恢复、完整流程配置、配置与配音工作台隔离、1024/1440 控件边界、存储失败常驻错误/导航守卫、修复后重试及重载放弃。`test:e2e:readiness` **`smartsub-readiness-e2e-GbrppE`** 通过配置依赖确认、真实磁盘失败留存草稿、延迟确认禁用重复开始、本机 ASR 夹具、丢确认重放不重复流水线和任务页确认。
- 本轮验证：全量 Jest **41 套件、432 项**通过；`test:pro-baseline` **35 组命令**通过；`npm run typecheck`、`npm run check:i18n`、`git diff --check` 通过；`npm run build` 生产构建通过。构建仍报告既有 browserslist、JASSUB runtime chunk 循环和大页面数据警告，没有编译错误。开发服务器缓存曾导致 500（缺少 Next vendor chunk），仅重启仓库自有 Nextron/Next 进程后，向导和就绪性 E2E 均通过。
- 边界：任务配置保存仍依赖向导/工程草稿的现有持久化机制，未声称新增跨进程配置草稿或多窗口自动合并；服务商列表和设置页的独立持久化机制不由本次任务配置 hook 代替。第 3.3 复杂 ASS/4K、在线站点和真人语音质量、生产模式复杂万行稳定 60 FPS、全页表面层级，以及其他路线图明确保留的范围限制仍未完成，不能据此勾选 P2-05、P2-06、P3-01、P3-02 或 P3-03。

> 更正：上方较早的历史段落记录了当时的中间状态（`useUnifiedTaskConfig` 曾有全量 `setUserConfig` 与失败后误标已加载问题）。该问题已由“配置读取与任务配置隔离修复（本轮续做，2026-09-21）”完成修复；最终源码中任务配置 hook 已无 `setUserConfigAck` 或 `setUserConfig` 写入路径。

### 当前交付状态（2026-09-21，覆盖上方历史待办描述）

第 3 节的主要功能链路已实现并通过下列本机生产版验证；第 4 节的表面层级、键盘与常驻错误已补齐，原阶段交付已提交推送 `b186ea8`。本节是当前状态索引，上方按时间保留的“未构建”“读取问题仍在”“本地/MiMo 待验”等叙述属于历史状态；本日后续角色/滚动优化、联网验收及三平台草稿强杀修复见末尾补充。**P3-03 稳定 60 FPS 仍未验收，不将整份路线图标为全部完成。**

| 需求                                    | 当前实现与证据                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 3.1 两段检查器、场景预设、输入即草稿    | 已有向导/就绪性/预设/首页下载恢复专项。本轮进一步将默认值读取与工程快照隔离；`useTaskDependencies` 原子发布模型、翻译/ASR 服务商与设置快照，任何读取失败均常驻详情、阻止编辑/纠正引擎选择，重试不重置任务配置。10 项依赖读取测试覆盖各通道拒绝/坏数据、过期响应和 StrictMode。生产版 `smartsub-task-config-e2e-8KnRx6` 验证两入口拒绝/畸形默认、所有四项依赖故障与重试、已有工程不读取坏的全局默认、全局偏好不被写回，零 pageerror。先前 wizard/readiness 专项没有注入 getUserConfig 故障，此独立专项才是该故障的 UI 证据。 |
| 3.2 波形、切分/合并、行内 AI、术语、CPS | 保留前文真实波形/词库/恢复证据；最新生产版 `smartsub-inline-ai-e2e-Q73mjl` 通过真实本机 HTTP、行内 Diff、采纳/忽略、并发修改保护、流式批次结果/取消、缩短及真实字幕落盘。Unicode 字素计数改为每行一次迭代，保持组合字符/emoji 语义。                                                                                                                                                                                                                                                                                        |
| 3.3 导演画布、字体/预设、软硬隔离       | 生产版 `smartsub-compose-canvas-e2e-aIfTeo` 通过实际指针拖动/键盘微调/Esc、数值中点、两种安全遮罩、文件/媒体故障修复、原生定位 ASS 保持 X 且 Y 不跳；真实硬字幕像素定位，软轨可独立提取，MP4 视频 packet SHA-256 保持。字体菜单、嵌入字体、预设、输出恢复、多窗口与重连专项见前文，复杂 4K/异体字体极端组合并未穷举。                                                                                                                                                                                                       |
| 3.4 角色矩阵、音色库/试听、自适应       | MiMo 生产专项 `smartsub-mimo-library-e2e-Ordljl`：1200 音色虚拟化/筛选、完成式 base64 WAV 的三秒回放、缓存、悬停取消、失败重试及实际拒写/重新合成恢复。使用本机协议夹具，不是商业 API 成功率证据。真实 VITS AIShell3 `smartsub-local-preview-e2e-gczcdd`：官方模型、实际 sherpa 原生 worker、非零 PCM 和 Chromium 回放（三秒）、角色 DSP、缓存隔离、取消后再次合成。发现并修复 no-pack 生产运行找错 extraResources：现在按 `app.isPackaged` 选择目录，4 个组合测试覆盖打包/非打包与环境变量；不再误报“原生库未安装”。       |
| 3.4 自动适应及导出                      | 生产版 `smartsub-auto-fit-e2e-3DmxYX` 通过轻微压缩、过长红条、磁盘失败恢复、借空白避碰、AI 失败/取消/重试、真实 PCM 句尾与后句时间、重复 WAV/MP3 与字幕碰撞保护、导出拒写重试和重载。真人语音专项 `smartsub-auto-fit-L691xd` 使用授权目录 15 秒参考原声：5%/10%/15% 压缩实际为 14286/13637/13044ms，PCM 非静音且数值合法。原波形/尾标记测试仍通过；未凭时长断言“听感无损”，产物保留供听审。                                                                                                                                 |
| 3.5 下载接流水线、工具行动条/批量       | 已有实际 yt-dlp 本机 HTTP 下载、原生双语轨/缺轨回落、失败恢复和配方接续验证，及八工具批量/失败重试/后续任务入口。同日后续已通过 YouTube 公开视频及英/德字幕、B站公开视频真实下载和解码，并修复弹幕 XML 转 SRT 导致下载失败；样例与限制见末尾补充。                                                                                                                                                                                                                                                                          |
| 4 视觉层级/尺寸                         | Panel 使用 `bg-muted/40`，内部 Card 使用 `bg-card`；移除面板及设置分区、任务/校对列表、引擎与工具选项的重复装饰框，保留输入、焦点、选择/错误与拖放边界。生产版 `smartsub-ergonomics-e2e-77G6iU`：23 路由 × 两主题 × 1024×700/1440×900，共 92 视图，无 pageerror/页面横向溢出/检测到的不可滚动控件裁切；已查看浅色引擎、深色任务/配音及加载后的复杂字幕代表截图。首屏几何巡检不是所有弹窗/状态的穷举证明。                                                                                                                   |
| 4 常驻错误/键盘                         | 设置、三类服务商、引擎操作、任务/工程读取和输出流程具有常驻详情与修复/重试。引擎生产专项 `smartsub-engine-settings-e2e-PAljW3` 通过九组读取、未失焦守卫、真实 chmod 拒写、坏确认、参数/操作失败及云服务错误隔离，零 pageerror。复杂字幕中角色菜单按需挂载，Enter 打开/Esc 还焦点/点击开关及 CPS 聚焦提示通过；保留输入、搜索、弹窗快捷键隔离和强杀恢复。后续原生 Windows/Ubuntu 托管 CI 已通过键盘与强杀恢复，见末尾；实体硬件手工验收仍未进行。                                                                            |

本轮最终生产构建成功；全量 Jest **43 套件 446 项**、集中基线 **35 组命令**、类型/i18n/diff 检查通过。最后仅涉及 Unicode 计数和角色徽章布局的小改在构建与对应专项再次验证；不将此前全量结果伪称为每次小改后的重跑。构建仍有已有 browserslist、JASSUB chunk 循环、大语言资源页及 i18next 初始化警告。测试使用隔离配置，未覆盖日常配置；VITS 临时模型约 31.6MB 压缩包，不加入仓库。

#### 万行性能证据与未通过项

`SubtitleList` 稳定过滤映射和 getItemKey，避免滚动时无谓失效万行测量；使用虚拟器直接更新位置、40px 紧凑行与动态展开行，角色弹窗只在打开时挂载，CPS 提示保留可聚焦说明。1024 的多个角色徽章限制宽度，给正文留空间。合成的五小时音频具有静音间隔，10000 行含中日韩/阿拉伯语、双行、8 个角色与重叠归属；通过真实 UI 保留 50 个 AI Diff，AI 服务边界使用确定性 IPC 夹具。搜索/编辑/草稿/强杀/恢复走真实代码。

测量脚本分别报告 120px/帧滚动、600px/帧压力滚动、页面空闲与空白窗口。RAF 时间间隔是渲染调度代理指标，不等于 GPU 合成器帧轨迹；`--profile` 可输出 CPU profile。之前只用 performance.now 的数据也保留为历史观测，不能无条件横向比较。稳定 60 FPS 尚无证据，路线图 P3-03 保持未勾选。同步约 4.09MB localStorage 草稿写入仍有约 15ms 开销；保留即时原子恢复语义，未引入延迟写入来掩盖数据丢失窗口。

最终复杂工程证据 **`smartsub-keyboard-perf-e2e-n3IhkZ`**：载入 **3226.5ms**，搜索末行 **132.3ms**，32 次输入到下一 RAF 中位 **21.6ms / P95 23.4ms / 最大 23.5ms**；同步草稿最大 **15ms / 4,085,217 字符**（原脚本字段名 bytes，实际为 JS 字符串 length，并非编码后的字节数）。50 行 AI Diff 保留与剩余批次取消约 **225.7ms**。JS heap used **45.3MB**（未强制 GC）。

| 尺寸/负载      | 平均 RAF FPS | P95 / 最大间隔 | >25ms 帧数（179 个间隔） | DOM 最大行数 |
| -------------- | -----------: | -------------- | -----------------------: | -----------: |
| 1024 空闲      |        59.01 | 20.0 / 28.1ms  |                        2 |           12 |
| 1024，120px/帧 |        59.01 | 19.5 / 28.9ms  |                        4 |           29 |
| 1024，600px/帧 |        60.04 | 18.2 / 18.7ms  |                        0 |           29 |
| 1440 空闲      |        59.36 | 19.6 / 29.6ms  |                        2 |           12 |
| 1440，120px/帧 |        59.32 | 18.4 / 31.8ms  |                        2 |           34 |
| 1440，600px/帧 |        60.00 | 18.3 / 18.7ms  |                        0 |           34 |
| 空白窗口对照   |        59.69 | 18.6 / 23.6ms  |                        — |            — |

本轮测试还验证角色按钮键盘/鼠标交互、CPS 可聚焦提示、最小尺寸给字幕正文保留宽度、过滤后的连续编辑、搜索输入原生撤销、媒体实际播放状态、面板折叠、整应用 SIGKILL 恢复最后输入并真实保存。复测波动存在：此前同版本接近的 `smartsub-keyboard-perf-e2e-BsXGBX` 空白窗口只有 58.09 FPS。最新一次高速滚动达到平均 60 FPS，不能据此宣称任意复杂工程稳定 60 FPS，P3-03 保留未完成。

本阶段提交的是已实现且可复查的功能和可靠性改进，不再无限期压住全部代码等待外部条件；后续仍需独立机器/合成器帧轨迹确认性能目标、Windows/Linux 真机快捷键、YouTube/B站实际联网兼容性与语音主观听审。上述外部验证限制不冒充通过，总目标不标记全部完成。

### 后续修复与验收补充（2026-09-21）

本节更新上方“合成器轨迹缺失”和“外站未验”的状态。已补真实 Chromium 轨迹和 YouTube/B站公开链接下载，修复两个实际缺陷；稳定 60 FPS、Windows/Linux 真机与语音主观听审仍未完成。

#### 万行列表行高修复与合成器测量

- 新几何断言发现：50 个 AI Diff 保留时点击“展开全部”，旧行会按 40px 估算定位而真实高度达 258–290px，产生重叠。原先清空缓存后调用 `measureElement`，在虚拟器仍处于滚动状态时会跳过同步测量；高度未变的 AI 行也不会收到新的 ResizeObserver 回调。现在在 layout effect 内先批量读取当前展开行高度，再清空并回填缓存；紧凑行直接使用固定 40px，行容器隔离布局与样式计算。没有延迟草稿持久化。
- 性能专项增加展开/收起/字号切换后的相邻行几何检查、3000px/s 的 Chromium 原生滚轮输入（AI Diff 区与紧凑区各 9000px）、滚动后视口覆盖，以及 `--trace` 输出的 `PipelineReporter`、`Display::FrameDisplayed`、`FrameSequenceTrackerV3`。自动记录 Electron/Chromium、屏幕刷新率和 GPU 功能。CPU profile 与合成器采集分开运行；`storageMaxCharacters` 纠正原 bytes 字段的实际单位。
- `compositor-trace.mjs` 根据页面阶段标记和 renderer PID 汇总；pipeline 可能在同一显示周期有多个记录，不能直接作为帧数相加。只用完整的 Chromium WheelScroll 序列判断 dropped/expected，计数缺失或序列不完整均不视为零丢帧。三个 Node 测试覆盖跨进程 ID、重复 pipeline、缺失计数、真实丢帧、缺失内容和不完整轨迹，并加入集中基线。
- 最新生产证据 **`smartsub-keyboard-perf-e2e-Kvan8D`**：macOS arm64，Electron 30.5.1 / Chromium 124.0.6367.243，60Hz、2×显示器，GPU 合成与栅格化启用。载入 3201.6ms、搜索末行 219.6ms；32 次输入中位 18.9ms / P95 20.7ms / 最大 20.8ms；原子草稿写入最大 13.4ms / 4,085,217 字符；JS heap used 47.8MB（非强制 GC）。两尺寸几何检查、角色弹窗、CPS 提示、键盘/搜索、真实音频播放、强杀恢复最后输入和真实保存均通过，截图已查看。

| 原生滚轮负载          | 呈现平均 FPS | P95 / 最大呈现间隔 | Chromium 丢帧 / 期望帧 | 主线程帧工作 P95 / 最大 |
| --------------------- | -----------: | ------------------ | ---------------------: | ----------------------- |
| 1024，AI Diff 区      |        58.72 | 18.93 / 50.31ms    |                0 / 178 | 12.83 / 16.20ms         |
| 1024，紧凑区          |        59.03 | 21.78 / 26.13ms    |                0 / 176 | 5.32 / 6.56ms           |
| 1440，AI Diff 区      |        58.38 | 22.33 / 26.93ms    |                0 / 176 | 12.09 / 14.36ms         |
| 1440，紧凑区          |        59.71 | 17.79 / 27.07ms    |                0 / 179 | 5.36 / 5.80ms           |
| 1024，纯 CSS 滚动对照 |        59.19 | 19.75 / 43.89ms    |         无完整序列计数 | —                       |
| 1440，纯 CSS 滚动对照 |        59.18 | 20.37 / 25.56ms    |         无完整序列计数 | —                       |

四段真实滚轮的 Chromium 缺失内容均为 0；纯 CSS 对照没有完整 WheelScroll 计数，不假报零丢帧。120px/帧脚本滚动的两尺寸均有 180 个完整呈现 pipeline；600px/帧压力滚动仍各有一个 dropped pipeline 和 partial 记录。此前同一修复版 **`smartsub-keyboard-perf-e2e-9y34Yz`** 的 1024 紧凑滚轮有 **1/175** 丢帧，1440 两段零丢帧。保留这份反例，不用最新四段零丢帧覆盖历史波动。采集本身有开销，低成本 CSS 对照也抖动，不能把全部波动归咎于应用或宣称固定 60 FPS；**P3-03 仍未勾选**。

复现：先 `npm run build`，关闭开发服务及其他测试，再执行 `npm run test:e2e:proofread-keyboard -- --production --complex --trace`。结果目录含逐尺寸 `.trace.json`、`.summary.json`、截图与 `result.json`；可用 `node scripts/e2e/compositor-trace.mjs <trace.json>` 独立重算。当前键盘脚本运行于 macOS，Windows/Linux 真机仍需对应平台验证。

#### YouTube/B站联网验证与弹幕下载修复

- 实际 yt-dlp **2026.08.19**、应用生产版、隔离配置、未导入登录 Cookie。旧 YouTube 测试视频 `BaW_jenozKc` 返回 unavailable；改用仍公开的 `jNQXAC9IVRw`。首次真实 B 站下载在 `--write-subs --sub-langs all,-live_chat --convert-subs srt` 下把 `danmaku.xml` 交给 FFmpeg，报 Invalid data 并终止整个任务，证据 **`smartsub-download-live-e2e-R3rNpB`**。
- 将 `live_chat`、`rechat`、`danmaku` 的排除列表共用于下载参数、预检语言和最终字幕产物解析。弹幕不会尝试转 SRT，也不会误交给后续转写/翻译。126 项下载解析测试及适配器测试通过，包含弹幕被伪装为 `.srt` 时仍不能认领。
- 修复后 YouTube **`smartsub-download-live-e2e-8LmJdB`**：`https://www.youtube.com/watch?v=jNQXAC9IVRw`，19 秒视频下载成功，FFmpeg 完整解码；德文 4 条、英文 6 条人工字幕转为 SRT 并认领。
- 修复后 B 站 **`smartsub-download-live-e2e-sM5Kqq`**：`https://www.bilibili.com/video/BV1xx411c7mD/`，2055.637 秒、88,071,163 字节 MP4 下载并完整解码；该匿名样例无可用人工字幕，`subtitlePaths=[]`，不把弹幕伪装成字幕。B 站登录字幕/受限内容和两站全部视频不由这两个样例代表。
- 生产版完整流水线 **`smartsub-download-pipeline-e2e-yu5rpi`**：真实 yt-dlp 访问本机 HTTP，页面同时声明两条字幕和弹幕 XML；弹幕请求数为 0，双语字幕接翻译、配音、可播放成片通过。缺字幕需显式上传同意再 ASR、依赖移除后修复、重启确认恢复且零重复下载/提交通过，两窗口尺寸无溢出。此专项的云服务是本机协议夹具，外站传输由上述联网专项证明。
- 新可选命令 `test:e2e:download-live` 使用 `SMARTSUB_E2E_YTDLP`（真实可执行文件）与 `SMARTSUB_E2E_DOWNLOAD_URLS`（JSON URL 数组）；`--production` 指定生产版，默认仅解析，`--download` 才下载及解码，可用 `SMARTSUB_E2E_PROXY` 设置隔离测试代理。远端状态变化会返回失败结果。两次中间复测出现 page closed，未获得进程诊断，未算通过；新增退出码/信号/stderr 证据采集后独立复测成功，不能据此认定退出原因已修复。

本轮生产构建、**43 套件 446 项 Jest**、**36 组集中基线**、类型/i18n/diff 检查通过。之后只调整 E2E 脚本和记录，最终流水线及带 CSS 对照的性能专项单独运行。保留 Windows/Linux 真机、语音主观听审、复杂 ASS/4K 极端组合及稳定 60 FPS 的验收边界；本轮不声称全部阶段验收完成。

### 角色交互、万行性能与跨平台草稿修复（2026-09-21）

本节接续 `8492115`、`d2cb974`、`a1f711a` 的实际修复。阶段 3 的六项功能已实现；阶段 4 的 P3-03 稳定 60 FPS 仍未完成。以下证据更新历史记录，不把托管虚拟机或单次流畅结果当作全部硬件验收。

#### 角色选择与展开行预渲染

- 修复角色弹层内点击复选框/说明文字会意外关闭的缺陷。现在可以连续修改多个角色归属，保留触发按钮开关、Escape 还焦点及父行事件隔离；组件测试和生产版真实交互通过。
- `subtitleVirtualRange` 根据展开状态约束视口外预渲染预算：紧凑行按 40px、展开行按 200px 估算，保持每侧约 400px 余量，所有可见行仍保留。估值仅决定预渲染数量，定位继续使用真实测量。顶部 50 个 AI Diff 区域的挂载行从 11–12 降至 3–4。
- 保留原有跳回顶部的 600px/帧压力测试，另外测量稳定区压力、顶部跳转和反向真实滚轮；未靠删除最慢帧改善结果。
- 优化后轨迹 `smartsub-keyboard-perf-e2e-2W79HH`：两个尺寸原压力段各 180 个完整呈现 pipeline，主线程帧工作 P95 为 6.400/6.624ms，最大 7.140/9.228ms；顶部跳转最大 5.731/6.506ms。
- 加入反向滚轮后的 `smartsub-keyboard-perf-e2e-NWwKld`：原压力段仍各 180 个完整 pipeline，主线程 P95 6.553/6.828ms、最大 8.828/10.093ms；六段正反向真实滚轮计数完整且 dropped/missing_content 均为 0。反向回到 AI Diff 区域最大主线程工作 14.801/13.764ms。两个尺寸的 AI Diff 截图已查看，行高/间隙及视口覆盖断言通过。

上述两份轨迹采集于原生草稿修复之前，不能代替修改持久化后的性能复测。普通滚动呈现时间仍有抖动；此前掉帧反例保留。Linux CI 软件渲染压力段也未达到 60 FPS，P3-03 不勾选。

#### 强杀恢复中的实际数据丢失

新增 `.github/workflows/proofread-platforms.yml`，在 Windows 2022 与 Ubuntu 22.04 原生运行生产 Electron，使用平台实际 Ctrl 快捷键和进程终止方式，Ubuntu 使用 Xvfb/虚拟音频输出。测试包含五小时音频、万行字幕、两个窗口尺寸、连续编辑/撤销、搜索、角色交互、50 个 AI Diff、重启恢复和真实保存。

第一次运行 `35600096288` 在恢复后搜索失败。第二次诊断运行 `35600791802` 确认：强杀前内存中完整的最后一行，在重启后回退为较旧内容。Linux 只保留到 `Original 9999. Needle9999 `，Windows 只保留到 `thirty keyboard even`。因此之前关于 localStorage 即时写入即可保证强杀恢复的表述不成立：API 返回并不意味着 Chromium 已完成磁盘持久化。此外 Windows 的 npm/PowerShell 参数转发丢失了 `--complex`，改为直接执行 Node 脚本；旧 Windows 结果不能算复杂工程测试。

`a1f711a` 将草稿改为受限 preload API → 主进程同步写入私有临时文件 → fsync → 原子替换，成功后才确认。文件保存在当前 userData 的 `proofread-drafts` 中，文件名由工程键 SHA-256 得出，不接受任意目标路径。分块序列化缓存仍保留，但磁盘提交为完整快照。可读取旧 localStorage 草稿；成功写入后清除旧缓存，保存/丢弃通过持久化清除记录防止旧缓存复活。读取失败阻止加载和保存，写入失败保留内存修改及常驻提示，清除失败不关闭恢复对话框或误报保存成功。

测试新增原子写入、fsync/替换失败保留原稿、临时文件清理、旧稿迁移/清除记录、原生读取失败与重试、保存清除失败与重试。生产 E2E 在所有滚动结束后再次输入 `CRASH_LAST_KEY`，不增加等待或浏览器存储刷新，立即读取证据并强杀；重启比较草稿、恢复后搜索及最终 SRT 保存。

macOS 生产版 `smartsub-keyboard-perf-e2e-E8Frsq` 通过上述流程：载入 3205.9ms，末行搜索 219.0ms，32 次输入到下一 RAF 中位 26.0ms / P95 28.1ms / 最大 29.3ms；原生写入主进程最大 15.727ms，快照 4,085,252 字符。该存储耗时只含主进程落盘，不含 IPC/渲染；总输入指标包含实际阻塞。同步落盘有成本，不宣称输入也满足单帧 16.7ms。

最终生产构建、**46 套件 458 项 Jest**、类型检查、校对可靠性专项与 diff 检查通过。全量测试曾发现“草稿读取失败仍允许保存”的保护缺口，修复后已全量重跑。此前 36 组集中基线不是本次持久化修复后的重跑。

#### 最终复测结果

- **Windows 2022、Ubuntu 22.04 均通过**：[Actions 35602210728](https://github.com/buxuku/SmartSub/actions/runs/35602210728)，验证提交 `a1f711a`。两端 `result.json` 均确认复杂多语言/八角色/50 Diff 工作负载，强杀前后快照一致，恢复搜索和真实保存通过。Linux 压力段 RAF 为 45.70/42.79 FPS，输入 P95 83.1ms；Windows 为 62.63/61.59 FPS，输入 P95 90.8ms，虚拟显示计时不作为物理屏幕帧率证据。托管 VM 不代替实体设备的输入法、GPU 和主观操作验收；截图/JSON 作为 Actions artifact 保存 14 天。
- 原生持久化后的 macOS 合成器专项 **`smartsub-keyboard-perf-e2e-DHA5tC`**：1024/1440 原压力段各 180 个完整 pipeline，主线程 P95 **6.775/6.451ms**、最大 **8.567/8.916ms**；稳定区压力最大 **7.621/7.159ms**，顶部跳转最大 **5.539/6.410ms**。六段真实滚轮 dropped/missing_content 均为 0，完整 expected 计数依次为 180/178/179 和 180/180/179。反向滚轮主线程最大 **15.451/14.142ms**，呈现间隔最大仍出现 **32.393ms**；零 Chromium 掉帧计数不等于每一帧间隔都不超过 16.7ms。CSS 对照约 60.11/59.71 FPS，P3-03 继续保留未完成。
- 真实文件系统故障专项 **`smartsub-draft-storage-e2e-3dZLYn`**：把隔离 profile 的草稿目录替换为普通文件以制造真实读写失败。旧快照保持、最新 UI 文本保留及常驻警告通过；修复目录后再次编辑可落盘。刷新后读取失败阻止保存，重新加载进入恢复对话框，放弃失败时提示仍在对话框内，修复后恢复并保存 SRT 成功。随后重新注入旧 localStorage 值并刷新，持久化清除记录成功阻止旧稿复活。复现：`node scripts/e2e/proofread-draft-storage.mjs --production`。

性能/键盘完整复现：`node scripts/e2e/proofread-keyboard-performance.mjs --production --complex --trace`；Windows 使用直接 Node 命令避免 PowerShell/npm 转发丢失参数。所有专项使用隔离 profile，未修改日常配置。剩余边界为稳定 60 FPS、多硬件手工测试、复杂 ASS/4K 极端组合和语音主观听审；本轮没有将这些项目冒充为通过。
