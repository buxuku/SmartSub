# B14 零碎收尾实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. 决策细节见 `docs/superpowers/specs/2026-06-12-b14-cleanup-design.md`(决策 #1-#11),本计划按 task 引用决策编号。

**Goal:** 清完 P1 存量(#35)与 P2 解释/打磨类积压,消除「猜含义」摩擦。

**门禁(每 task commit 前):** `node scripts/check-i18n.mjs` 通过;renderer 非测试 TSC 错误 0(`npx tsc --noEmit -p renderer/tsconfig.json 2>&1 | grep -v -E "__tests__|\.test\.|\.spec\." | grep -c "error TS"` → 0);main TSC `^main/` ≤95。一 task 一 commit。

---

## Task 1: P1#35 工程状态单源推导(决策 #1)

**Files:** `renderer/pages/[locale]/home.tsx`(getProjectStatus L116-136 及调用点 L419)

- `getProjectStatus(project)` 移除 userConfig 参数;`getFileStages(file, typeDef, undefined)`;调用点同步。userConfig state 若仅此一处消费则连带清理(先 grep 确认)。
- Commit: `fix(launchpad): derive project status from its own task type, not global config`

## Task 2: 匹配度自解释 + 评分图例 + 档位图标(决策 #2/#3)

**Files:** `renderer/components/proofread/ProofreadFileList.tsx`(L481-483、L546-548)、`renderer/components/resources/ModelsTab.tsx`(L54-58、L247-259、L358-363)、zh/en `home.json` + `modelsControl.json`

- ProofreadFileList 两处 `{s.confidence}%` → `t('matchConfidence', { percent: s.confidence })`(「{{percent}}% 匹配」);两个下拉 label 行加 HelpCircle+Tooltip(key `matchConfidenceTip`,解释 95=同名/90=语言后缀/其余按相似度)。
- ModelsTab:速度/精度 span 包 Tooltip(key `speedRatingTip`/`qualityRatingTip`);MODEL_TIERS emoji 字段换 lucide 组件引用(Rocket/Scale/Crosshair),渲染处 `<span className="text-sm">{tier.emoji}</span>` 改 icon 渲染。
- Commit: `feat(resources): explain match confidence, rating dots and replace emoji tier icons`

## Task 3: VAD 预设三档 + 0 语义 + 回填修复(决策 #4)

**Files:** `renderer/pages/[locale]/settings.tsx`(L125-156 state 回填、L578-773 VAD 区)、zh/en `settings.json`

- VAD 开关下加预设行:三个 outline 按钮(安静环境/标准·推荐/嘈杂环境)+「恢复默认」ghost 按钮;数值见决策 #4 表;点击批量调 `handleVADSettingChange` 六次(或抽批量函数复用 pending/debounce 机制);当前六值与某档全等时该按钮高亮(variant secondary)。
- `vadMaxSpeechDurationTip` 文案明确「0 表示不限制」。
- L150-156 回填 `||` → `??`。
- i18n key:`vadPresets`(标签)、`vadPresetQuiet/Standard/Noisy`、`vadPresetReset`,zh/en 对等。
- Commit: `feat(settings): one-click VAD environment presets with documented values`

## Task 4: 下载失败提示(决策 #5)

**Files:** `renderer/components/DownModel.tsx`、`renderer/components/DownModelButton.tsx`、zh/en `modelsControl.json`

- DownModel:`result.success === false` 时 `toast.error`——error==='anotherDownloadInProgress' 用 key `downloadBusy`,否则 `downloadFailedToast`(插值 error);import { toast } from 'sonner'。
- DownModelButton:props 已收 detail;`!loading && detail?.status === 'error'` 渲染重试态(RefreshCw + `t('retryDownload')`,onClick=handleDownModel);取消(idle)不渲染失败态。
- Commit: `feat(models): surface download failures with toast and inline retry`

## Task 5: 语向检测统一(决策 #6)

**Files:** 新建 `renderer/lib/subtitleLangUtils.ts`(或并入 proofreadUtils);改 `ProofreadImport.tsx` L118-119、`ProofreadFileList.tsx` L243-245、`proofreadUtils.ts` L204-210、main `subtitleDetector.ts` L135 与 L249-265、`languageDetector.ts` detectLanguagePair

- 函数语义:`classify(lang, src, tgt)`:lang===src→source;lang===tgt→translated;否则 en→source / 其他→translated / 无 lang→unknown。renderer 端调用方 invoke getUserConfig 取 sourceLanguage/targetLanguage;main 端读 store('userConfig')。main/renderer 各放一份同语义实现(无法共享模块,注释互指)。
- Commit: `fix(proofread): classify subtitle language against user task languages, not en-hardcode`

## Task 6: 黄色警告中性化 + 概览整卡可点(决策 #7/#11)

**Files:** `renderer/components/settings/GpuAccelerationCard.tsx`(deriveStatus L409-443、statusToneClasses、crashTip L1013-1018)、`renderer/components/resources/OverviewTab.tsx`(三 Card L131-265)

- deriveStatus 两个 yellow 分支:`gpuMode === 'auto'` 时 tone 改 'neutral'(图标随 tone:neutral 用 Info);非 auto 保持 yellow。crashTip 块 `bg-warning/10 border-warning/30` → `bg-muted/50` + Info 图标。
- OverviewTab 三 Card:onClick + cursor-pointer + hover:shadow-md transition-shadow + role="button" + tabIndex={0} + onKeyDown(Enter/Space);卡内 DownModel 区与「去启用」按钮 stopPropagation。
- Commit: `refactor(resources): neutral tone for expected GPU fallback, clickable overview cards`

## Task 7: 完成率文案 + 当前字幕卡移除 + 格式统一(决策 #9/#10/#8)

**Files:** `renderer/components/subtitle/VideoInfo.tsx`、删除 `renderer/components/subtitle/CurrentSubtitle.tsx`、`renderer/components/proofread/ProofreadEditor.tsx`(L22/L323-330 及 currentTime/hasTranslationFile 解构)、zh/en `home.json`(completionRate 改文案、currentSubtitle/noCurrentSubtitle 删键)、zh/en `tasks.json`(empty.mediaFormats/subtitleFormats)、`main/helpers/ipcHandlers.ts`(selectFile L366-392 / selectFiles L422-448 filters)

- VideoInfo:合并已翻译行,`{t('completionRate')}: {withTranslation}/{total} ({percent}%)`,completionRate 文案改「已翻译」/「Translated」,删独立 translated 行(key `translated` 若无他处引用一并删)。
- ProofreadEditor 删 CurrentSubtitle import/JSX 与孤儿解构;删组件文件;删 i18n 死键(grep 确认无引用)。
- tasks.json 文案按决策 #8;ipcHandlers 两个对话框 extensions 改 `MEDIA_EXTENSIONS.map(e => e.substring(1))`。
- Commit: `refactor(proofread): drop redundant current-subtitle card, honest format hints, unified file dialogs`

## Task 8: 门禁复核 + 进度文档

- 三门禁全量;更新 `docs/UX_REFACTOR_PROGRESS.md`(批次表加 B14、§4 划掉已完成项、§5 指向 B15)。
- Commit: `docs: mark B14 cleanup batch complete in progress handover`

## 自审

- 决策 #1-#11 全部映射到 Task 1-7;#2 复制 tooltip 已存在跳过(探索复核);范围外清单见设计文档。
- 类型一致性:`classify` 函数签名 renderer/main 同义;i18n key 命名在各 task 内自洽。
