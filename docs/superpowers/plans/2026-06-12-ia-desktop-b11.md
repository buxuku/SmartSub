# 批次 11 实施计划：信息架构与桌面公民身份

对应设计：`docs/superpowers/specs/2026-06-12-ia-desktop-b11-design.md`

## T1 PageHeader/EmptyState + 六页接入

**文件**：新建 `renderer/components/PageHeader.tsx`、`renderer/components/EmptyState.tsx`；改 `home.tsx`、`resources.tsx`、`settings.tsx`、`subtitleMerge.tsx`、`proofread.tsx`、`common.json`(zh/en subtitleProofread)、`subtitleMerge.json`(头部 key)、`settings.json`(描述 key)。
**验证**：五页头部视觉一致（h1 字号/描述色）；合成页出现头部；最近任务空态为 EmptyState；侧边栏与启动台卡片均显示「校对字幕」。

## T2 应用菜单本地化

**文件**：新建 `main/helpers/menu.ts`（buildAppMenu + zh/en 字典）；`main/background.ts` ready 后调用；`main/helpers/ipcStoreHandlers.ts` setSettings 拦截 language 重建；`renderer/components/Layout.tsx` 监听 menu-check-updates / menu-open-logs。
**验证**：TSC；切语言菜单即时变化；编辑菜单复制粘贴可用（实机）。

## T3 更新去重 + 手动检查

**文件**：`main/helpers/updater.ts`（删 downloaded dialog、删 errorBox）；新建 `renderer/hooks/useUpdateCheck.ts`；`Layout.tsx`（帮助菜单项+火箭改徽章）；`UpdateDialog.tsx`（platform 判断）；common.json 增 key。
**验证**：手动检查四态 toast；下载完成仅一个 toast；徽章 primary 色。

## T4 加速徽章平台分支

**文件**：`Layout.tsx`（gpuCapable 含 darwin、backend 标签、CPU 中性文案）；common.json。
**验证**：mac 显示「Metal/CoreML 加速」绿徽章（实机）；cpu 态显示中性「CPU 模式」。

## T5 设置页三件套

**文件**：`settings.tsx`（关于卡、模型路径改链接行、删 GPU 过渡卡）；settings.json(zh/en)。
**验证**：关于卡含版本/检查更新/GitHub/日志；模型路径只剩链接；GPU 过渡卡消失。

## T6 最近任务查看全部

**文件**：`home.tsx`、`launchpad.json`(zh/en)。
**验证**：>8 工程时出现「查看全部 (N)」；展开有搜索；过滤即时生效；收起恢复 8 条。

## T7 i18n + 门禁 + 交接

`yarn check:i18n` 通过；renderer TSC 0；main TSC ≤95 基线；每 task 一 commit；interactive_feedback 交接附实机验证清单。
